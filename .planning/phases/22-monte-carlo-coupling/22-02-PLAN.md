---
phase: 22-monte-carlo-coupling
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - scripts/create_notebook_17.py
  - notebooks/17_mc_coupling.ipynb
autonomous: false

must_haves:
  truths:
    - "User can see a synthetic MC event dataset loaded and processed through the full pipeline"
    - "User can see a pulse height distribution histogram from 1000+ events"
    - "User can see CCE vs LET overlay comparing lookup table to per-event results"
    - "User can see summary statistics (mean collected energy, CCE distribution, event counts)"
  artifacts:
    - path: "scripts/create_notebook_17.py"
      provides: "Generator script for notebook 17"
      min_lines: 100
    - path: "notebooks/17_mc_coupling.ipynb"
      provides: "Executed publication-quality notebook with MC coupling demonstration"
      min_lines: 50
  key_links:
    - from: "notebooks/17_mc_coupling.ipynb"
      to: "src/mc_coupling.py"
      via: "imports load_mc_events_csv, process_mc_ensemble, pulse_height_distribution"
      pattern: "from src.mc_coupling import"
    - from: "notebooks/17_mc_coupling.ipynb"
      to: "data/cce_let_table_100um.json"
      via: "load_cce_let_table for CCE interpolation"
      pattern: "load_cce_let_table"
---

<objective>
Create publication-quality notebook 17 demonstrating the MC coupling pipeline: synthetic event generation, CSV import, batch CCE lookup, and pulse height distribution visualization.

Purpose: Provide a reproducible demonstration of the MC coupling workflow that serves as both documentation and a template for users to adapt to their own Geant4/FLUKA output files.

Output: scripts/create_notebook_17.py generator + notebooks/17_mc_coupling.ipynb executed notebook.
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-monte-carlo-coupling/22-RESEARCH.md
@.planning/phases/22-monte-carlo-coupling/22-01-SUMMARY.md
@.planning/phases/21-single-particle-transient/21-02-SUMMARY.md

<interfaces>
<!-- mc_coupling.py public API (created in Plan 22-01) -->

From src/mc_coupling.py:

```python
def load_mc_events_csv(filepath, column_map=None, pos_unit="cm", energy_unit="keV"):
    """Returns DataFrame with columns: event_id, x_cm, y_cm, z_cm, edep_keV"""

def process_mc_ensemble(events_df, cce_interp, sv_thickness_um=10.0):
    """Returns dict: event_energies_keV, event_LET_keV_um, event_CCE, event_collected_keV, n_events, n_zero_energy"""

def pulse_height_distribution(collected_energies_keV, n_bins=200, e_min=None, e_max=None):
    """Returns dict: bin_centers_keV, counts, bin_edges_keV"""
```

From src/single_particle.py:

```python
def load_cce_let_table(filepath):
    """Returns (cce_interp, metadata) where cce_interp(LET_keV_um) -> CCE"""
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notebook 17 with MC coupling pipeline demonstration</name>
  <files>scripts/create_notebook_17.py, notebooks/17_mc_coupling.ipynb</files>
  <action>
Create scripts/create_notebook_17.py that generates and executes notebook 17. Follow the exact pattern from scripts/create_notebook_16.py (nbformat cells, nbconvert execution, timeout).

Notebook structure (approximately 15-18 cells):

**Cell 1 (markdown):** Title "Monte Carlo Coupling: From Energy Deposition to Pulse Height Distribution". Brief intro explaining the pipeline: MC transport -> energy deposition -> CCE lookup -> collected charge -> PHD.

**Cell 2 (code):** Imports: numpy, pandas, matplotlib, from src.single_particle import load_cce_let_table, from src.mc_coupling import load_mc_events_csv, process_mc_ensemble, pulse_height_distribution.

**Cell 3 (markdown):** Section: "1. Load CCE(LET) Lookup Table". Explain the pre-computed table from Phase 21.

**Cell 4 (code):** Load CCE table from data/cce_let_table_100um.json. Print metadata (geometry, bias, LET range, number of points).

**Cell 5 (markdown):** Section: "2. Generate Synthetic MC Events". Explain that real Geant4/FLUKA data would be loaded here; we generate synthetic data to demonstrate the pipeline.

**Cell 6 (code):** Generate synthetic dataset: 2000 events, each with 1-10 random steps. For each event, sample a total energy from a realistic distribution: mix of (a) proton-like events (mean ~50 keV, sigma ~20 keV, 70% of events) and (b) heavy-ion-like events (mean ~500 keV, sigma ~200 keV, 30% of events). Distribute each event's total energy across its steps. Save to a temporary CSV. This creates a bimodal distribution typical of mixed-field microdosimetry.

**Cell 7 (code):** Load the synthetic CSV via load_mc_events_csv. Print DataFrame shape, first few rows, number of unique events.

**Cell 8 (markdown):** Section: "3. Process Events Through CCE(LET) Lookup"

**Cell 9 (code):** Call process_mc_ensemble with the loaded events and CCE interpolator. Print summary: n_events, n_zero_energy, LET range, CCE range, mean/median collected energy.

**Cell 10 (markdown):** Section: "4. Pulse Height Distribution"

**Cell 11 (code):** Figure 1 -- Pulse height distribution. Call pulse_height_distribution(result["event_collected_keV"]). Plot as step histogram (plt.step or plt.bar) with log-x axis. Labels: "Collected Energy (keV)" x-axis, "Counts" y-axis. Title: "Pulse Height Distribution (2000 events, 100 um SV)". Use publication-quality formatting (fig size 8x5, fontsize 12, grid).

**Cell 12 (markdown):** Section: "5. CCE Analysis"

**Cell 13 (code):** Figure 2 -- Two-panel figure:

- Left: CCE vs LET scatter plot of individual events overlaid on the CCE(LET) lookup curve. Events as semi-transparent dots, lookup as solid line.
- Right: Histogram of CCE values across all events.
  Publication formatting, shared figure.

**Cell 14 (markdown):** Section: "6. Energy Deposition vs Collection"

**Cell 15 (code):** Figure 3 -- Deposited vs collected energy scatter plot. Plot deposited_keV (x) vs collected_keV (y). Add y=x reference line. Color by CCE value. Colorbar. Shows how CCE modulates the energy response.

**Cell 16 (markdown):** Section: "7. Summary Statistics". Table showing: total events, zero-energy events filtered, LET range, mean CCE, mean deposited energy, mean collected energy, processing time.

**Cell 17 (code):** Print summary statistics table. Also demonstrate how a user would adapt this for real Geant4 ROOT files (code comment block showing load_mc_events_root usage).

**Cell 18 (markdown):** Conclusion: The MC coupling pipeline processes thousands of events in sub-second time using the pre-computed CCE(LET) table. For Phase 23, these collected energies will be converted to lineal energy spectra.

Execute notebook via nbconvert with timeout=300 (this is pure numpy/pandas, no TCAD -- should be fast).
</action>
<verify>cd /Users/ngcex/projects/physics/petringa && python scripts/create_notebook_17.py && test -f notebooks/17_mc_coupling.ipynb</verify>
<done>Notebook 17 generated and executed with all figures rendered, 2000 synthetic events processed, pulse height distribution visible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of notebook 17 figures and science</name>
  <files>notebooks/17_mc_coupling.ipynb</files>
  <action>
Human verifies publication quality of notebook 17 MC coupling demonstration.
  </action>
  <verify>
    1. Open notebooks/17_mc_coupling.ipynb in Jupyter
    2. Verify Figure 1: Pulse height distribution shows bimodal shape (proton + heavy-ion peaks) on log-x scale
    3. Verify Figure 2: CCE vs LET scatter shows events following the lookup curve; CCE histogram shows distribution
    4. Verify Figure 3: Deposited vs collected energy scatter shows linear relationship modulated by CCE
    5. Verify summary statistics are printed and reasonable (2000 events, sub-second processing)
    6. Verify markdown text is clear and publication-quality
  </verify>
  <done>Human approves notebook 17 as publication-quality with correct scientific content</done>
</task>

</tasks>

<verification>
1. `test -f notebooks/17_mc_coupling.ipynb` -- notebook exists
2. `python -c "import json; nb=json.load(open('notebooks/17_mc_coupling.ipynb')); print(f'{len(nb[\"cells\"])} cells')"` -- verify cell count >= 15
3. Visual inspection of 3 figures in notebook
</verification>

<success_criteria>

- Notebook 17 exists with 15+ cells and 3 publication-quality figures
- Synthetic MC dataset demonstrates bimodal energy distribution
- Full pipeline runs in notebook without errors
- Human approves notebook quality and scientific content
  </success_criteria>

<output>
After completion, create `.planning/phases/22-monte-carlo-coupling/22-02-SUMMARY.md`
</output>
