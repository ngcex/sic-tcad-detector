---
phase: 40-geometry-viewer
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - app/workflows/field_map.py
  - tests/test_app_field_page.py
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03]
must_haves:
  truths:
    - "A 2D device config no longer hits st.stop() on the field-map page; run_field runs and its result is cached"
    - "For a 2D result, a quantity heatmap renders (no empty line charts)"
    - "For a 1D result, the existing E-field/potential line charts still render AND a geometry viewer (bar) renders below them"
    - "A quantity dropdown lets the user switch quantity and re-renders from the cached result without re-running run_field"
  artifacts:
    - path: "app/workflows/field_map.py"
      provides: "2D-through routing, dimensionality-branched render, quantity selectbox feeding build_geometry_figure"
      contains: "build_geometry_figure"
    - path: "tests/test_app_field_page.py"
      provides: "2D-route test, selectbox-no-resolve test, updated fakes with populated mesh"
      contains: "select("
  key_links:
    - from: "app/workflows/field_map.py"
      to: "app.components.geometry_viewer.build_geometry_figure"
      via: "import + st.plotly_chart(build_geometry_figure(result.mesh, quantity))"
      pattern: "build_geometry_figure\\(result\\.mesh"
    - from: "app/workflows/field_map.py"
      to: "st.session_state['field_result']"
      via: "read cached result on rerun (no re-solve on selectbox change)"
      pattern: "session_state.*field_result"
---

<objective>
Wire the geometry viewer (from plan 40-01) into the field-map page and remove the Phase-40 blocker.

Three integration seams (RESEARCH §2, §5):

1. REMOVE the hard 2D `st.stop()` guard (`if cfg.half_width_um is not None: st.warning(...); st.stop()`) — it currently makes VIZ-01 unreachable by short-circuiting every 2D config before `run_field` runs. Route 2D THROUGH `run_field`.
2. BRANCH the render on `result.mesh.y_coords is None`: 1D keeps the existing line charts + CSV download (they read `result.x`/`result.y`, populated for 1D) AND adds the geometry-viewer bar below (A1 SUPPLEMENT); 2D SKIPS the line charts and CSV (arrays are empty for 2D; `to_csv_bytes` raises ValueError with no 2D branch — A2 SKIP) and renders only the geometry heatmap.
3. ADD a quantity `st.selectbox` feeding `build_geometry_figure(result.mesh, quantity)`; on a selectbox change Streamlit reruns, the Run button returns False, the cached `session_state["field_result"]` is read, and only the griddata interpolation re-runs — NOT the devsim solve (VIZ-03 "without re-running").

Locked decisions (no CONTEXT.md — RESEARCH Assumptions Log adopted):

- A1: 1D geometry-viewer bar SUPPLEMENTS the existing line charts (does not replace).
- A2: 2D branch SKIPS the CSV download (2D CSV out of scope this phase).
- A6: dropdown default = "Electric field" (first QUANTITIES key).

Keep the `try/except RuntimeError` around `run_field` (2D `ramp_bias` may not converge — STATE.md blocker; same posture as Phase 39). Render the viewer only when `result.mesh is not None`.

Purpose: Make VIZ-01/VIZ-02/VIZ-03 user-observable on the actual page.
Output: modified `app/workflows/field_map.py`, extended `tests/test_app_field_page.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-geometry-viewer/40-RESEARCH.md
@.planning/phases/40-geometry-viewer/40-PATTERNS.md

<interfaces>
<!-- Contract produced by plan 40-01 (app/components/geometry_viewer.py) — consume directly. -->
```python
QUANTITIES: dict[str, str]  # {"Electric field": "ElectricField", "Net doping": "NetDoping",
                            #  "Electrostatic potential": "Potential"}; "Electric field" first
def build_geometry_figure(mesh: MeshData, quantity: str) -> go.Figure: ...
    # 2D (mesh.y_coords not None) -> go.Heatmap ; 1D (mesh.y_coords is None) -> go.Bar
    # reads only MeshData; pure; no st.*, no devsim
```
<!-- SimResult.mesh is MeshData | None, populated by run_field for BOTH 1D and 2D.
     For 2D, result.x / result.y are EMPTY arrays (all data routed to mesh). -->
```
Streamlit selectbox analog (persistent key survives reruns):
  st.selectbox("Quantity", list(QUANTITIES.keys()), index=0, key="geo_quantity")
```
</interfaces>

# The page being modified (read current state, do not assume):

@app/workflows/field_map.py

# The line-chart/CSV builders used on the 1D branch; to_csv_bytes has NO 2D branch (raises ValueError):

@app/components/results.py

# The AppTest file being extended (reuse the run_field mock seam):

@tests/test_app_field_page.py

# Selectbox widget analog (options list, index=0, persistent key=):

@app/components/device_sidebar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route 2D through, branch render, add quantity selectbox in field_map.py</name>
  <files>app/workflows/field_map.py</files>
  <read_first>
    - app/workflows/field_map.py (full current file — the module docstring lines 7-14 describe the OLD 2D pre-check rationale and must be rewritten; the `st.stop()` block lines 32-38 must be deleted; the render block lines 50-76 must be branched)
    - app/components/geometry_viewer.py (the QUANTITIES + build_geometry_figure contract from 40-01 — import both)
    - app/components/results.py lines 130-141 (proof that `to_csv_bytes` raises ValueError for a non-cv/cce/field OR empty 2D case has no branch — motivates skipping CSV on the 2D branch, A2)
    - app/components/device_sidebar.py lines 66-71 (selectbox widget shape: options list, index=0, persistent key=)
    - .planning/phases/40-geometry-viewer/40-RESEARCH.md §2 (exact required changes) and §5 (no-rerun mechanic)
  </read_first>
  <action>
    Modify `app/workflows/field_map.py` (all Streamlit calls stay inside `render()` — no module-level side effects, so AppTest.from_function keeps working):

    1. DELETE the 2D stop-guard block (`if cfg.half_width_um is not None: st.warning(...); st.stop()`, current lines 32-38). Do NOT replace it with a pre-check — 2D must flow into `run_field`.

    2. REWRITE the module docstring (currently states "The 2D guard here is CRITICAL ... 2D geometry viewer ... arrives in Phase 40"). New docstring: describe that the page routes BOTH 1D and 2D through `run_field`, branches the render on `result.mesh.y_coords is None`, keeps the `try/except RuntimeError` because 2D `ramp_bias` may not converge, and renders the geometry viewer from `result.mesh`.

    3. KEEP the `try/except RuntimeError` around `st.session_state["field_result"] = petringa.run_field(cfg)` exactly as-is (current lines 40-48). Reference `petringa.run_field` as a MODULE ATTRIBUTE (never `from petringa import run_field`) so the AppTest monkeypatch seam works (STATE.md:103).

    4. Import `from app.components.geometry_viewer import build_geometry_figure, QUANTITIES` at module top (alongside the existing `from app.components.results import build_field_figures, to_csv_bytes`).

    5. Replace the unconditional render block (current lines 50-76) with a dimensionality branch, guarded by `if result is not None and result.mesh is not None:`
       - 1D branch (`result.mesh.y_coords is None`): keep the existing `build_field_figures(result)` two line charts, the `to_csv_bytes` download button, and the "Net doping vs depth" expander UNCHANGED (they read `result.x`/`result.y`/`result.metadata`, populated for 1D). THEN, below them (A1 supplement), add the quantity selectbox + geometry viewer (step 6).
       - 2D branch (`result.mesh.y_coords is not None`): do NOT call `build_field_figures` or `to_csv_bytes` (empty arrays for 2D; `to_csv_bytes` has no 2D branch). Render ONLY the quantity selectbox + geometry viewer (step 6).

    6. In BOTH branches, add the quantity dropdown and chart (mirror device_sidebar.py selectbox shape): `quantity = st.selectbox("Quantity", list(QUANTITIES.keys()), index=0, key="geo_quantity")` then `st.plotly_chart(build_geometry_figure(result.mesh, quantity))`. The persistent `key="geo_quantity"` makes the selection survive reruns; combined with reading the cached `session_state["field_result"]`, this satisfies VIZ-03 "without re-running" (the Run button returns False on a selectbox rerun, so run_field is not called again). Do NOT add `@st.cache_data` (DeviceConfig is unhashable — Phase 39).

    Do NOT modify `app/components/results.py` in this plan (out of scope — 2D CSV is A2-deferred).

  </action>
  <verify>
    <automated>uv run python -c "import ast,sys; src=open('app/workflows/field_map.py').read(); assert 'st.stop()' not in src.split('run_field')[0] or 'half_width_um' not in src, 'old 2D stop-guard still present'; assert 'build_geometry_figure' in src and 'geo_quantity' in src; assert 'from petringa import run_field' not in src; ast.parse(src); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The old 2D warning string "These workflows are 1D-only" is GONE (`grep -c '1D-only' app/workflows/field_map.py` returns 0).
    - Source contains `from app.components.geometry_viewer import build_geometry_figure, QUANTITIES`.
    - Source contains `st.selectbox("Quantity"` with `key="geo_quantity"` and `index=0`.
    - Source contains `build_geometry_figure(result.mesh, quantity)`.
    - Source references the facade as `petringa.run_field` and does NOT contain `from petringa import run_field` (monkeypatch seam preserved).
    - Source still contains `except RuntimeError` (convergence guard kept).
    - Render is guarded by `result.mesh is not None` and branches on `result.mesh.y_coords is None`.
    - File parses (`ast.parse` in the verify command succeeds).
  </acceptance_criteria>
  <done>2D stop-guard removed; render branches 1D (line charts + CSV + supplemental bar) vs 2D (heatmap only); quantity selectbox with persistent key feeds build_geometry_figure; try/except RuntimeError and module-attribute run_field preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite field-page AppTests — 2D route, selectbox no-resolve, populated-mesh fakes (VIZ-03)</name>
  <files>tests/test_app_field_page.py</files>
  <read_first>
    - tests/test_app_field_page.py (full current file — `_fake_run_field` at lines 27-39 returns `mesh=None`, which will make the viewer branch skip; `test_2d_config_warns_and_skips` at lines 66-76 asserts the NOW-REMOVED behavior and must be deleted/replaced; keep the other three tests which remain valid for the 1D path)
    - app/workflows/field_map.py (the modified page from Task 1 — the render now guards on `result.mesh is not None` and branches on `result.mesh.y_coords`)
    - petringa/api/results.py lines 20-46 (MeshData + SimResult constructor signatures for building fakes)
    - .planning/phases/40-geometry-viewer/40-RESEARCH.md "AppTest accessor facts" (at.selectbox EXISTS; still NO plotly_chart/download_button accessor; assert on at.exception, at.selectbox, at.session_state, at.warning)
  </read_first>
  <action>
    Modify `tests/test_app_field_page.py`:

    1. UPDATE the module docstring: it currently says the 2D guard is a pre-check that keeps `field_result` out of session_state for 2D. Rewrite to state that Phase 40 routes 2D THROUGH run_field, that fakes now return a POPULATED mesh, and that VIZ-03 no-resolve is verified via a run_field call counter.

    2. UPDATE `_fake_run_field` (the 1D fake) to return a SimResult with a POPULATED `mesh` — `MeshData(x_coords=np.array([0.0,1e-4,2e-4]), y_coords=None, node_values={"ElectricField":..., "Potential":..., "NetDoping":...}, regions=[], contacts=[])`. Import `MeshData` alongside the existing `DeviceConfig, SimResult`. (The existing `mesh=None` would make the viewer branch skip and break selectbox assertions — this is REQUIRED, not optional.)

    3. ADD a `_fake_run_field_2d` returning a SimResult with `x=np.array([])`, `y=np.array([])`, `metadata={}`, and a POPULATED 2D `mesh` (`y_coords` non-None, e.g. an irregular 5-node scatter with the three node_values keys).

    4. DELETE `test_2d_config_warns_and_skips` (lines 66-76 — it asserts the removed `st.stop()` behavior). REPLACE with `test_2d_config_routes_through_and_caches`: monkeypatch `petringa.run_field` -> `_fake_run_field_2d`, seed `DeviceConfig(half_width_um=50.0)`, run, click Run, run; assert `at.exception == []`, `"field_result" in at.session_state`, and no warning containing "1D-only" (`assert not any("1D-only" in (w.value or "") for w in at.warning)`).

    5. ADD `test_selectbox_change_does_not_resolve` (VIZ-03 core check — use a CALL COUNTER, not just `at.exception`): wrap the 1D fake so it increments a counter (e.g. a list `calls` closed over, or a small callable class); monkeypatch it; seed a 1D `DeviceConfig()`; run; click Run; run (counter == 1); then `at.selectbox[0].select("Net doping").run()`; assert `at.exception == []` AND the counter is STILL 1 (run_field was NOT called again — proves no re-solve on quantity change). Also assert `at.session_state["field_result"]` is unchanged (same cached object).

    6. ADD `test_quantity_selectbox_present`: after a 1D run, assert `len(at.selectbox) >= 1` and that its options equal `list(QUANTITIES.keys())` with the value defaulting to "Electric field". Import `QUANTITIES` from `app.components.geometry_viewer` for the options assertion.

    7. KEEP the three still-valid tests unchanged: `test_run_caches_field_result`, `test_empty_state_guard`, `test_solver_convergence_failure_shows_error_not_crash` (all exercise the 1D path / error path which are unaffected). Note: because `_fake_run_field` now populates a 1D mesh, `test_run_caches_field_result` still passes (mesh presence only ADDS the viewer render, does not break the existing assertions).

    Assertions stay limited to `at.exception`, `at.session_state`, `at.button`, `at.selectbox`, `at.warning`, `at.info`, `at.error` — AppTest 1.55 has NO `plotly_chart`/`download_button` accessor. Chart-content assertions live in the 40-01 pure unit test, not here.

  </action>
  <verify>
    <automated>uv run pytest tests/test_app_field_page.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_app_field_page.py -x` passes (all tests green).
    - `test_2d_config_warns_and_skips` is GONE (`grep -c 'test_2d_config_warns_and_skips' tests/test_app_field_page.py` returns 0).
    - A 2D-route test exists asserting `"field_result" in at.session_state` for a `half_width_um=50.0` config (2D no longer blocked).
    - A selectbox-no-resolve test exists using a call counter that stays at 1 across a `at.selectbox[0].select(...).run()` (VIZ-03 verified: no re-solve).
    - Both `_fake_run_field` (1D) and `_fake_run_field_2d` return a SimResult with a POPULATED `mesh` (`grep -c 'mesh=None' tests/test_app_field_page.py` returns 0 for the fake definitions — the only `mesh=None` allowed, if any, is not on the two field-viewer fakes).
    - A test asserts the selectbox options equal `list(QUANTITIES.keys())`.
  </acceptance_criteria>
  <done>Obsolete 2D-guard test removed; 2D-route + selectbox-present + selectbox-no-resolve (counter==1) tests added; fakes populate mesh; three legacy 1D/error tests retained; suite green under uv run pytest.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                  |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| user -> field-map page | Quantity dropdown (fixed hardcoded options in QUANTITIES) + numeric sidebar widgets (bounded). No file upload, no free text. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                | Disposition | Mitigation Plan                                                                                                                                                                                        |
| --------- | ---------------------- | ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-40-03   | Tampering              | st.selectbox quantity input              | mitigate    | Options are a fixed hardcoded set (`list(QUANTITIES.keys())`); `build_geometry_figure` does `QUANTITIES[quantity]` (KeyError on any non-member) — no arbitrary key reaches node_values. Bounded input. |
| T-40-04   | Denial of Service      | griddata re-run on each selectbox change | accept      | Interpolation is ~ms on a device-sized mesh; no devsim re-solve (Run button returns False on rerun). Risk LOW.                                                                                         |
| T-40-05   | Information Disclosure | rendered heatmap/bar                     | accept      | Only numeric simulation values rendered; no PII/secrets. Risk LOW.                                                                                                                                     |

No package-manager install in this plan (no new deps — scipy/plotly already in pyproject per PKG-02) — no `T-40-SC` supply-chain threat, no legitimacy checkpoint required.
</threat_model>

<verification>
- `uv run pytest tests/test_app_field_page.py -x` green.
- `grep -c '1D-only' app/workflows/field_map.py` returns 0 (2D guard removed).
- `grep -c 'test_2d_config_warns_and_skips' tests/test_app_field_page.py` returns 0 (obsolete test removed).
- Field-map page references `build_geometry_figure(result.mesh, quantity)` and `st.selectbox("Quantity"` with `key="geo_quantity"`.
</verification>

<success_criteria>

- Selecting Dimensionality=2D no longer shows the old "1D-only" warning or hits `st.stop()`; `run_field` runs and `field_result` is cached (VIZ-01 reachable).
- 1D result shows the existing line charts + CSV download AND a geometry-viewer bar; 2D result shows only the geometry heatmap (VIZ-01/VIZ-02).
- A quantity dropdown switches E-field / net doping / potential and re-renders from the cached result with run_field call count staying at 1 (VIZ-03 "without re-running").
- `try/except RuntimeError` and module-attribute `petringa.run_field` preserved.
  </success_criteria>

<output>
Create `.planning/phases/40-geometry-viewer/40-02-SUMMARY.md` when done.
</output>
