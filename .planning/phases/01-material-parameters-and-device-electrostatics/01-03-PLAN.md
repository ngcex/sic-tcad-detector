---
phase: 01-material-parameters-and-device-electrostatics
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-material-parameters-and-device-electrostatics/01-01-PLAN.md
  - .planning/phases/01-material-parameters-and-device-electrostatics/01-02-PLAN.md
  - tests/test_poisson.py
  - tests/test_analytical.py
autonomous: true
gap_closure: true
requirements:
  - MAT-04
  - ELEC-03

must_haves:
  truths:
    - "MAT-04 is marked Partial in REQUIREMENTS.md with note that bias-dependent W targets (9.5 um@-10V, 9.73 um@-30V) are deferred to Phase 2 graded doping profile"
    - "ROADMAP success criterion #4 is updated to reflect that only W(0V)=1.7 um is validated in Phase 1, with bias-dependent targets deferred"
    - "Vbi must_have truth in PLAN 01 reads 2.9-3.1V (not 3.0-3.1V)"
    - "Key links in PLAN 01 and PLAN 02 reflect actual dependency injection pattern (caller-side coupling, not module-level imports)"
    - "test_poisson.py depletion width tests are honest: document the model limitation explicitly rather than masking with relaxed bounds"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated MAT-04 status to Partial with deferral note"
      contains: "Partial"
    - path: ".planning/ROADMAP.md"
      provides: "Updated success criterion #4 with Phase 1 scope and deferral note"
      contains: "deferred"
    - path: "tests/test_poisson.py"
      provides: "Honest depletion width tests that document the uniform-doping limitation"
      contains: "known limitation"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "MAT-04 status consistent between both files"
      pattern: "Partial"
---

<objective>
Close three verification gaps from Phase 1 by formally descoping bias-dependent depletion width targets, correcting Vbi truth range, and updating key_links to match actual code architecture.

Purpose: The Phase 1 verification found gaps between stated criteria and actual results. Rather than implementing a graded doping profile (deferred to Phase 2 per user decision), this plan honestly documents limitations, updates planning artifacts, and makes tests truthful about what the uniform-doping model can and cannot achieve.

Output: Updated ROADMAP.md, REQUIREMENTS.md, PLAN frontmatter, and honest test assertions.
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-material-parameters-and-device-electrostatics/01-VERIFICATION.md
@.planning/phases/01-material-parameters-and-device-electrostatics/01-01-PLAN.md
@.planning/phases/01-material-parameters-and-device-electrostatics/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Descope bias-dependent W targets and update ROADMAP/REQUIREMENTS</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    **ROADMAP.md changes:**

    In Phase 1 success criteria, update item #4 from:
    ```
    4. Depletion width vs bias matches experimental C-V data within tolerance (1.7 um at 0V, 9.5 um at -10V, 9.73 um at -30V)
    ```
    to:
    ```
    4. Depletion width at 0V bias matches experimental C-V data (1.7 um) using calibrated N_D=1.07e15 cm^-3. Bias-dependent targets (9.5 um at -10V, 9.73 um at -30V) require a graded epi doping profile and are deferred to Phase 2.
    ```

    Also update the Plans list to include the new plan:
    ```
    - [x] 01-03-PLAN.md -- Gap closure: descope bias-dependent W targets, update Vbi range, fix key_links
    ```

    And update the Progress table plans count from 2/2 to 3/3 (or note 3 plans total).

    **REQUIREMENTS.md changes:**

    Change MAT-04 from:
    ```
    - [x] **MAT-04**: Calculate depletion width vs doping concentration and bias voltage (analytical + devsim numerical, validated against C-V data: 1.7um@0V, 9.5um@-10V, 9.73um@-30V)
    ```
    to:
    ```
    - [~] **MAT-04**: Calculate depletion width vs doping concentration and bias voltage (analytical + devsim numerical, validated against C-V data: 1.7um@0V). Bias-dependent targets (9.5um@-10V, 9.73um@-30V) not achievable with uniform N_D model; graded epi doping profile deferred to Phase 2.
    ```

    In the Traceability table, change MAT-04 status from "Complete" to "Partial".

  </action>
  <verify>
    <automated>cd /Users/ngcex/projects/physics/petringa && grep -c "deferred" .planning/ROADMAP.md && grep -c "Partial" .planning/REQUIREMENTS.md && grep "MAT-04" .planning/REQUIREMENTS.md | head -2</automated>
  </verify>
  <done>ROADMAP success criterion #4 clearly states that only W(0V)=1.7 um is validated in Phase 1 and bias-dependent targets are deferred. REQUIREMENTS.md shows MAT-04 as Partial with explanation. Traceability table updated.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PLAN frontmatter (Vbi range and key_links) and make tests honest</name>
  <files>
    .planning/phases/01-material-parameters-and-device-electrostatics/01-01-PLAN.md
    .planning/phases/01-material-parameters-and-device-electrostatics/01-02-PLAN.md
    tests/test_poisson.py
    tests/test_analytical.py
  </files>
  <action>
    **PLAN 01 must_haves update (Vbi range):**

    In 01-01-PLAN.md frontmatter, change the Vbi truth from:
    ```
    - "Built-in potential computed from ionized acceptor concentration gives Vbi ~ 3.0-3.1V for the asymmetric p+/n- junction"
    ```
    to:
    ```
    - "Built-in potential computed from ionized acceptor concentration gives Vbi ~ 2.9-3.1V for the asymmetric p+/n- junction"
    ```

    **PLAN 01 key_links update:**

    In 01-01-PLAN.md frontmatter, replace all three key_links entries with ones reflecting the actual dependency-injection architecture:
    ```yaml
    key_links:
      - from: "src/analytical.py"
        to: "src/incomplete_ionization.py"
        via: "caller passes ionized concentration as parameter (dependency injection, not module import)"
        pattern: "N_A_ionized.*parameter"
      - from: "src/analytical.py"
        to: "src/sic_material.py"
        via: "caller passes eps_r and n_i as function arguments (dependency injection)"
        pattern: "eps_r.*=.*9\\.7"
      - from: "src/incomplete_ionization.py"
        to: "src/sic_material.py"
        via: "E_A and g_A hardcoded as function defaults matching SiC4H_Parameters values (standalone module, no import)"
        pattern: "E_A.*=.*0\\.220"
    ```

    **PLAN 02 key_links update:**

    In 01-02-PLAN.md frontmatter, update the key_link from tests/test_poisson.py to src/analytical.py:
    ```yaml
      - from: "tests/test_poisson.py"
        to: "src/analytical.py"
        via: "uses poisson.extract_depletion_width() which wraps analytical formulas internally"
        pattern: "extract_depletion_width"
    ```

    Also update the must_have truth about depletion width matching C-V from:
    ```
    - "Depletion width extracted from numerical simulation matches experimental C-V data within tolerance (1.7 um at 0V, 9.5 um at -10V, 9.73 um at -30V)"
    ```
    to:
    ```
    - "Depletion width at 0V equilibrium extracted from numerical simulation matches experimental C-V data (1.7 um). Bias-dependent targets deferred to Phase 2 (requires graded doping profile)."
    ```

    **tests/test_poisson.py update:**

    Find the test `test_validation_depletion_widths` (or similarly named test that validates W at -10V and -30V using relaxed bounds of 2-10.5 um). Update it to:
    1. Keep the W(0V) assertion with tight tolerance (within 25% of 1.7 um).
    2. For W(-10V) and W(-30V), replace the relaxed-bound assertions with explicit documentation. Instead of asserting these values fall in an artificially wide range, assert that they are LESS than the experimental targets and add a comment explaining why:
       ```python
       # Known limitation: uniform N_D=1.07e15 model gives W(-10V)~3.6 um vs experimental 9.5 um
       # and W(-30V)~5.75 um vs experimental 9.73 um. The experimental values suggest a graded
       # epi doping profile (lower N_D deeper in epi) which is deferred to Phase 2.
       # Here we verify the model is internally consistent (W increases with reverse bias)
       # and document the quantitative gap.
       assert W_minus10 > W_0V, "W must increase with reverse bias"
       assert W_minus30 > W_minus10, "W must increase with more reverse bias"
       assert W_minus10 < 9.5e-4, "Uniform N_D model known to underestimate W at -10V (expected ~3.6 um vs 9.5 um experimental)"
       ```
    3. Remove or rework any assertion that uses bounds like `2 um < W < 10.5 um` which mask the real gap.

    **tests/test_analytical.py update:**

    Find any test that validates analytical W(-10V) or W(-30V) against experimental targets with relaxed tolerances. Apply the same honest-documentation approach: assert monotonic increase, document the known gap in a comment, and do NOT assert values fall in an artificially wide range that suggests the model is working when it is not.

  </action>
  <verify>
    <automated>cd /Users/ngcex/projects/physics/petringa && python -m pytest tests/test_poisson.py tests/test_analytical.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>PLAN 01 Vbi must_have truth shows 2.9-3.1V. Key links in both PLANs reflect the actual dependency injection pattern. tests/test_poisson.py depletion width test is honest: verifies monotonic behavior and documents the quantitative gap between uniform-doping model and experimental C-V data. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep "deferred" .planning/ROADMAP.md` -- success criterion #4 mentions deferral
2. `grep "Partial" .planning/REQUIREMENTS.md` -- MAT-04 marked Partial
3. `grep "2.9-3.1" .planning/phases/01-material-parameters-and-device-electrostatics/01-01-PLAN.md` -- Vbi range updated
4. `grep "dependency injection" .planning/phases/01-material-parameters-and-device-electrostatics/01-01-PLAN.md` -- key_links updated
5. `grep "known limitation\|Known limitation" tests/test_poisson.py` -- honest test documentation
6. `python -m pytest tests/ -v` -- all tests pass
</verification>

<success_criteria>

- ROADMAP success criterion #4 clearly states Phase 1 validates W(0V) only; bias-dependent targets deferred to Phase 2
- REQUIREMENTS.md MAT-04 marked Partial with graded-doping deferral note
- PLAN 01 Vbi truth range is 2.9-3.1V (was 3.0-3.1V)
- Key links in PLAN 01 and PLAN 02 match actual code architecture (dependency injection, not module imports)
- Depletion width tests are honest: document the model limitation instead of masking it with relaxed bounds
- All existing tests continue to pass
  </success_criteria>

<output>
After completion, create `.planning/phases/01-material-parameters-and-device-electrostatics/01-03-SUMMARY.md`
</output>
