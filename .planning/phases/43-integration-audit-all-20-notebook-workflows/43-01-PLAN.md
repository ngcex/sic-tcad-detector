---
phase: 43-integration-audit-all-20-notebook-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v5.0-MILESTONE-AUDIT.md
  - .planning/phases/43-integration-audit-all-20-notebook-workflows/43-VALIDATION.md
autonomous: true
requirements: [FEAT-05]

must_haves:
  truths:
    - "REQUIREMENTS.md marks UI-01, UI-02, UI-07, VIZ-01, VIZ-02, VIZ-03 as satisfied (both checkbox and traceability row)"
    - "The v5.0 milestone audit doc exists with a 21-row notebook-to-page coverage matrix"
    - "FEAT-05 is documented as 'partially satisfied — residual gaps logged as v6.0 tech debt'"
    - "Every NONE/PARTIAL coverage cell appears as a discrete v6.0 tech-debt backlog line item"
    - "The SC4 pytest per-file/per-class reinterpretation is documented explicitly in the audit doc"
    - "All 7 tests/test_api_*.py and 13 tests/test_app_*.py modules pass under per-file/per-class isolation"
  artifacts:
    - path: ".planning/milestones/v5.0-MILESTONE-AUDIT.md"
      provides: "v5.0 milestone audit: requirements cross-reference, coverage matrix, tech-debt summary"
      contains: "Tech Debt Summary"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected v5.0 requirement checkboxes and traceability"
      contains: "FEAT-05"
  key_links:
    - from: ".planning/milestones/v5.0-MILESTONE-AUDIT.md"
      to: ".planning/phases/43-integration-audit-all-20-notebook-workflows/43-RESEARCH.md"
      via: "coverage matrix lifted from research (21 rows)"
      pattern: "FEAT-05"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 38/40 VERIFICATION.md verified reality"
      via: "checkbox + traceability correction"
      pattern: "UI-01|VIZ-01"
---

<objective>
Produce the v5.0 milestone audit deliverable and correct REQUIREMENTS.md to match source-verified reality. This is the autonomous portion of the Phase 43 integration audit: it runs the acceptance-gate test suite, corrects the six stale requirement checkboxes, and writes the complete audit document (requirements cross-reference, notebook-to-page coverage matrix, and per-gap v6.0 tech-debt backlog).

Purpose: Phase 43 is a documentation/verification audit — no new source code, no new UI pages, no physics changes. The deliverable is a written audit that records what the v5.0 milestone actually delivers, corrects stale bookkeeping, and logs residual gaps as v6.0 tech debt (not silent omissions, not fixes).

Output:

- `.planning/milestones/v5.0-MILESTONE-AUDIT.md` (new audit document)
- Corrected `.planning/REQUIREMENTS.md` (6 stale requirements reconciled to satisfied)
- `43-VALIDATION.md` sign-off flipped to compliant

CONSTRAINT: This plan touches ONLY the two `.planning/` documents named in `files_modified` (plus the VALIDATION.md sign-off). It MUST NOT modify any physics/simulation source (device.py, petringa/core/_, app/workflows/_), MUST NOT add UI pages, MUST NOT add dependencies, and MUST NOT attempt to fix the ramp_bias convergence bug. All gaps are logged, never closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-integration-audit-all-20-notebook-workflows/43-RESEARCH.md
@.planning/phases/43-integration-audit-all-20-notebook-workflows/43-VALIDATION.md
@.planning/v3.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Structural template for the audit doc: use .planning/v3.0-MILESTONE-AUDIT.md verbatim as the shape. -->
<!-- It has YAML frontmatter (milestone, audited, status: tech_debt, scores, gaps, tech_debt), then -->
<!-- sections: Requirements Coverage cross-reference table, Phase Verification Summary, Cross-Phase -->
<!-- Integration, E2E Flow Assessment, Tech Debt Summary (per-phase item lists), and a total count line. -->

<!-- The 21-row notebook-to-page coverage matrix already exists, fully derived, in 43-RESEARCH.md -->
<!-- under "Notebook → Page Coverage Matrix". Lift it; do not re-derive. Tally from research: -->
<!--   FULL = 4 (notebooks 02, 07, 10, 18) -->
<!--   PARTIAL = 10 (01, 03, 05a, 05b, 06, 09, 11, 13, 15, 17) -->
<!--   NONE = 7 (04, 08, 12, 14, 16, 19, 20) -->
<!-- The 25-requirement source-verified status table also exists in 43-RESEARCH.md under -->
<!-- "25 v5.0 Requirements — Source-Verified Status". Lift that too. -->

<!-- The 7 test_api modules: test_api_cv, test_api_cce, test_api_device, test_api_facades, -->
<!-- test_api_field, test_api_microdosimetry, test_api_sweep. -->
<!-- The 13 test_app modules: test_app_batch_sweep_page, test_app_cce_page, test_app_csv_export, -->
<!-- test_app_cv_page, test_app_dark_current_page, test_app_device_sidebar, test_app_field_page, -->
<!-- test_app_geometry_viewer, test_app_microdosimetry_page, test_app_pages, -->
<!-- test_app_radiation_damage_page, test_app_run_mockability, test_app_session. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the acceptance-gate test suite under per-file/per-class isolation</name>
  <files>tests/test_api_*.py, tests/test_app_*.py (run only — not modified)</files>
  <read_first>
    - .planning/phases/43-integration-audit-all-20-notebook-workflows/43-VALIDATION.md (Test Infrastructure + Sampling Rate sections — the exact commands and the per-file-isolation contract)
    - .planning/STATE.md (lines 106, 110, 129 — PKG-03 per-file/per-class convention; `uv sync --extra dev` requirement so `uv run pytest` sees the venv packages; devsim single-process exhaustion proof)
    - .planning/phases/43-integration-audit-all-20-notebook-workflows/43-RESEARCH.md ("Validation Architecture" section — Q1 pytest reinterpretation, the fast `test_api_*` command, the slow-marker note)
  </read_first>
  <action>
    Run the phase acceptance-gate tests as the PKG-03 per-file/per-class isolation convention, NOT a bare `pytest -q`. Do NOT install `pytest-forked` or any other dependency (decision #2: rejected — no new dependencies in this phase).

    First ensure the dev extras are synced (`uv sync --extra dev`) so `uv run pytest` resolves to the project venv rather than a non-project pytest on PATH.

    Then run the fast API gate as one command: `uv run pytest tests/test_api_cv.py tests/test_api_cce.py tests/test_api_device.py tests/test_api_facades.py tests/test_api_field.py tests/test_api_microdosimetry.py tests/test_api_sweep.py -q` (research VERIFIED this yields ~24 passing in ~28s; default `-q` deselects the `@pytest.mark.slow` live-devsim classes, which is expected).

    Then run the AppTest suite for the app pages: `uv run pytest tests/test_app_batch_sweep_page.py tests/test_app_cce_page.py tests/test_app_csv_export.py tests/test_app_cv_page.py tests/test_app_dark_current_page.py tests/test_app_device_sidebar.py tests/test_app_field_page.py tests/test_app_geometry_viewer.py tests/test_app_microdosimetry_page.py tests/test_app_pages.py tests/test_app_radiation_damage_page.py tests/test_app_run_mockability.py tests/test_app_session.py -q`. If any DD-heavy module crashes from devsim resource exhaustion when combined, re-run that module alone (`uv run pytest tests/test_<module>.py -q`) to prove it passes in isolation.

    Capture the pass/fail counts and per-command results — these feed the audit doc's test-gate evidence and the SC4 reinterpretation note (written in Task 3). Do NOT modify any test file or source file; this task only RUNS the existing suite.

  </action>
  <verify>
    <automated>uv run pytest tests/test_api_cv.py tests/test_api_cce.py tests/test_api_device.py tests/test_api_facades.py tests/test_api_field.py tests/test_api_microdosimetry.py tests/test_api_sweep.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 7 `tests/test_api_*.py` modules pass in the fast command (exit code 0; no failures/errors reported).
    - All 13 `tests/test_app_*.py` modules pass, either combined or via per-module isolation for any DD-heavy module that exhausts devsim when combined.
    - No new package was installed (no `pytest-forked`, no `pyproject.toml` dependency edit); `git status` shows no change to `pyproject.toml` or `uv.lock` from this task.
    - Recorded pass/fail counts per command are available for Task 3's audit-doc evidence.
  </acceptance_criteria>
  <done>Both the fast API gate and the AppTest suite are green under per-file/per-class isolation, with recorded counts and zero dependency changes.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the six stale requirement checkboxes and traceability rows in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the current file — the "UI Core (UI)" checkbox list at lines 28-34, the "Geometry Viewer (VIZ)" checkbox list at lines 38-40, and the v5.0 Traceability table at lines 188-214)
    - .planning/phases/43-integration-audit-all-20-notebook-workflows/43-RESEARCH.md ("25 v5.0 Requirements — Source-Verified Status" table and Pitfall 1 — the exact stale-vs-actual reconciliation with evidence pointers to 38/40 VERIFICATION.md)
    - .planning/phases/38-*/38-VERIFICATION.md and .planning/phases/40-*/40-VERIFICATION.md (the browser-confirmed evidence that UI-01/02/07 and VIZ-01/02/03 are SATISFIED)
  </read_first>
  <action>
    Per decision #1 (CORRECT the stale checkboxes — this is an in-scope doc edit): reconcile REQUIREMENTS.md to source-verified reality for exactly six requirements that are marked incomplete but are actually SATISFIED (browser-confirmed in Phases 38 and 40).

    In the checkbox lists, change these six from their current incomplete marker to `[x]`:
    - UI-01 (currently `[ ]`) → `[x]` — st.navigation with 8 pages boots cleanly (38-VERIFICATION.md)
    - UI-02 (currently `[ ]`) → `[x]` — all 11 DeviceConfig fields as sidebar controls, persistence fixed (38-VERIFICATION.md)
    - UI-07 (currently `[ ]`) → `[x]` — cross-page persistence, Playwright-confirmed (38-VERIFICATION.md, commit f1bc8b7)
    - VIZ-01 (currently `[ ]`) → `[x]` — real 2D solve → heatmap, browser-confirmed 2026-07-13 (40-VERIFICATION.md)
    - VIZ-02 (currently `[ ]`) → `[x]` — 1D → depth-profile bar, browser-confirmed (40-VERIFICATION.md)
    - VIZ-03 (currently `[ ]`) → `[x]` — quantity dropdown, no re-solve (40-VERIFICATION.md)

    In the v5.0 Traceability table (lines ~200-214), change the "Status" column for those same six rows from "Pending" to "Complete".

    Do NOT change UI-04, UI-05, UI-06 (genuinely PARTIAL — upstream ramp_bias solver blocker; leave their `[~]` markers and their "Blocked"/"Partial" traceability statuses untouched). Do NOT change FEAT-05 here — its disposition (partially satisfied) is recorded in the audit doc in Task 3; leave its REQUIREMENTS.md checkbox as-is or set it to `[~]` partial to match, consistent with the audit doc's disposition. Do NOT touch any other requirement, any v4.0/v3.0 row, or the coverage summary counts beyond what these six corrections require.

  </action>
  <verify>
    <automated>grep -E '^\- \[x\] \*\*(UI-01|UI-02|UI-07|VIZ-01|VIZ-02|VIZ-03)\*\*' .planning/REQUIREMENTS.md | wc -l | grep -q 6 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All six of UI-01, UI-02, UI-07, VIZ-01, VIZ-02, VIZ-03 show `[x]` in their checkbox list entries (grep for `\[x\] \*\*UI-01\*\*` etc. matches all six).
    - The v5.0 Traceability table rows for those six IDs read "Complete" (not "Pending").
    - UI-04, UI-05, UI-06 markers and statuses are unchanged (still `[~]` / Blocked / Partial).
    - No v4.0 or v3.0 requirement row was modified; `git diff .planning/REQUIREMENTS.md` shows changes confined to the six IDs' checkbox lines and traceability rows (plus optionally FEAT-05's own marker to `[~]`).
  </acceptance_criteria>
  <done>REQUIREMENTS.md checkboxes and traceability match source-verified reality: six stale-but-satisfied requirements corrected, the three genuinely-partial UI items left intact.</done>
</task>

<task type="auto">
  <name>Task 3: Write the v5.0 milestone audit document with coverage matrix and v6.0 tech-debt backlog</name>
  <files>.planning/milestones/v5.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v3.0-MILESTONE-AUDIT.md (the structural template — copy its frontmatter shape and section layout: Requirements Coverage cross-reference, Phase Verification Summary, Cross-Phase Integration, E2E Flow Assessment, Tech Debt Summary with per-phase item lists and a total-count line)
    - .planning/phases/43-integration-audit-all-20-notebook-workflows/43-RESEARCH.md (lift the 21-row "Notebook → Page Coverage Matrix", the "25 v5.0 Requirements — Source-Verified Status" table, the "Known Unresolved Issues" table, and the "v6.0 Tech-Debt Logging" recommendation verbatim as source material)
    - .planning/REQUIREMENTS.md (the corrected checkboxes from Task 2 — cross-reference must match)
  </read_first>
  <action>
    Create `.planning/milestones/v5.0-MILESTONE-AUDIT.md` following the `.planning/v3.0-MILESTONE-AUDIT.md` structure. Content requirements (all mandatory):

    1. YAML frontmatter: `milestone: v5.0`, `audited:` (today), `status: tech_debt`, a `scores:` block (requirements: 25 total with the satisfied/partial split — 19 satisfied + 3 partial UI-04/05/06 + FEAT-05 partial; phases 35-42), and a `gaps:`/`tech_debt:` section listing FEAT-05 coverage gaps and the ramp_bias blocker.

    2. A Requirements Coverage cross-reference table for all 25 v5.0 requirements (PKG/LIB/UI/VIZ/FEAT), lifting the source-verified statuses from research. Show final status per requirement; mark UI-01/02/07 and VIZ-01/02/03 as satisfied (reconciled), UI-04/05/06 as partial (upstream ramp_bias), FEAT-05 as partial.

    3. The full 21-row notebook-to-page coverage matrix (both `05_*` notebooks as distinct rows 05a/05b; exclude `03_executed.ipynb`; count = 21 workflows, NOT 20 — encode this count explicitly with a one-line reconciliation note so it is not re-litigated). Columns: notebook #, name, coverage (FULL/PARTIAL/NONE), page, what's missing. Include the tally: FULL=4, PARTIAL=10, NONE=7.

    4. An explicit FEAT-05 disposition paragraph (decision #3): state FEAT-05 is "partially satisfied — residual gaps logged as v6.0 tech debt." Do NOT claim FEAT-05 is complete; do NOT propose building the missing pages.

    5. An explicit SC4 reinterpretation paragraph (decision #2): document that SC4's literal `pytest -q` is satisfied via the PKG-03 per-file/per-class isolation convention (bare single-process `pytest -q` is proven unsatisfiable on this machine due to devsim resource exhaustion, reproducing on pre-refactor commit fe3b43c); state that `pytest-forked` was explicitly rejected (no new dependencies); include the Task-1 pass counts as evidence.

    6. A "Tech Debt Summary" section with ONE discrete v6.0 backlog line item per NONE/PARTIAL coverage gap, each phrased "v6.0: ...". At minimum, one item for every NONE notebook (04 flash_recombination page, 08 transient page, 12 multi-defect comparison page, 14 validation-vs-literature page, 16 single-particle transient / CCE(LET) page, 19 alternative-structures page, 20 optimization/feasibility page) and one item for each material PARTIAL gap (e.g. "v6.0: fix ramp_bias deep-bias convergence blocking UI-04/05/06 and notebooks 03/15/16", "v6.0: surface analytical electrostatics for notebook 01", "v6.0: add C-V-vs-fluence evolution view for notebook 11", "v6.0: add FLASH parametric + radiation-hardness sweep facades to Batch Sweep for notebooks 05b/13", "v6.0: add PHD intermediate view for notebook 17", "v6.0: surface 2D CCE + material-props-vs-T plots for notebooks 15/06"). Also carry forward the kappa(E) data-blocked note as an existing (v4.0 Phase 27) item for completeness. End with a total-count line.

  </action>
  <verify>
    <automated>test -f .planning/milestones/v5.0-MILESTONE-AUDIT.md && grep -qi 'Tech Debt Summary' .planning/milestones/v5.0-MILESTONE-AUDIT.md && grep -c 'v6.0:' .planning/milestones/v5.0-MILESTONE-AUDIT.md | awk '{if ($1>=7) print "OK"; else print "TOO_FEW:"$1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v5.0-MILESTONE-AUDIT.md` exists with `status: tech_debt` frontmatter.
    - Contains a coverage matrix with 21 workflow rows (grep for both `05a`/`05b` markers or equivalent; explicit "21 workflows" reconciliation note present).
    - Contains the FULL=4 / PARTIAL=10 / NONE=7 tally.
    - FEAT-05 is described as "partially satisfied" (grep matches "partially satisfied") and NOT as "complete"/"fully satisfied".
    - Contains an SC4 reinterpretation paragraph naming per-file/per-class isolation AND explicitly stating pytest-forked was rejected.
    - "Tech Debt Summary" section present with at least 7 lines beginning "v6.0:" (one per NONE notebook minimum), plus a total-count line.
    - The requirements cross-reference table lists all 25 v5.0 requirements and marks UI-01/02/07 + VIZ-01/02/03 satisfied, consistent with the Task-2 REQUIREMENTS.md edits.
  </acceptance_criteria>
  <done>The v5.0 milestone audit doc is complete: 21-row coverage matrix, 25-requirement cross-reference, FEAT-05 partial disposition, SC4 reinterpretation, and a v6.0 tech-debt backlog with one item per gap.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                 |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| user→Streamlit CSV upload (pre-existing) | The Microdosimetry page `st.file_uploader(type=["csv"])` → server-side tempfile bridge, introduced in Phase 42. Not modified by this phase. |

## STRIDE Threat Register

| Threat ID | Category  | Component                            | Disposition | Mitigation Plan                                                                                                                      |
| --------- | --------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-43-01   | Tampering | Microdosimetry CSV upload (Phase 42) | accept      | Read-only audit adds no new attack surface; upload restricted to CSV and out of scope for this phase. No V2–V6 ASVS control changes. |

No `npm`/`pip`/`cargo` installs occur in this phase; the supply-chain (T-43-SC) row is intentionally omitted (zero installs). `pytest-forked` is explicitly NOT adopted (decision #2).
</threat_model>

<verification>
- Fast API gate green: `uv run pytest tests/test_api_*.py -q` (7 modules, ~24 tests).
- AppTest suite green under per-file/per-class isolation (13 test_app modules).
- REQUIREMENTS.md: six stale requirements corrected (checkbox + traceability); three partial UI items untouched.
- Audit doc exists at `.planning/milestones/v5.0-MILESTONE-AUDIT.md` with coverage matrix, requirements cross-reference, FEAT-05 partial disposition, SC4 reinterpretation, and v6.0 tech-debt backlog.
- No physics/source/app file modified (git diff confined to the two `.planning/` docs + VALIDATION.md sign-off).
</verification>

<success_criteria>

- All 7 `tests/test_api_*.py` and 13 `tests/test_app_*.py` modules pass under per-file/per-class isolation with no new dependencies.
- REQUIREMENTS.md UI-01/UI-02/UI-07/VIZ-01/VIZ-02/VIZ-03 marked `[x]` and "Complete" in traceability.
- `.planning/milestones/v5.0-MILESTONE-AUDIT.md` complete with a 21-row coverage matrix, 25-requirement cross-reference, FEAT-05 "partially satisfied" disposition, documented SC4 reinterpretation, and ≥7 "v6.0:" backlog items.
- Zero modifications to physics/simulation source, UI pages, or dependencies.
  </success_criteria>

<output>
Create `.planning/phases/43-integration-audit-all-20-notebook-workflows/43-01-SUMMARY.md` when done.
Also flip `.planning/phases/43-integration-audit-all-20-notebook-workflows/43-VALIDATION.md` `nyquist_compliant: true` and check its final sign-off box.
</output>
