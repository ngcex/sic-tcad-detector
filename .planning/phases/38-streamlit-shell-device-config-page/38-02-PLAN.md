---
phase: 38-streamlit-shell-device-config-page
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - app/main.py
  - app/pages/__init__.py
  - app/pages/home.py
  - app/pages/cv.py
  - app/pages/cce.py
  - app/pages/field_map.py
  - app/pages/radiation_damage.py
  - app/pages/dark_current.py
  - app/pages/microdosimetry.py
  - app/pages/batch_sweep.py
  - .streamlit/config.toml
  - tests/test_app_pages.py
autonomous: true
requirements: [UI-01, UI-07]

must_haves:
  truths:
    - "`streamlit run app/main.py` boots without error and st.navigation lists Home + all 7 workflow pages"
    - "The device-config sidebar renders on every page (called in the entry script before pg.run()), so device_config persists across navigation (UI-07)"
    - "Every page implements the empty-state guard: no page crashes when device_config is absent from session_state"
    - "An AppTest headless smoke test loads app/main.py in-process with no exception, confirms device_config is set, and confirms the nav registers all 8 pages"
  artifacts:
    - path: "app/main.py"
      provides: "st.navigation entry script: set_page_config, render_device_sidebar(), page registration, pg.run()"
      contains: "st.navigation"
      min_lines: 25
    - path: "app/pages/home.py"
      provides: "Home landing page with orientation line + config summary (st.json monospace readout)"
      contains: "device_config"
    - path: ".streamlit/config.toml"
      provides: "theme tokens (primaryColor, backgroundColor, secondaryBackgroundColor, textColor) per 38-UI-SPEC Color table"
      contains: "primaryColor"
    - path: "tests/test_app_pages.py"
      provides: "UI-01 empty-state guard test + AppTest nav/sidebar smoke"
      contains: "test_empty_state_guard"
  key_links:
    - from: "app/main.py"
      to: "app.components.device_sidebar.render_device_sidebar"
      via: "call before pg.run() so sidebar runs on every page"
      pattern: "render_device_sidebar\\(\\)"
    - from: "app/main.py"
      to: "st.navigation"
      via: "st.Page callable registration for Home + 7 workflow pages"
      pattern: "st\\.navigation"
    - from: "app/pages/*.py"
      to: "st.session_state device_config"
      via: "empty-state guard (st.info + st.stop when absent)"
      pattern: "session_state.get\\(.device_config.\\)"
---

<objective>
Build the Streamlit multi-page shell on top of the config core from 38-01: `app/main.py` as the `st.navigation` entry script that calls `render_device_sidebar()` before `pg.run()` (so the config sidebar renders on every page and persists across navigation), the theme `config.toml`, the Home page (orientation + config summary), the 7 workflow placeholder pages each with the empty-state guard, and the UI-01 tests (empty-state guard unit test + an AppTest headless smoke that boots the whole app in-process).

Purpose: Satisfies UI-01 (launch + all pages listed in nav) and completes UI-07 (config survives navigation because the entry-script sidebar always runs). Establishes the page structure, empty-state guard, and theme tokens that Phases 39–43 inherit.

Output: `app/main.py`, `app/pages/*.py` (Home + 7 workflows), `.streamlit/config.toml`, `tests/test_app_pages.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-streamlit-shell-device-config-page/38-RESEARCH.md
@.planning/phases/38-streamlit-shell-device-config-page/38-UI-SPEC.md
@.planning/phases/38-streamlit-shell-device-config-page/38-VALIDATION.md
@.planning/phases/38-streamlit-shell-device-config-page/38-01-SUMMARY.md
@app/components/device_sidebar.py

<verified_import_convention>
<!-- Proven empirically this session — use verbatim; the executor should NOT invent an alternative. -->

- `app/main.py` FIRST lines: insert project root on sys.path BEFORE importing app.*:
  `import sys, os; sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`
  This makes `from app.components.device_sidebar import render_device_sidebar` and `from app.pages.home import render as render_home` resolve under `streamlit run app/main.py` (whose entry-dir-on-path default would otherwise break `app.` imports).
- Use CALLABLE-based `st.Page(render_fn, title=..., icon=..., default=...)` — NOT string file paths. Verified: callables load correctly under both `streamlit run` and `AppTest.from_file`, and sidestep CWD-relative path resolution.
- Each page module exposes a `render()` function (no top-level Streamlit side effects beyond what render() does when called by pg.run()).
- Verified this session: `AppTest.from_file("app/main.py").run()` produces no exception, sets `device_config` in session_state, and renders page titles — this is the smoke-test harness to use.
  </verified_import_convention>

<page_registration_contract>
<!-- From 38-UI-SPEC Navigation & Page Contract. Register all 8 as placeholders now (UI-01). -->

| Nav order | render fn | Page title | Icon | "coming in phase" caption |
| 0 | render_home | Home | :material/home: | (Home is live this phase — orientation + config summary) |
| 1 | render_cv | C-V Analysis | :material/show_chart: | Phase 39 |
| 2 | render_cce | Charge Collection (CCE) | :material/bolt: | Phase 39 |
| 3 | render_field_map | Field Map | :material/map: | Phase 39 (geometry viewer is a component here in Phase 40, not a separate nav entry) |
| 4 | render_radiation_damage | Radiation Damage | :material/warning: | Phase 41 |
| 5 | render_dark_current | Dark Current | :material/dark_mode: | Phase 41 |
| 6 | render_microdosimetry | Microdosimetry | :material/scatter_plot: | Phase 42 |
| 7 | render_batch_sweep | Batch Sweep | :material/stacked_line_chart: | Phase 42 |

Home is the default nav target (`default=True`). Icons are discretionary Material Symbols; emoji fallback acceptable.
</page_registration_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme config.toml + Home page + 7 placeholder pages with empty-state guard</name>
  <files>.streamlit/config.toml, app/pages/__init__.py, app/pages/home.py, app/pages/cv.py, app/pages/cce.py, app/pages/field_map.py, app/pages/radiation_damage.py, app/pages/dark_current.py, app/pages/microdosimetry.py, app/pages/batch_sweep.py</files>
  <read_first>
    - .planning/phases/38-streamlit-shell-device-config-page/38-UI-SPEC.md (Color table for config.toml values; Typography single-primitive rule — st.header only; Copywriting Contract empty-state + config-set caption; Navigation & Page Contract page list)
    - .planning/phases/38-streamlit-shell-device-config-page/38-RESEARCH.md (Pattern 4 empty-state guard, "Placeholder page with empty-state guard" code example)
    - app/components/device_sidebar.py (from 38-01 — confirms the session_state key is "device_config")
  </read_first>
  <action>
    Create `.streamlit/config.toml` with a `[theme]` table setting the exact tokens from 38-UI-SPEC Color: `primaryColor="#1F6FEB"`, `backgroundColor="#FFFFFF"`, `secondaryBackgroundColor="#F0F2F6"`, `textColor="#1A1A1A"`. Do NOT add custom CSS/HTML injection (spec: Streamlit-native primitives only). Create `app/pages/__init__.py` (empty). Create 8 page modules, each exposing a `render()` function. `app/pages/home.py::render()`: `st.title("petringa — SiC TCAD Simulator")` (or equivalent page title via st.title), a one-line orientation `st.write`, then the empty-state guard, then the config summary as a monospace readout `st.json({k: getattr(cfg, k) for k in cfg.__dataclass_fields__})`. The 7 workflow pages (cv, cce, field_map, radiation_damage, dark_current, microdosimetry, batch_sweep) each: `st.title(<page title>)`, the empty-state guard verbatim (`cfg = st.session_state.get("device_config"); if cfg is None: st.info("Configure a device in the sidebar to begin."); st.stop()`), then the config summary `st.json(...)`, then a `st.caption("Running this simulation is implemented in Phase <N>.")` with the phase number per the page_registration_contract (C-V/CCE/Field Map → 39; Radiation Damage/Dark Current → 41; Microdosimetry/Batch Sweep → 42). Use `st.header` as the ONLY section-heading primitive if any sub-section headings are needed (38-UI-SPEC Typography rule — no st.subheader). Every page MUST implement the guard so it never crashes on empty session_state (success criterion 4).
  </action>
  <verify>
    <automated>python -c "import tomllib; t=tomllib.load(open('.streamlit/config.toml','rb')); assert t['theme']['primaryColor']=='#1F6FEB' and t['theme']['backgroundColor']=='#FFFFFF' and t['theme']['secondaryBackgroundColor']=='#F0F2F6' and t['theme']['textColor']=='#1A1A1A'; print('theme OK')" && for p in home cv cce field_map radiation_damage dark_current microdosimetry batch_sweep; do grep -q "def render" app/pages/$p.py || { echo "MISSING render in $p"; exit 1; }; done && grep -rl 'session_state.get("device_config")' app/pages/cv.py app/pages/cce.py app/pages/field_map.py app/pages/radiation_damage.py app/pages/dark_current.py app/pages/microdosimetry.py app/pages/batch_sweep.py | wc -l | xargs -I{} sh -c 'test {} -eq 7 && echo "7 guards OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `.streamlit/config.toml` `[theme]` has exactly `primaryColor=#1F6FEB`, `backgroundColor=#FFFFFF`, `secondaryBackgroundColor=#F0F2F6`, `textColor=#1A1A1A` (verified by the tomllib assert above).
    - All 8 page modules (home + 7 workflows) define a `render()` function (verified: `grep -q "def render" app/pages/<name>.py` for each).
    - All 7 workflow pages contain the empty-state guard reading `session_state.get("device_config")` and calling `st.stop()` when absent (verified: 7 files match).
    - No custom CSS/HTML injection: `grep -c "unsafe_allow_html" app/pages/*.py` returns 0.
    - No `st.subheader` used (verified: `grep -c "st.subheader" app/pages/*.py` returns 0).
  </acceptance_criteria>
  <done>Theme tokens set; 8 render()-exposing pages exist; all 7 workflow pages guard against empty session_state; no CSS injection; no st.subheader.</done>
</task>

<task type="auto">
  <name>Task 2: st.navigation entry script (app/main.py) wiring sidebar + all 8 pages</name>
  <files>app/main.py</files>
  <read_first>
    - .planning/phases/38-streamlit-shell-device-config-page/38-UI-SPEC.md (st.set_page_config params: page_title="petringa — SiC TCAD Simulator", page_icon=":material/memory:", layout="wide", initial_sidebar_state="expanded"; Navigation & Page Contract)
    - .planning/phases/38-streamlit-shell-device-config-page/38-RESEARCH.md (Pattern 1 st.navigation multi-page shell)
    - app/components/device_sidebar.py (render_device_sidebar signature)
    - app/pages/home.py (render callable to import)
  </read_first>
  <action>
    Create `app/main.py` as the `st.navigation` entry script. FIRST lines (before any `app.*` import): `import sys, os` then `sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))` — this is the verified convention that makes `app.` imports resolve under `streamlit run app/main.py`. Then `import streamlit as st`, and import the 8 page `render` callables (`from app.pages.home import render as render_home`, etc.) and `from app.components.device_sidebar import render_device_sidebar`. Call `st.set_page_config(page_title="petringa — SiC TCAD Simulator", page_icon=":material/memory:", layout="wide", initial_sidebar_state="expanded")` as the FIRST Streamlit call (before any other st.*). Then call `render_device_sidebar()` — this runs BEFORE `pg.run()` so the sidebar renders on every page and writes `st.session_state["device_config"]` (satisfies UI-02 on-any-page + UI-07 persistence). Register all 8 pages with CALLABLE-based `st.Page(render_fn, title=..., icon=..., default=...)` per the page_registration_contract table (Home has `default=True`), assemble into a list, `pg = st.navigation(pages)`, then `pg.run()`. Keep the page list easy to append to (Phases 39–42 add behavior to these same pages). Do NOT use the legacy `pages/` magic directory. Do NOT use string-path `st.Page("app/pages/cv.py")` — use callables (verified more robust under AppTest and CWD-independent).
  </action>
  <verify>
    <automated>python -c "from streamlit.testing.v1 import AppTest; at=AppTest.from_file('app/main.py'); at.run(); assert at.exception is None or len(at.exception)==0, at.exception; print('AppTest boot OK')" && grep -q "render_device_sidebar()" app/main.py && grep -q "st.navigation" app/main.py && grep -c "st.Page" app/main.py | xargs -I{} sh -c 'test {} -ge 8 && echo "8+ pages registered"'</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` inserts the project root on `sys.path` before importing `app.*` (verified: `grep -q "sys.path.insert" app/main.py`).
    - `st.set_page_config(...)` is present with `layout="wide"` and `initial_sidebar_state="expanded"` (verified: `grep -q 'layout="wide"' app/main.py && grep -q 'initial_sidebar_state="expanded"' app/main.py`).
    - `render_device_sidebar()` is called in the entry script before `pg.run()` (verified: line number of `render_device_sidebar()` < line number of `pg.run()`).
    - `st.navigation` is used and at least 8 `st.Page(...)` registrations exist (verified: `grep -c "st.Page" app/main.py` ≥ 8).
    - `AppTest.from_file("app/main.py").run()` completes with no exception (verified by the automated command above).
    - No legacy `pages/` magic-dir usage and no string-path st.Page (verified: `grep -c 'st.Page("' app/main.py` returns 0).
  </acceptance_criteria>
  <done>app/main.py boots cleanly under AppTest, renders sidebar on every page, registers Home + 7 workflow pages via callable st.Page, uses st.navigation.</done>
</task>

<task type="auto">
  <name>Task 3: UI-01 empty-state guard test + AppTest nav/sidebar smoke</name>
  <files>tests/test_app_pages.py</files>
  <read_first>
    - .planning/phases/38-streamlit-shell-device-config-page/38-VALIDATION.md (Per-Task Verification Map — required test name test_empty_state_guard)
    - .planning/phases/38-streamlit-shell-device-config-page/38-RESEARCH.md (Testing note: streamlit.testing.v1.AppTest is the headless harness)
    - app/pages/cv.py (a workflow page whose guard is under test)
    - app/main.py (the entry script the smoke test boots)
  </read_first>
  <action>
    Create `tests/test_app_pages.py` with two tests. `test_empty_state_guard`: use `streamlit.testing.v1.AppTest.from_function(render)` where `render` is imported via `from app.pages.cv import render` — do NOT use `AppTest.from_file` on a page module (page modules only DEFINE `render()` with no module-level Streamlit call, so `from_file` would render nothing and the guard text would never emit; only `from_function(render)` actually invokes the guard). Ensure session_state is EMPTY before running (do not pre-seed device_config). Run it, and assert (a) no exception was raised and (b) an `st.info` element with the text "Configure a device in the sidebar to begin." is present (i.e. the guard fired and the page did not crash). This directly proves success criterion 4 — no page crashes on empty session_state. `test_nav_sidebar_smoke`: `AppTest.from_file("app/main.py")`, `.run()`, assert `at.exception` is empty/None, assert `"device_config" in at.session_state` (sidebar ran and wrote the config), and assert the app registered the expected number of nav pages (assert on the number of page titles / a title readout ≥ 8 if the AppTest API exposes it; otherwise assert the boot succeeded and device_config is set as the minimum smoke). Name the first test EXACTLY `test_empty_state_guard` (fixed by 38-VALIDATION.md). These tests pull part of UI-01 out of manual-only into automated coverage.
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_pages.py -x 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_app_pages.py` defines `test_empty_state_guard` (verified: `grep -q "def test_empty_state_guard" tests/test_app_pages.py`).
    - `test_empty_state_guard` asserts the guard's info text "Configure a device in the sidebar to begin." appears when session_state has no device_config (verified: the string is present in the test and asserted).
    - `test_empty_state_guard` uses `AppTest.from_function` on an imported page `render` (verified: `grep -q "AppTest.from_function" tests/test_app_pages.py`), NOT `from_file` on a page module.
    - The nav smoke test boots `app/main.py` via `AppTest.from_file` and asserts no exception + `device_config` set (verified: `grep -q "AppTest.from_file" tests/test_app_pages.py`).
    - `python -m pytest tests/test_app_pages.py -x` passes.
  </acceptance_criteria>
  <done>test_empty_state_guard proves no crash on empty session_state; AppTest smoke boots the full app cleanly; both green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                                            |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| user → nav / page routing | User selects pages; each page reads device_config from session_state. Local single-user tool; no network, no auth, no file upload, no simulation execution this phase. |

## STRIDE Threat Register

| Threat ID       | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                |
| --------------- | ---------------------- | ---------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-04         | Denial of Service      | page render on empty session_state | mitigate    | Every page implements the empty-state guard (`st.info` + `st.stop()`); a page loaded before the sidebar writes config shows a prompt instead of crashing. Covered by `test_empty_state_guard`. |
| T-38-05         | Tampering              | st.Page registration               | mitigate    | Pages registered via explicit callable references (`st.Page(render_fn, ...)`), not dynamic string/attribute lookup — no path/attribute injection surface.                                      |
| T-38-06         | Information Disclosure | config summary readout (`st.json`) | accept      | Displays only the user's own DeviceConfig numeric values in a single-user local tool; no secrets, no other-user data. Accept.                                                                  |
| T-38-SC         | Tampering              | npm/pip/cargo installs             | accept      | No package installs this phase — streamlit already declared/installed. `[VERIFIED: 38-RESEARCH Package Legitimacy Audit — none added]` No slopcheck gate required.                             |
| </threat_model> |

<verification>
- `python -m pytest tests/test_app_pages.py -x` — green (empty-state guard + AppTest smoke).
- `AppTest.from_file("app/main.py").run()` — no exception, device_config set, nav lists all pages.
- Manual (phase gate, human-run before /gsd:verify-work): `streamlit run app/main.py` opens in browser with no traceback; sidebar nav lists Home + 7 workflow pages; editing the sidebar then navigating between pages preserves the config summary.
- Do NOT run bare `pytest -q` (unsatisfiable per STATE.md line 98). App tests build no devsim devices; scope to `tests/test_app_*.py`.
</verification>

<success_criteria>

- `streamlit run app/main.py` launches without error; st.navigation lists Home + all 7 workflow pages (UI-01).
- The device-config sidebar renders on every page (called before pg.run()), so device_config persists across navigation (UI-07).
- No page crashes on empty session_state — every page guards and prompts (success criterion 4).
- AppTest headless smoke boots the app in-process with no exception and confirms device_config is set + nav registered.
  </success_criteria>

<output>
Create `.planning/phases/38-streamlit-shell-device-config-page/38-02-SUMMARY.md` when done.
</output>
