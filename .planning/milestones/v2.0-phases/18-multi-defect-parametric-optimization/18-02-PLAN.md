---
phase: 18-multi-defect-parametric-optimization
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - notebooks/12_multi_defect_comparison.ipynb
  - notebooks/13_parametric_optimization.ipynb
autonomous: true
requirements: [PARM-01, PARM-02, PARM-03]

must_haves:
  truths:
    - "User can see CCE vs fluence overlay comparing single-defect and three-defect models"
    - "User can see dark current and C-V comparison between single-defect and three-defect models"
    - "User can see CCE vs fluence with per-defect uncertainty bands"
    - "User can see a ranked table of device configurations sorted by radiation hardness"
    - "User can see CCE retention heatmap or bar chart for the parametric sweep"
  artifacts:
    - path: "notebooks/12_multi_defect_comparison.ipynb"
      provides: "PARM-03 single-vs-multi-defect comparison + PARM-01 uncertainty bands"
    - path: "notebooks/13_parametric_optimization.ipynb"
      provides: "PARM-02 parametric design sweep with ranked table"
  key_links:
    - from: "notebooks/12_multi_defect_comparison.ipynb"
      to: "src/radiation_damage.py::make_single_defect_params"
      via: "import and call"
      pattern: "make_single_defect_params"
    - from: "notebooks/12_multi_defect_comparison.ipynb"
      to: "src/radiation_damage.py::cce_uncertainty_envelope"
      via: "import and call"
      pattern: "cce_uncertainty_envelope"
    - from: "notebooks/13_parametric_optimization.ipynb"
      to: "src/radiation_damage.py::radiation_hardness_sweep"
      via: "import and call"
      pattern: "radiation_hardness_sweep"
---

<objective>
Create two publication-quality notebooks: (1) multi-defect model comparison with uncertainty bands, and (2) parametric radiation hardness optimization.

Purpose: Deliver the primary user-facing output for PARM-01 (uncertainty bands), PARM-02 (design sweep), and PARM-03 (model comparison). These notebooks are the scientific deliverables of Phase 18.

Output: notebooks/12_multi_defect_comparison.ipynb, notebooks/13_parametric_optimization.ipynb
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-multi-defect-parametric-optimization/18-RESEARCH.md
@.planning/phases/18-multi-defect-parametric-optimization/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-defect comparison and uncertainty bands notebook</name>
  <files>notebooks/12_multi_defect_comparison.ipynb</files>
  <action>
Create `notebooks/12_multi_defect_comparison.ipynb` using nbformat (follow established create_notebook pattern from prior phases). The notebook should have these cells:

**Cell 1 - Title and Introduction (markdown):**
"# Multi-Defect Model Comparison and Uncertainty Analysis"
Brief intro explaining: comparing single-effective-defect vs three-defect (Burin 2024) model for 4H-SiC radiation damage. The single-defect model lumps Z1/2 + EH6/7 + EH4 into one effective defect with K_tau matched to the three-defect model.

**Cell 2 - Imports and Setup (code):**

```python
import numpy as np
import matplotlib.pyplot as plt
from src.radiation_damage import (
    RadiationDamageParams, compute_K_tau,
    make_single_defect_params, cce_uncertainty_envelope,
)
from src.charge_collection import cce_vs_fluence
from src.dark_current import dark_current_vs_fluence
from src.cv_analysis import cv_at_fluence

plt.rcParams.update({"font.family": "serif", "font.size": 12,
                      "axes.labelsize": 14, "figure.dpi": 150})
```

**Cell 3 - Construct Models (code):**
Create three_defect = RadiationDamageParams() and single_defect = make_single_defect_params(). Print K_tau comparison for both carriers showing exact match.

**Cell 4 - CCE Comparison (code):**
Fluences = [0, *np.geomspace(1e10, 5e13, 10)]. Run cce_vs_fluence at V_bias=-40 for both models. Plot overlay. The curves should overlap (same K_tau). Title: "CCE vs Fluence: Single-Defect vs Three-Defect Model". Save to figures/12a_cce_model_comparison.png.

**Cell 5 - Dark Current Comparison (code):**
Run dark_current_vs_fluence for both models at V_bias=-30. Plot overlay on log-log scale. Note: total dark current is TAT-dominated (N_t term, ~4 orders of magnitude above SRH), so models should give near-identical results. Save to figures/12b_dark_current_model_comparison.png.

**Cell 6 - C-V Comparison (code):**
Run cv_at_fluence at fluence=1e13 for both models. Plot C-V overlay. Should be identical since carrier removal uses same eta_removal. Save to figures/12c_cv_model_comparison.png.

**Cell 7 - Discussion: When Models Differ (markdown):**
Explain that single-defect and three-defect models are equivalent for CCE, dark current, and C-V because K_tau and eta_removal are matched. The key difference emerges in annealing predictions (different defects anneal at different rates) and in the physical interpretation of defect concentrations.

**Cell 8 - Per-Defect Uncertainty Bands (code):**
Run cce_uncertainty_envelope(fluences, V_bias=-40, scale_low=0.5, scale_high=2.0). Plot nominal curve with fill_between shading for min/max. Title: "CCE vs Fluence with Per-Defect Uncertainty (0.5x-2.0x eta scatter)". Save to figures/12d_cce_uncertainty_bands.png.

**Cell 9 - Multi-Bias Uncertainty (code):**
Run cce_uncertainty_envelope at V_bias=-20, -40, -60, -100. Plot all 4 envelopes with different colors. Title: "CCE Uncertainty Bands at Multiple Bias Voltages". Save to figures/12e_cce_uncertainty_multibias.png.

**Cell 10 - Summary Table (code):**
Print markdown table summarizing: model type, K_tau_n, K_tau_p, eta_removal, and CCE at fluence=1e13 for single-defect and three-defect.

Use established notebook conventions: serif fonts, figure saving to figures/ directory, semilog-x for fluence plots, descriptive axis labels with units.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -c "import nbformat, ast; nb = nbformat.read('notebooks/12_multi_defect_comparison.ipynb', as_version=4); print(f'Cells: {len(nb.cells)}'); assert len(nb.cells) >= 8, 'Too few cells'; [ast.parse(c['source']) for c in nb.cells if c['cell_type']=='code']; print('All code cells parse OK')"</automated>
</verify>
<done>Notebook 12 has 10 cells covering single-vs-multi-defect CCE/dark current/C-V comparison, discussion of model equivalence, and per-defect uncertainty bands at multiple biases.</done>
</task>

<task type="auto">
  <name>Task 2: Parametric radiation hardness optimization notebook</name>
  <files>notebooks/13_parametric_optimization.ipynb</files>
  <action>
Create `notebooks/13_parametric_optimization.ipynb` using nbformat. Cells:

**Cell 1 - Title (markdown):**
"# Parametric Radiation Hardness Optimization"
Explain: sweeping epi thickness, bulk doping, and bias voltage to identify the most radiation-hard 4H-SiC detector configuration at a target fluence.

**Cell 2 - Imports (code):**

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from src.radiation_damage import radiation_hardness_sweep
from src.charge_collection import cce_vs_fluence

plt.rcParams.update({"font.family": "serif", "font.size": 12,
                      "axes.labelsize": 14, "figure.dpi": 150})
```

**Cell 3 - Sweep Parameters (code):**
Define sweep grid:

- `epi_thicknesses = [5e-4, 10e-4, 20e-4, 50e-4]` (5, 10, 20, 50 um)
- `N_D_bulks = [5e13, 8.5e13, 5e14, 5e15]` (cm^-3)
- `V_biases = [-20, -40, -60, -100]` (V)
- `target_fluence = 1e13` (protons/cm^2, moderate damage)
  Print grid size: "4 x 4 x 4 = 64 configurations, 128 device solves"
  Add markdown note: "Expected runtime: ~15-20 minutes"

**Cell 4 - Run Sweep (code):**

```python
df = radiation_hardness_sweep(epi_thicknesses, N_D_bulks, V_biases, target_fluence)
print(f"Sweep complete: {len(df)} configurations")
df.head(10)
```

**Cell 5 - Top 10 Results (code):**
Display `df.head(10)` with formatted columns. Highlight best configuration.

**Cell 6 - CCE Retention Heatmap (code):**
For each epi thickness, create a pivot table of N_D_bulk vs V_bias with CCE_retention values. Plot as 2x2 subplot heatmaps (one per epi thickness) using plt.imshow or plt.pcolormesh. Title: "CCE Retention at 1e13 p/cm^2 by Device Configuration". Save to figures/13a_cce_retention_heatmap.png.

**Cell 7 - Effect of Epi Thickness (code):**
For fixed N_D_bulk=8.5e13 (Petringa device), plot CCE_retention vs epi_thickness at each bias voltage. Line plot with 4 curves. Save to figures/13b_cce_vs_epi_thickness.png.

**Cell 8 - Effect of Doping (code):**
For fixed epi=10e-4 (Petringa device), plot CCE_retention vs N_D_bulk at each bias voltage. Semilog-x. Save to figures/13c_cce_vs_doping.png.

**Cell 9 - Discussion and Recommendations (markdown):**
Summarize findings: which parameters have the largest effect on radiation hardness? What is the optimal configuration? Note that higher doping provides more margin before full compensation (higher Phi_crit) but may reduce pristine CCE. Higher bias recovers CCE but increases dark current and may exceed breakdown limits.

**Cell 10 - Summary (markdown):**
Key takeaways in bullet point format.

IMPORTANT: This notebook will take a long time to execute (~15-20 min for the 64-point sweep). The notebook should be created with code cells but NOT executed. The user will run it manually.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -c "import nbformat, ast; nb = nbformat.read('notebooks/13_parametric_optimization.ipynb', as_version=4); print(f'Cells: {len(nb.cells)}'); assert len(nb.cells) >= 8, 'Too few cells'; [ast.parse(c['source']) for c in nb.cells if c['cell_type']=='code']; print('All code cells parse OK')"</automated>
</verify>
<done>Notebook 13 has 10 cells covering parametric sweep setup, execution, ranked table output, heatmap visualization, and design recommendations.</done>
</task>

</tasks>

<verification>
1. Both notebooks parse as valid nbformat v4
2. Notebook 12 covers PARM-03 (single vs multi defect) and PARM-01 (uncertainty bands)
3. Notebook 13 covers PARM-02 (parametric design sweep)
4. All imports reference functions created in Plan 01
</verification>

<success_criteria>

- Notebook 12 demonstrates single-defect vs three-defect equivalence for CCE, dark current, C-V
- Notebook 12 shows per-defect uncertainty bands with fill_between envelope
- Notebook 13 runs a full parametric sweep and produces a ranked table of configurations
- Notebook 13 includes heatmap visualization of CCE retention
- Both notebooks follow publication-quality conventions (serif fonts, labeled axes, figure saving)
  </success_criteria>

<output>
After completion, create `.planning/phases/18-multi-defect-parametric-optimization/18-02-SUMMARY.md`
</output>
