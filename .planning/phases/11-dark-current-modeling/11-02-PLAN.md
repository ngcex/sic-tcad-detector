---
phase: 11-dark-current-modeling
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/dark_current.py
  - notebooks/07_dark_current.ipynb
autonomous: true
requirements: [DARK-04, DARK-05, NOTE-02]

must_haves:
  truths:
    - "User can see separate TAT, SRH, and SRV contributions plotted against reverse voltage on a single log-scale figure"
    - "User can vary epi thickness, doping, and SRV and observe their effect on dark current magnitude and composition"
    - "A Jupyter notebook guides user through dark current analysis with publication-quality figures"
  artifacts:
    - path: "src/dark_current.py"
      provides: "sensitivity_sweep function for parameter studies"
      contains: "def sensitivity_sweep"
    - path: "notebooks/07_dark_current.ipynb"
      provides: "Dark current analysis notebook with decomposition and sensitivity"
      min_lines: 100
  key_links:
    - from: "notebooks/07_dark_current.ipynb"
      to: "src/dark_current.py"
      via: "imports create_dark_current_device, dark_current_sweep, sensitivity_sweep"
      pattern: "from src.dark_current import"
    - from: "src/dark_current.py"
      to: "src/dark_current.py"
      via: "sensitivity_sweep calls create_dark_current_device + dark_current_sweep in loop"
      pattern: "create_dark_current_device"
---

<objective>
Create dark current visualization, sensitivity analysis utilities, and Jupyter notebook 07 for dark current characterization.

Purpose: Users need to understand which mechanisms dominate dark current and how design parameters affect it. This enables detector design optimization.
Output: sensitivity_sweep function in dark_current.py, notebook 07 with decomposition plots, calibration, and sensitivity studies.
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dark-current-modeling/11-RESEARCH.md
@.planning/phases/11-dark-current-modeling/11-01-SUMMARY.md

@src/dark_current.py
@src/plotting.py
@notebooks/06_temperature_dependence.ipynb

<interfaces>
<!-- Interfaces from Plan 01 output (dark_current.py) -->

From src/dark_current.py (created in Plan 01):

```python
def create_dark_current_device(T=300, N_t=None, S_n=None, S_p=None, **kwargs):
    """Creates DD device with TAT + SRV enabled. Returns device_info."""

def dark_current_sweep(device_info, V_range, area=0.05, V_step=0.5):
    """Returns dict: voltages, I_total, I_SRH, I_TAT, I_SRV, J_total, ..."""

def extract_dark_current_components(device_info, area=0.05):
    """Returns dict: J_total, J_SRH, J_TAT, J_SRV, I_total, I_SRH, I_TAT, I_SRV"""
```

From src/plotting.py (existing plot utilities):

```python
# Has functions for publication-quality matplotlib figures
# Follow existing style conventions (font sizes, grid, labels)
```

From notebooks/06_temperature_dependence.ipynb (style reference):

```
# 6 sections, markdown headers, publication-quality figures
# Pattern: import, setup, compute, plot, analyze
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sensitivity_sweep utility to dark_current.py</name>
  <files>src/dark_current.py</files>
  <action>
Add a `sensitivity_sweep` function to the existing dark_current.py module:

**`sensitivity_sweep(param_name, param_values, V_eval=-30.0, T=300, area=0.05, base_kwargs=None)`**

- param_name: one of "epi_thickness_cm", "N_D", "N_t", "S_n" (the parameter to sweep)
- param_values: array of values to sweep
- V_eval: voltage at which to evaluate dark current (default -30V)
- base_kwargs: dict of baseline device parameters (passed to create_dark_current_device)
- For each param_value:
  1. Create a fresh device with the swept parameter overridden in kwargs
  2. Ramp to V_eval using incremental steps (reuse the iv_sweep ramping pattern)
  3. Extract dark current components
  4. Clean up device (devsim.delete_device)
- Return pandas DataFrame with columns: param_name, I_total, I_SRH, I_TAT, I_SRV
- Handle param_name mapping: "epi_thickness_cm" and "N_D" go to create_dd_device kwargs; "N_t" and "S_n"/"S_p" go to create_dark_current_device kwargs

Also add a convenience function:
**`plot_dark_current_decomposition(sweep_result, ax=None, title=None)`**

- Takes output of dark_current_sweep
- Plots |I_total|, |I_SRH|, |I_TAT|, |I_SRV| vs voltage on semilogy axes
- Uses matplotlib with publication-quality styling (match existing plotting.py conventions)
- Returns the matplotlib axes object
  </action>
  <verify>
  <automated>cd /Users/ngcex/projects/physics/petringa && python -c "from src.dark_current import sensitivity_sweep, plot_dark_current_decomposition; print('imports OK')"</automated>
  </verify>
  <done>sensitivity_sweep and plot_dark_current_decomposition importable from dark_current module</done>
  </task>

<task type="auto">
  <name>Task 2: Create Jupyter notebook 07 for dark current analysis</name>
  <files>notebooks/07_dark_current.ipynb</files>
  <action>
Create notebooks/07_dark_current.ipynb following the style of notebook 06 (temperature_dependence). Use nbformat to create programmatically. The notebook should have these sections:

**Section 1: Introduction (markdown)**

- Title: "Dark Current Analysis: TAT and Surface Recombination in 4H-SiC"
- Brief description of dark current mechanisms (bulk SRH, trap-assisted tunneling through Z1/2 center, surface recombination)
- Note the experimental target: 18 pA at -30V

**Section 2: Model Setup (code + markdown)**

- Import src.dark_current functions
- Display default parameters (E_t, m_t, N_t, S_n, S_p from SiC4H_Parameters)
- Create device with create_dark_current_device()

**Section 3: Dark Current I-V with Decomposition (code + markdown)**

- Run dark_current_sweep over V_range = np.arange(0, -35, -1) or similar
- Plot decomposition using plot_dark_current_decomposition: log-scale |I| vs V showing I_total, I_TAT, I_SRH, I_SRV as separate lines
- Add horizontal line at 18 pA target
- Markdown cell discussing which mechanism dominates

**Section 4: Calibration Check (code + markdown)**

- Extract I_dark at -30V, compare to 18 pA target
- Print ratio and order-of-magnitude assessment
- If not within range, show what N_t adjustment would be needed

**Section 5: Sensitivity Studies (code + markdown)**
Three sub-studies:

1. **Epi thickness sweep**: 5, 10, 15, 20 um at -30V. Plot I_total vs epi thickness.
2. **Doping sweep**: N_D from 5e14 to 5e15 (5 points). Plot I_total vs N_D.
3. **SRV sweep**: S from 0 to 1e5 (5 points). Plot I_total vs S.
   Use sensitivity_sweep for each. Create a 1x3 subplot figure.

**Section 6: Summary (markdown)**

- Key findings table: mechanism, contribution, dominant regime
- Design implications for detector optimization
- Comparison to experimental measurement

Follow notebook 06 conventions: sys.path.insert for imports, matplotlib inline, consistent figure sizing (10x6 or similar), grid on all plots, proper axis labels with units.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -c "import nbformat; nb = nbformat.read('notebooks/07_dark_current.ipynb', as_version=4); print(f'Cells: {len(nb.cells)}'); assert len(nb.cells) >= 12, 'Too few cells'"</automated>
</verify>
<done>Notebook 07 exists with at least 12 cells covering all 6 sections. Dark current decomposition and sensitivity studies are visualized with publication-quality figures.</done>
</task>

</tasks>

<verification>
1. `python -c "from src.dark_current import sensitivity_sweep, plot_dark_current_decomposition"` -- new functions importable
2. `python -m pytest tests/ -v` -- all tests pass (no regression from adding functions)
3. `python -c "import nbformat; nb = nbformat.read('notebooks/07_dark_current.ipynb', as_version=4); print(len(nb.cells))"` -- notebook has sufficient cells
</verification>

<success_criteria>

- sensitivity_sweep produces DataFrame with dark current components for parameter variations
- plot_dark_current_decomposition creates publication-quality log-scale decomposition plot
- Notebook 07 guides user through complete dark current analysis workflow
- Notebook shows decomposed contributions (TAT vs SRH vs SRV) on same axes
- Notebook includes epi thickness, doping, and SRV sensitivity studies
- Zero regression in existing test suite
  </success_criteria>

<output>
After completion, create `.planning/phases/11-dark-current-modeling/11-02-SUMMARY.md`
</output>
