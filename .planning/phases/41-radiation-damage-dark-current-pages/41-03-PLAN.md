---
phase: 41-radiation-damage-dark-current-pages
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - app/workflows/dark_current.py
  - tests/test_app_dark_current_page.py
autonomous: true
requirements: [FEAT-02]

must_haves:
  truths:
    - "User can specify a temperature range (min, max, number of points) and a fixed operating bias, click Run, and see dark current vs temperature with J_SRH, J_TAT, J_SRV individually decomposed and overlaid"
    - "The temperature sweep is implemented via petringa.ParametricSweep(param='T', sim_fn=petringa.run_dark_current, ...), NOT a bias sweep and NOT a hand-rolled for-loop calling run_dark_current directly"
    - "A per-temperature failure (fewer successful points than requested) does not crash the page; an informational banner reports how many of n_temperatures succeeded"
    - "A CSV download button appears after a successful run, with one row per temperature"
  artifacts:
    - path: "app/workflows/dark_current.py"
      provides: "Full render() implementation replacing the Phase 38 placeholder, temperature-sweep architecture per RESEARCH.md Decision Addendum"
      min_lines: 50
    - path: "tests/test_app_dark_current_page.py"
      provides: "AppTest coverage for happy path (list[SimResult] fake), partial-failure tolerance, empty state, RuntimeError handling, ParametricSweep usage"
  key_links:
    - from: "app/workflows/dark_current.py"
      to: "petringa.ParametricSweep"
      via: "ParametricSweep(base_config=cfg, param='T', values=temperatures, sim_fn=petringa.run_dark_current, sim_kwargs={...}).run()"
      pattern: "ParametricSweep\\(.*param=\"T\""
    - from: "app/workflows/dark_current.py"
      to: "app/components/results.py::build_dark_current_figure"
      via: "import + st.plotly_chart call, after aggregating list[SimResult] into one SimResult"
      pattern: "build_dark_current_figure"
    - from: "app/workflows/dark_current.py"
      to: "app/components/results.py::to_csv_bytes"
      via: "import + st.download_button call"
      pattern: "to_csv_bytes"
---

<objective>
Replace the Phase 38 placeholder body of `app/workflows/dark_current.py` with a full
Run -> cache -> render -> download page implementing FEAT-02 per RESEARCH.md's Decision
Addendum: the page sweeps TEMPERATURE (not bias) at a fixed operating bias, using
`petringa.ParametricSweep(param="T", sim_fn=petringa.run_dark_current, ...)`, aggregates the
returned `list[SimResult]` into one temperature-indexed `SimResult`, and renders the J_SRH/J_TAT/
J_SRV decomposition via the `build_dark_current_figure` builder from Plan 41-01.

Purpose: Deliver the second of the two FEAT-01/FEAT-02 UI-wiring deliverables for Phase 41. This
is the page most at risk of being built wrong: RESEARCH.md's own Pattern 1/2 sections and Code
Examples describe an earlier, SUPERSEDED bias-sweep design; the Decision Addendum (near the end
of 41-RESEARCH.md) and 41-UI-SPEC.md both lock in a literal temperature x-axis via
`ParametricSweep`, and this plan implements ONLY that design.

Output: `app/workflows/dark_current.py` fully implemented; `tests/test_app_dark_current_page.py`
created with AppTest coverage for the list[SimResult]-aggregation architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-RESEARCH.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-UI-SPEC.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md
@app/workflows/cce.py
@app/workflows/dark_current.py
@app/components/results.py
@petringa/api/sweep.py
@tests/test_app_field_page.py
@tests/test_app_run_mockability.py
</context>

<interfaces>
<!-- CRITICAL SUPERSESSION NOTE: 41-RESEARCH.md's "Pattern 1" dark-current section, "Pattern 2"
     dark-current widget group, and the dark-current "Code Examples" (bias-axis
     build_dark_current_figure, bias_V CSV columns) are SUPERSEDED by the file's own
     "Decision Addendum" section near the end. This plan implements ONLY the Decision Addendum
     + 41-UI-SPEC.md's Dark Current Plot Contract. Do NOT build a bias-sweep page. -->

<!-- petringa.api.sweep.ParametricSweep (petringa/api/sweep.py) -->

```python
@dataclass
class ParametricSweep:
    base_config: "DeviceConfig"
    param: str          # e.g. "T" — must be a real DeviceConfig field
    values: list
    sim_fn: Callable     # e.g. petringa.run_dark_current
    sim_kwargs: dict = field(default_factory=dict)

    def run(self) -> "list[SimResult]":
        # Clones base_config via dataclasses.replace(base_config, **{param: value})
        # for each value, calls sim_fn(cloned_config, **sim_kwargs), collects results.
```

<!-- petringa.api.simulation.run_dark_current exact signature (petringa/api/simulation.py:665) —
     called once PER TEMPERATURE by ParametricSweep, at a fixed operating bias -->

```python
def run_dark_current(
    config: DeviceConfig,
    v_start: float = 0.0,
    v_stop: float = -100.0,
    n_points: int = 20,
    N_t: "float | None" = None,
    S_n: "float | None" = None,
    S_p: "float | None" = None,
) -> SimResult:
    # Returns: sim_type="dark_current", x=voltages (V, length n_points, MAY BE
    # TRUNCATED shorter than n_points with NO metadata flag — verified empirically),
    # y=I_total (A), metadata={"I_SRH", "I_TAT", "I_SRV", "area_cm2"}.
```

<!-- app/components/results.py (Plan 41-01 output) — build_dark_current_figure takes ONE
     aggregated SimResult (x=temperatures, y=I_total per temperature, metadata arrays of
     I_SRH/I_TAT/I_SRV per temperature) — NOT the raw list[SimResult] from ParametricSweep.run().
     THIS PAGE must perform the list-to-single-SimResult aggregation before calling the builder. -->

```python
def build_dark_current_figure(result: SimResult) -> go.Figure: ...
def to_csv_bytes(result: SimResult) -> bytes: ...  # dispatches on result.sim_type == "dark_current",
    # expects x=T_K array, metadata["I_SRH"/"I_TAT"/"I_SRV"] arrays of the SAME length as x.
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement app/workflows/dark_current.py render() with ParametricSweep(param="T") + list-to-SimResult aggregation</name>
  <files>app/workflows/dark_current.py</files>
  <read_first>
    app/workflows/dark_current.py (current placeholder — full file), app/workflows/cce.py (full
    file — Run->cache->render->download skeleton to adapt), petringa/api/sweep.py (full file —
    ParametricSweep.run() exact return shape), app/components/results.py (after Plan 41-01 —
    build_dark_current_figure's expected single-SimResult input shape),
    .planning/phases/41-radiation-damage-dark-current-pages/41-UI-SPEC.md (Widget label contract:
    Dark Current page table — T_min/T_max/n_temperatures/V_bias/N_t/S_n/S_p exact labels and
    defaults; Copywriting Contract: per-temperature truncation banner text),
    .planning/phases/41-radiation-damage-dark-current-pages/41-RESEARCH.md (Decision Addendum
    section — read in full, this supersedes the file's own earlier Pattern 1/2/Code Examples for
    this page), .planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md
    (confirmed sim_kwargs for the per-temperature run_dark_current call — v_start/v_stop/n_points
    combination verified not to raise; use these confirmed values, not a guess)
  </read_first>
  <behavior>
    - render() shows the empty-state guard (`cfg is None` -> `st.info(...)` + `st.stop()`) first
      (no persistent banner on this page — that requirement is radiation-damage-only).
    - render() shows the 1D-only guard (`cfg.half_width_um is not None` -> `st.warning(...)` +
      `st.stop()`) after the empty-state guard, per RESEARCH.md Pitfall 5's defensive
      recommendation.
    - Widget inputs, each with an explicit `key=`: `T_min` (`st.number_input`, min_value=1.0,
      value=250.0, key="dc_t_min"), `T_max` (`st.number_input`, min_value=1.0, value=400.0,
      key="dc_t_max"), `n_temperatures` (`st.number_input`, min_value=2, step=1, value=6,
      key="dc_n_temps"), `V_bias` (`st.number_input`, value=-20.0 or the spike-confirmed value
      from 41-01-SPIKE-NOTES.md if different, key="dc_v_bias"). Inside
      `st.expander("Advanced (trap/surface parameters)")`: `N_t`, `S_n`, `S_p` optional
      `st.number_input(value=None, format="%.3e")` overrides (keys "dc_nt", "dc_sn", "dc_sp"),
      blank/None by default meaning "use facade default" — pass `None` through unchanged when
      the widget is left blank (Streamlit's `number_input(value=None)` returns `None` until the
      user types a value).
    - `st.button("Run simulation")` click constructs `temperatures = np.linspace(T_min, T_max,
      int(n_temperatures))`, builds `sim_kwargs` using the spike-confirmed
      v_start/v_stop/n_points combination from 41-01-SPIKE-NOTES.md (both equal to `V_bias`, plus
      whatever `n_points` the spike confirmed works, e.g. 1 or 2) plus `N_t`, `S_n`, `S_p` from
      the (possibly-None) advanced overrides, then calls
      `petringa.ParametricSweep(base_config=cfg, param="T", values=temperatures,
      sim_fn=petringa.run_dark_current, sim_kwargs=sim_kwargs).run()` inside a
      `try/except RuntimeError` block (a RuntimeError here means the FIRST temperature's call
      raised outright, mirroring Phase 39's precedent for a hard first-point failure).
    - Aggregation (this page's unique step, not delegated to results.py): iterate the returned
      `list[SimResult]` alongside `temperatures`; for each `SimResult` that has `len(result.x) >=
      1`, take the FIRST element of `result.y`, `result.metadata["I_SRH"]`,
      `result.metadata["I_TAT"]`, `result.metadata["I_SRV"]` as that temperature's operating-point
      values (index 0, since v_start==v_stop makes every returned point equivalent); for any
      per-temperature result with `len(result.x) == 0` (or a result that could not be obtained),
      skip that temperature entirely from all 4 output arrays. Build `T_K = np.array(...)`,
      `I_total = np.array(...)`, `I_SRH = np.array(...)`, `I_TAT = np.array(...)`, `I_SRV =
      np.array(...)` of equal length (the count of temperatures that succeeded), then construct
      one aggregated `SimResult(config=cfg, sim_type="dark_current", x=T_K, y=I_total,
      metadata={"I_SRH": I_SRH, "I_TAT": I_TAT, "I_SRV": I_SRV})` and store it at
      `st.session_state["dark_current_result"]`. Also store the count `n_ok = len(T_K)` and the
      originally-requested `n_temperatures` so the partial-failure banner (see next bullet) can
      report "n_ok of n_temperatures" — store these two ints in
      `st.session_state["dark_current_n_ok"]` / `st.session_state["dark_current_n_requested"]`
      alongside the aggregated result at Run time.
    - After the button block: if `st.session_state.get("dark_current_result")` is not None, and
      `st.session_state.get("dark_current_n_ok", 0) < st.session_state.get("dark_current_n_requested", 0)`,
      show the UI-SPEC's exact per-temperature truncation `st.warning(...)` banner (with `n_ok`
      and `n_temperatures` interpolated) before the chart. Then render
      `st.plotly_chart(build_dark_current_figure(result))` and the CSV download button with
      `file_name="dark_current_result.csv"`.
  </behavior>
  <action>
    Rewrite app/workflows/dark_current.py's render() function per the &lt;behavior&gt; block
    above. Import `numpy as np`, `petringa` (module-level — `petringa.ParametricSweep` and
    `petringa.run_dark_current` both referenced as module attributes so tests can monkeypatch
    `petringa.run_dark_current`, mirroring the seam `petringa.ParametricSweep` itself uses
    internally), and `from app.components.results import build_dark_current_figure,
    to_csv_bytes`. Update the module docstring to describe the temperature-sweep architecture and
    explicitly note it supersedes any bias-sweep design implied elsewhere in RESEARCH.md (mirror
    the field_map.py docstring's pattern of documenting a non-obvious architectural decision
    inline). Use the exact widget labels/defaults and banner copy from the UI-SPEC verbatim.
  </action>
  <acceptance_criteria>
    - `grep -n "ParametricSweep" app/workflows/dark_current.py` shows it called with `param="T"` and `sim_fn=petringa.run_dark_current`
    - `grep -n "petringa.run_dark_current" app/workflows/dark_current.py` appears ONLY inside the `sim_fn=` kwarg to ParametricSweep, never called directly as a standalone `petringa.run_dark_current(cfg, ...)` line
    - `grep -n "np.linspace" app/workflows/dark_current.py` shows the temperature array construction (`np.linspace(T_min, T_max, ...)`), confirming temperature (not bias) is the swept parameter
    - `grep -n "build_dark_current_figure\|to_csv_bytes" app/workflows/dark_current.py` shows both imported and called
    - `python -c "import ast; ast.parse(open('app/workflows/dark_current.py').read())"` succeeds
  </acceptance_criteria>
  <done>app/workflows/dark_current.py implements the temperature-sweep architecture via ParametricSweep, aggregates list[SimResult] into one SimResult, and renders the 4-trace decomposition with partial-failure tolerance and CSV download.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppTest coverage for the dark current page's ParametricSweep-based temperature sweep</name>
  <files>tests/test_app_dark_current_page.py</files>
  <read_first>
    tests/test_app_field_page.py (full file — exact AppTest fixture/assertion pattern to
    replicate), tests/test_app_run_mockability.py (monkeypatch seam proof),
    app/workflows/dark_current.py (after Task 1 — exact session_state keys:
    "dark_current_result", "dark_current_n_ok", "dark_current_n_requested", and widget keys)
  </read_first>
  <behavior>
    - Test 1 (`test_empty_state_guard`): no device_config; assert
      "Configure a device in the sidebar to begin." in `at.info`.
    - Test 2 (`test_2d_config_shows_1d_only_warning`): `device_config =
      DeviceConfig(half_width_um=50.0)`; assert a warning containing "1D-only" appears.
    - Test 3 (`test_run_uses_parametric_sweep_and_caches_result`): monkeypatch
      `petringa.run_dark_current` with a fake that returns a clean single-point SimResult per
      call (`x=np.array([v]), y=np.array([1e-13]), metadata={"I_SRH": np.array([1e-13]),
      "I_TAT": np.array([-1e-14]), "I_SRV": np.array([0.0]), "area_cm2": 1e-4}` where `v` is
      whatever bias the sim_kwargs specify); seed `device_config = DeviceConfig()`; click Run;
      assert no exception; assert `at.session_state["dark_current_result"].sim_type ==
      "dark_current"`; assert `len(at.session_state["dark_current_result"].x) ==
      at.session_state["dark_current_n_requested"]` (all 6 temperatures succeeded, matching the
      default `n_temperatures=6`); assert the cached result's `x` array is monotonically
      increasing (proving it holds TEMPERATURES, e.g. values near 250-400, NOT bias values near
      0 to -100).
    - Test 4 (`test_partial_temperature_failure_shows_warning_not_crash`): monkeypatch
      `petringa.run_dark_current` with a fake that returns an EMPTY-x SimResult
      (`x=np.array([]), y=np.array([]), metadata={"I_SRH": np.array([]), "I_TAT": np.array([]),
      "I_SRV": np.array([])}`) for exactly one of the temperature values passed in (e.g. the 3rd
      call) and a normal single-point result for the rest (use a call-counter closure); click
      Run; assert no exception; assert `len(at.warning) >= 1` with a warning mentioning both a
      count less than the requested `n_temperatures` and the word "temperature"; assert the
      cached result's arrays are one element shorter than `n_temperatures`.
    - Test 5 (`test_first_call_runtime_error_shows_error_not_crash`): monkeypatch
      `petringa.run_dark_current` to raise `RuntimeError("...")` unconditionally; click Run;
      assert no exception, `len(at.error) >= 1`, and `"dark_current_result" not in
      at.session_state`.
  </behavior>
  <action>
    Create tests/test_app_dark_current_page.py mirroring tests/test_app_field_page.py's
    structure: module docstring explaining the ParametricSweep(param="T") architecture under
    test, a `_run_dark_current_page()` wrapper importing and calling
    `app.workflows.dark_current.render`, and the 5 test functions described in the
    &lt;behavior&gt; block, each using `monkeypatch.setattr(petringa, "run_dark_current", fake)`
    (NOT monkeypatching `ParametricSweep` itself — the real `ParametricSweep.run()` orchestration
    logic should execute for real against the fake `run_dark_current`, proving the page wires
    `ParametricSweep` correctly end-to-end) before `AppTest.from_function(...).run()`.
  </action>
  <acceptance_criteria>
    <automated>uv run pytest tests/test_app_dark_current_page.py -q</automated>
    - All 5 tests pass
    - `grep -c "monkeypatch.setattr(petringa, \"run_dark_current\"" tests/test_app_dark_current_page.py` returns at least 3
    - `grep -c "ParametricSweep" tests/test_app_dark_current_page.py` returns 0 (the real ParametricSweep class runs unmocked — only run_dark_current is faked, proving genuine end-to-end wiring)
  </acceptance_criteria>
  <done>tests/test_app_dark_current_page.py exists with 5 passing AppTest cases proving the page correctly wires ParametricSweep(param="T") over a faked run_dark_current, including partial-failure and hard-failure tolerance.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                        | Description                                                                                                                              |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Streamlit widget input -> ParametricSweep kwargs                | User-entered T_min/T_max/n_temperatures/V_bias/N_t/S_n/S_p flow into `np.linspace` and the per-temperature `run_dark_current` sim_kwargs |
| Per-temperature run_dark_current return -> aggregated SimResult | A truncated (empty-x) per-temperature result must be detected and excluded, not silently indexed into a crash                            |

## STRIDE Threat Register

| Threat ID       | Category                  | Component                                                                            | Disposition | Mitigation Plan                                                                                                                                                                                       |
| --------------- | ------------------------- | ------------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-06         | Denial of Service (local) | `n_temperatures` widget -> `np.linspace(T_min, T_max, int(n_temperatures))`          | mitigate    | `st.number_input(min_value=2, step=1)` on `n_temperatures` prevents a degenerate/empty linspace call                                                                                                  |
| T-41-07         | Denial of Service (local) | Per-temperature empty-x SimResult -> `result.y[0]` index access                      | mitigate    | Aggregation step explicitly checks `len(result.x) >= 1` before indexing `[0]`; a truncated per-temperature result is skipped, not indexed blindly                                                     |
| T-41-08         | Tampering                 | `N_t`/`S_n`/`S_p` optional override widgets forwarded into `run_dark_current` kwargs | accept      | These are physical-parameter overrides consumed entirely within the already-tested `petringa.core.dark_current` module; no code-injection surface (numeric `st.number_input` only, no free text/eval) |
| T-41-SC         | Tampering                 | npm/pip/cargo installs                                                               | accept      | No new package installs in this plan                                                                                                                                                                  |
| </threat_model> |

<verification>
Run `uv run pytest tests/test_app_dark_current_page.py -q` (per-file isolation convention) and
confirm all 5 tests pass. Manually confirm via `python -c "import ast;
ast.parse(open('app/workflows/dark_current.py').read())"` that the file has no syntax errors.
Grep-confirm `ParametricSweep` is invoked with `param="T"` and that `run_dark_current` is never
called directly outside the `sim_fn=` kwarg.
</verification>

<success_criteria>

- `app/workflows/dark_current.py` implements the temperature-sweep architecture via
  `petringa.ParametricSweep(param="T", sim_fn=petringa.run_dark_current, ...)`, satisfying
  FEAT-02 and ROADMAP Phase 41 Success Criterion 3 (literal "vs temperature" wording, per the
  Decision Addendum)
- `tests/test_app_dark_current_page.py` has 5 passing AppTest cases, none of which monkeypatch
  `ParametricSweep` itself (proving genuine end-to-end wiring against the real sweep class)
- CSV download button present after a successful run, one row per temperature
  </success_criteria>

<output>
Create `.planning/phases/41-radiation-damage-dark-current-pages/41-03-SUMMARY.md` when done
</output>
