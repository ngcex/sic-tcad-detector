---
phase: 42-microdosimetry-page-batch-sweep-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/results.py
  - tests/test_app_csv_export.py
  - .planning/phases/42-microdosimetry-page-batch-sweep-page/42-01-SPIKE-NOTES.md
autonomous: true
requirements: [FEAT-03, FEAT-04]

must_haves:
  truths:
    - "build_microdosimetry_figure exists as a pure Plotly builder (single trace, xaxis_type='log') callable without a Streamlit runtime"
    - "build_sweep_overlay_figure exists as a pure 4-arg builder (results, param, values, sim_label) selecting axis titles from a per-facade map"
    - "sweep_results_to_csv_bytes emits ONE CSV for N results with a leading run-identifier column named after the swept param"
    - "to_csv_bytes gains a 'microdosimetry' branch producing y_keV_per_um/y_times_d_y columns with y_F/y_D/l_bar_um in the # header"
    - "A live ParametricSweep(param='epi_thickness_um', values=[10,15,20], sim_fn=run_cce) over the default DeviceConfig returns 3 renderable (full or gracefully-truncated) SimResults"
  artifacts:
    - path: "app/components/results.py"
      provides: "build_microdosimetry_figure, build_sweep_overlay_figure, sweep_results_to_csv_bytes, to_csv_bytes microdosimetry branch"
    - path: "tests/test_app_csv_export.py"
      provides: "microdosimetry single-result CSV test + sweep bulk CSV shape test"
    - path: ".planning/phases/42-microdosimetry-page-batch-sweep-page/42-01-SPIKE-NOTES.md"
      provides: "confirmed convergence-safe batch-sweep default (run_cce + epi_thickness_um=[10,15,20]) for the Wave 2 batch sweep page plan"
  key_links:
    - from: "app/components/results.py::to_csv_bytes"
      to: "SimResult.sim_type"
      via: "new elif dispatch branch"
      pattern: "result\\.sim_type == \"microdosimetry\""
    - from: "app/components/results.py::build_sweep_overlay_figure"
      to: "_SWEEP_AXIS_TITLES"
      via: "sim_label lookup for per-facade axis titles"
      pattern: "_SWEEP_AXIS_TITLES"
    - from: "42-01-SPIKE-NOTES.md"
      to: "42-03-PLAN.md batch sweep default"
      via: "confirmed convergent param/values/sim combo"
      pattern: "epi_thickness_um|run_cce"
---

<objective>
Establish the shared, pure `app/components/results.py` foundation both Phase 42 pages depend on, and
de-risk the one still-unverified live claim (RESEARCH A1: that the locked batch-sweep default renders
3 usable curves). This plan adds three new pure builders/serializers plus a new `to_csv_bytes` branch,
extends the pure CSV-export test suite, and records a live-devsim spike note.

Purpose: Give both Wave 2 page plans (42-02 microdosimetry, 42-03 batch sweep) a single already-tested
`results.py` dependency instead of each duplicating figure/serializer code (which would also create a
same-wave file conflict), and confirm the convergence-safe batch-sweep default BEFORE the page plan
hardcodes it — mirroring the 41-01 spike-first precedent.

Output: `app/components/results.py` extended with `build_microdosimetry_figure`, `build_sweep_overlay_figure`,
`sweep_results_to_csv_bytes`, and a new `microdosimetry` branch in `to_csv_bytes`; `tests/test_app_csv_export.py`
extended with two new pure tests; `42-01-SPIKE-NOTES.md` recording the confirmed convergence-safe default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-microdosimetry-page-batch-sweep-page/42-RESEARCH.md
@.planning/phases/42-microdosimetry-page-batch-sweep-page/42-PATTERNS.md
@.planning/phases/42-microdosimetry-page-batch-sweep-page/42-UI-SPEC.md
@.planning/phases/42-microdosimetry-page-batch-sweep-page/42-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor builds against. Extracted from codebase — no exploration needed. -->

From petringa/api/results.py (SimResult): a dataclass with fields
`config` (DeviceConfig), `sim_type` (str), `x` (np.ndarray), `y` (np.ndarray),
`metadata` (dict), `mesh` (optional). Import as `from petringa import SimResult`.

From petringa/api/sweep.py:

- `ParametricSweep(base_config, param, values, sim_fn, sim_kwargs={}).run() -> list[SimResult]`
  - clones base_config per value via `dataclasses.replace(base_config, **{param: value})`, then calls `sim_fn(cloned_config, **sim_kwargs)`. Unknown param -> TypeError (no setattr/eval).

From petringa/api/simulation.py:

- `run_cce(config, ...) -> SimResult` — sim_type="cce", default v_stop=-40 (inside convergence envelope); truncates gracefully (metadata["truncated"]=True) rather than raising.
- `run_microdosimetry(config, mc_csv_path: str, sv_thickness_um=10.0, sv_width_um=150.0) -> SimResult` — sim_type="microdosimetry"; x=bin centers (keV/µm, spans ~0.01–9772), y=y·d(y); metadata keys include "y_F", "y_D", "l_bar_um" (all keV/µm except l_bar_um in µm).

Existing app/components/results.py (this file) already imports `petringa`, `numpy as np`,
`pandas as pd`, `plotly.graph_objects as go`, `asdict`, `datetime`/`timezone`, `SimResult`.
Existing pure builders to mirror: `build_damage_figure` (single trace, log-x, color "#1F6FEB"),
`build_dark_current_figure` (multi-trace palette loop). `to_csv_bytes` already has branches for
`cv|cce|field|damage|dark_current` and a shared `# device:`/`# generated:` header + final
`else: raise ValueError`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two pure figure builders to results.py</name>
  <files>app/components/results.py</files>
  <read_first>
    - app/components/results.py (current builders: build_damage_figure lines 63-86 for the single-trace log-x analog; build_dark_current_figure lines 89-132 for the multi-trace palette-loop analog; module purity contract lines 1-11)
    - .planning/phases/42-microdosimetry-page-batch-sweep-page/42-PATTERNS.md (results.py additions section, lines 236-281 — the two builder sketches AND the flagged 4-arg signature override)
    - .planning/phases/42-microdosimetry-page-batch-sweep-page/42-UI-SPEC.md (Plot Contract lines 174-199 — the mandatory xaxis_type="log", the value-keyed palette, and the per-facade axis-title map)
  </read_first>
  <action>
    Add two new PURE functions (no st.* calls) to app/components/results.py, matching the existing module purity contract.

    (1) `build_microdosimetry_figure(result: SimResult) -> go.Figure` — single `go.Scatter`, `mode="lines"`,
    `line=dict(color="#1F6FEB")`, plotting `result.x` vs `result.y`. Layout: title `"Microdosimetric Spectrum"`,
    `xaxis_title="Lineal energy y (keV/µm)"`, `xaxis_type="log"` (MANDATORY — the fixture x-range spans
    0.01–9772 keV/µm; a linear x-axis is a defect), `yaxis_title="y · d(y)"`. This mirrors build_damage_figure's
    single-trace log-x shape.

    (2) `build_sweep_overlay_figure(results, param, values, sim_label) -> go.Figure` — FOUR arguments, NOT the
    three-arg `(results, param, values)` sketch in RESEARCH.md (that RESEARCH sketch is SUPERSEDED by UI-SPEC; the
    4th `sim_label` argument is required to pick axis titles). Define a module-level constant
    `_SWEEP_PALETTE = ["#1F6FEB", "#D32F2F", "#2E7D32", "#1A1A1A", "#9AA0A6"]` and a module-level dict
    `_SWEEP_AXIS_TITLES` keyed by the SIM_FACADES selectbox LABEL with (x_title, y_title) tuples:
    `"CCE vs bias (run_cce)"` -> `("Bias V (V)", "Charge Collection Efficiency")`,
    `"C-V (run_cv)"` -> `("Bias V (V)", "Capacitance (F)")`,
    `"CCE vs temperature (run_temperature_sweep)"` -> `("Temperature (K)", "Value")`.
    The builder loops `for i, (val, res) in enumerate(zip(values, results))` adding one
    `go.Scatter(x=res.x, y=res.y, mode="lines+markers", name=f"{param}={val}", line=dict(color=_SWEEP_PALETTE[i % len(_SWEEP_PALETTE)]))`
    per swept value — palette cycled by TRACE ORDER (value-keyed qualitative), NOT Phase 41's fixed-quantity mapping.
    Select `x_title, y_title = _SWEEP_AXIS_TITLES.get(sim_label, ("(facade x-axis)", "Value"))` and set layout
    title `f"Parametric Sweep: {param}"` with those axis titles. Do NOT ship the literal `"(facade x-axis)"` as the
    normal path — it is only the defensive fallback for an unknown label.

  </action>
  <verify>
    <automated>uv run python -c "from app.components.results import build_microdosimetry_figure, build_sweep_overlay_figure, _SWEEP_AXIS_TITLES; import numpy as np; from petringa import DeviceConfig, SimResult; r=SimResult(config=DeviceConfig(), sim_type='microdosimetry', x=np.geomspace(0.01,9000,300), y=np.random.rand(300), metadata={'y_F':17.0,'y_D':53.0,'l_bar_um':20.0}); f=build_microdosimetry_figure(r); assert f.layout.xaxis.type=='log'; g=build_sweep_overlay_figure([r,r,r],'epi_thickness_um',[10,15,20],'CCE vs bias (run_cce)'); assert len(g.data)==3; assert g.layout.xaxis.title.text=='Bias V (V)'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `build_microdosimetry_figure` returns a `go.Figure` whose `layout.xaxis.type == "log"` and whose single trace has `line.color == "#1F6FEB"`
    - `build_sweep_overlay_figure` accepts exactly 4 positional args `(results, param, values, sim_label)` and returns a figure with `len(fig.data) == len(results)` traces
    - Each overlay trace name equals `f"{param}={val}"` for its swept value, and trace i uses color `_SWEEP_PALETTE[i % 5]`
    - Passing `sim_label="CCE vs bias (run_cce)"` yields `xaxis.title.text == "Bias V (V)"` and `yaxis.title.text == "Charge Collection Efficiency"`
    - Neither function contains any `st.` reference (module purity preserved)
  </acceptance_criteria>
  <done>Both pure builders exist in results.py with the 4-arg overlay signature and per-facade axis map; the inline verify command prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Add sweep bulk-CSV serializer and the microdosimetry to_csv_bytes branch</name>
  <files>app/components/results.py</files>
  <read_first>
    - app/components/results.py (to_csv_bytes lines 160-248 — the per-sim_type elif dispatch, the shared `# device:` header block lines 234-248, and the final `else: raise ValueError`; the damage branch lines 212-219 as the extra_header_lines analog)
    - .planning/phases/42-microdosimetry-page-batch-sweep-page/42-PATTERNS.md (sweep_results_to_csv_bytes sketch lines 283-297; the new microdosimetry branch spec lines 300)
    - .planning/phases/42-microdosimetry-page-batch-sweep-page/42-RESEARCH.md (Pitfall 2 lines 277-282 — bulk CSV is a SEPARATE function, NOT a to_csv_bytes branch; and the single-result microdosimetry download is Q2-resolved as INCLUDED)
  </read_first>
  <action>
    Two additions to app/components/results.py, both pure.

    (1) New standalone function `sweep_results_to_csv_bytes(results, param, values) -> bytes` — a SEPARATE bulk
    serializer (do NOT route this through to_csv_bytes, which is single-result and dispatches on sim_type; see
    Pitfall 2). Build one DataFrame per (val, res) as `pd.DataFrame({param: val, "x": res.x, "y": res.y})`,
    `pd.concat(frames, ignore_index=True)` into one combined frame so the leading run-identifier column is named
    literally after the swept `param` (e.g. a column `epi_thickness_um`). Prepend a `#`-comment header mirroring
    the existing to_csv_bytes convention: `f"# petringa SiC TCAD Simulator — parametric sweep ({param})"`,
    `f"# software_version: {petringa.__version__}"`, `f"# generated: {datetime.now(timezone.utc).isoformat()}"`,
    `f"# swept_values: {list(values)}"`. Return `("\n".join(header_lines) + "\n" + combined.to_csv(index=False)).encode("utf-8")`.
    This bulk CSV omits the `# device:` line (it is a multi-run export) and adds `# swept_values:` instead.

    (2) New `elif result.sim_type == "microdosimetry":` branch inside to_csv_bytes, inserted BEFORE the final
    `else: raise ValueError`. Set `df = pd.DataFrame({"y_keV_per_um": result.x, "y_times_d_y": result.y})` and
    `extra_header_lines = [f"# y_F_keV_per_um: {result.metadata['y_F']}", f"# y_D_keV_per_um: {result.metadata['y_D']}", f"# l_bar_um: {result.metadata['l_bar_um']}"]`.
    The shared `# device:` header + `# generated:` footer (lines 234-248) apply unchanged to this branch. Do NOT
    modify any existing branch (cv/cce/field/damage/dark_current) or the shared header assembly.

  </action>
  <verify>
    <automated>uv run pytest tests/test_app_csv_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `to_csv_bytes(microdosimetry_result)` returns bytes whose decoded text contains header columns `y_keV_per_um,y_times_d_y` and `#` header lines carrying `y_F_keV_per_um`, `y_D_keV_per_um`, and `l_bar_um`
    - The unknown-sim_type `else: raise ValueError` path is still reached for any sim_type other than the six now-supported types
    - `sweep_results_to_csv_bytes([r1,r2,r3], "epi_thickness_um", [10,15,20])` returns ONE CSV whose data rows total `len(r1.x)+len(r2.x)+len(r3.x)` and which contains a column literally named `epi_thickness_um`
    - The bulk CSV `#` header contains `swept_values:` and `software_version:` lines
    - Neither addition contains any `st.` reference
  </acceptance_criteria>
  <done>The microdosimetry branch and the standalone bulk serializer exist; tests/test_app_csv_export.py passes (after Task 3 adds the new cases).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend CSV-export tests + run the batch-sweep-default live spike</name>
  <files>tests/test_app_csv_export.py, .planning/phases/42-microdosimetry-page-batch-sweep-page/42-01-SPIKE-NOTES.md</files>
  <read_first>
    - tests/test_app_csv_export.py (existing per-sim_type tests, e.g. test_cv_csv_columns_and_header lines 24-... and the damage test — mirror their hand-built SimResult -> to_csv_bytes -> assert-columns/header shape; the imports line 17)
    - app/components/results.py (the Task 1/2 additions being tested — build the fixtures to match the exact column names and header keys)
    - .planning/phases/42-microdosimetry-page-batch-sweep-page/42-RESEARCH.md (Wave 0 Gaps lines 468-472; A1 spike lines 288-294 — run_cce + epi_thickness_um=[10,15,20] over default DeviceConfig; Environment Availability lines 424-434 — devsim available despite a libopenblas warning)
    - .planning/phases/41-radiation-damage-dark-current-pages/41-01-PLAN.md (spike-notes precedent for the SPIKE-NOTES.md shape and how confirmed defaults are recorded for downstream page plans)
  </read_first>
  <behavior>
    - test_microdosimetry_csv_columns_and_header: a hand-built SimResult(sim_type="microdosimetry", x, y, metadata={"y_F","y_D","l_bar_um",...}) serialized via to_csv_bytes yields columns `y_keV_per_um,y_times_d_y` and header lines containing `y_F_keV_per_um`, `y_D_keV_per_um`, `l_bar_um`
    - test_sweep_results_to_csv_bytes_shape: three hand-built SimResults through sweep_results_to_csv_bytes produce ONE CSV with a column named after `param`, N-runs worth of concatenated data rows, and a `swept_values:` header line — pure, no Streamlit, no devsim
  </behavior>
  <action>
    Add two pure tests to tests/test_app_csv_export.py (no Streamlit runtime, no devsim build — hand-built SimResult
    fixtures only), and import `sweep_results_to_csv_bytes` alongside the existing `to_csv_bytes` import.

    `test_microdosimetry_csv_columns_and_header` — mirror the existing damage-branch test: build a
    `SimResult(config=DeviceConfig(), sim_type="microdosimetry", x=np.geomspace(0.01, 9000, 5), y=np.array([...5 values...]), metadata={"y_F": 17.23, "y_D": 53.22, "l_bar_um": 20.0})`,
    decode `to_csv_bytes(result)`, assert the CSV column header line contains `y_keV_per_um` and `y_times_d_y`, and assert
    the `#` header contains `y_F_keV_per_um`, `y_D_keV_per_um`, and `l_bar_um` (parse via the existing `_decode` helper +
    `pd.read_csv(StringIO(...), comment="#")` shape used by the sibling tests).

    `test_sweep_results_to_csv_bytes_shape` — build three `SimResult`s (sim_type "cce", differing x/y lengths acceptable),
    call `sweep_results_to_csv_bytes(results, "epi_thickness_um", [10.0, 15.0, 20.0])`, decode, and assert: exactly one CSV
    body, the parsed frame has a column literally named `epi_thickness_um`, the total data-row count equals the sum of the
    three results' `len(x)`, and the `#` header contains a `swept_values:` line.

    Then run the LIVE-devsim spike (RESEARCH A1) via a throwaway `uv run python -c` one-liner (NOT committed as a test —
    devsim solves are too slow/resource-heavy for the suite; STATE.md): execute
    `petringa.ParametricSweep(base_config=petringa.DeviceConfig(), param="epi_thickness_um", values=[10,15,20], sim_fn=petringa.run_cce).run()`
    and record in `42-01-SPIKE-NOTES.md`: how many of the 3 results returned non-empty x arrays, whether any set
    `metadata["truncated"]`, the x/y length of each, and a one-line VERDICT confirming (or, if it fails, revising) the
    locked batch-sweep default that 42-03 will hardcode (`sim_label="CCE vs bias (run_cce)"`, `param="epi_thickness_um"`,
    `values="10, 15, 20"`). If the spike shows fewer than 3 renderable curves, record the adjusted convergence-safe default
    (e.g. a thicker-epi range) instead — 42-03 will read this file for its default. Note the benign libopenblas DLL warning
    if it appears (devsim still solves — Environment Availability).

  </action>
  <verify>
    <automated>uv run pytest tests/test_app_csv_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_app_csv_export.py -q` passes including the two new tests
    - `test_microdosimetry_csv_columns_and_header` asserts both the `y_keV_per_um`/`y_times_d_y` columns and all three metadata header keys
    - `test_sweep_results_to_csv_bytes_shape` asserts the param-named run-identifier column, the summed row count, and the `swept_values:` header line
    - `42-01-SPIKE-NOTES.md` exists and states, for the run_cce + epi_thickness_um=[10,15,20] spike, a per-value renderable/truncated/empty count and an explicit VERDICT naming the convergence-safe default for 42-03
  </acceptance_criteria>
  <done>Both new CSV tests pass under per-file isolation; SPIKE-NOTES.md records the confirmed (or revised) batch-sweep default for the Wave 2 batch sweep page.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

- (none new in this plan): this plan touches only pure serializers/builders in `app/components/results.py` and pure tests. No user input crosses any boundary here — the input surfaces (CSV upload, value-list parse) live in the Wave 2 page plans (42-02, 42-03). The live spike calls `run_cce` with hardcoded planner-controlled values, not user input.

## STRIDE Threat Register

- **T-42-01-I** — Information Disclosure — `sweep_results_to_csv_bytes` / `to_csv_bytes` microdosimetry branch header — _accept_: headers emit only `petringa.__version__`, a UTC timestamp, device config fields, and swept values — no secrets/PII (single-user local lab tool). Same header convention as the five existing branches.
- **T-42-01-D** — Denial of Service (local) — live-devsim spike — _accept_: the spike runs a fixed 3-value `run_cce` sweep once, by the planner/executor, not in the committed test suite (kept out to avoid devsim resource exhaustion per STATE.md). No recurring cost.
- **T-42-01-SC** — Tampering — npm/pip/cargo installs — _mitigate_: no package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages; every dep already vetted). No install task exists — planner must not invent one.
  </threat_model>

<verification>
- `uv run pytest tests/test_app_csv_export.py -q` passes (existing + 2 new tests) under per-file isolation.
- Both new builders and the bulk serializer import and execute without a Streamlit runtime (module purity preserved — no `st.` anywhere in results.py).
- `42-01-SPIKE-NOTES.md` records the confirmed convergence-safe batch-sweep default.
</verification>

<success_criteria>

- `app/components/results.py` exposes `build_microdosimetry_figure`, `build_sweep_overlay_figure` (4-arg), `sweep_results_to_csv_bytes`, and a `microdosimetry` branch in `to_csv_bytes`.
- All new results.py functions are pure (no `st.*`).
- `tests/test_app_csv_export.py` green with the microdosimetry single-result and sweep bulk-CSV shape tests.
- The Wave 2 batch sweep default is confirmed (or revised) in `42-01-SPIKE-NOTES.md`.
  </success_criteria>

<output>
Create `.planning/phases/42-microdosimetry-page-batch-sweep-page/42-01-SUMMARY.md` when done.
</output>
