---
phase: 36-core-api-deviceconfig-c-v-field-vertical-slice
verified: 2026-07-02T18:30:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 36: Core API — DeviceConfig + C-V + Field + Vertical Slice Verification Report

**Phase Goal:** Developers can call `run_cv(DeviceConfig())` and `run_field(DeviceConfig(), bias_V=-100)` from a Python script and get physically reasonable results — the public API contract is established and validated end-to-end
**Verified:** 2026-07-02T18:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

All four truths were verified by executing live devsim solves against `.venv/bin/python` (the project's `uv`-managed virtualenv, per project convention — NOT the system `/opt/miniconda3` python, which does not have `etna` installed), not by trusting SUMMARY.md claims.

| #   | Truth (ROADMAP Success Criteria)                                                                                                                                                                                      | Status   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `from etna import DeviceConfig` and instantiation with named parameters covering geometry, doping profile (uniform or graded), and operating conditions (T, area) works without error                             | VERIFIED | Live run: imported `DeviceConfig` from `etna`, confirmed all 11 dataclass fields (`epi_thickness_um, substrate_thickness_um, half_width_um, N_A, doping_profile, N_D, N_D_junction, N_D_bulk, L_transition_um, T, area_cm2`) with spec-correct defaults (N_D_junction=2.93e15, N_D_bulk=8.82e13, L_transition_um=0.987, doping_profile="graded"); instantiated with named params overriding geometry/doping/T/area (`DeviceConfig(epi_thickness_um=8.0, N_A=2e19, T=310.0, area_cm2=2e-4, doping_profile='uniform', N_D=1e15)`) — succeeded |
| 2   | `from etna import run_cv` and a call returning a `SimResult` with bias array, capacitance array, depletion width, and 1/C² metadata works; capacitance values decrease monotonically with increasing reverse bias | VERIFIED | Live run: `run_cv(DeviceConfig(), v_start=0, v_stop=-30, n_points=4)` returned `SimResult(sim_type="cv")`, `x`=[0,-10,-20,-30], `y`(C)=[5.06e-13, 9.03e-14, 8.61e-14, 8.60e-14] (monotonically decreasing), `metadata` contains `depletion_widths` and `one_over_C_squared` keys, all C>0 and finite                                                                                                                                                                                                                                            |
| 3   | `from etna import run_field` returns a `SimResult` whose `.mesh` attribute is a populated `MeshData` with node coordinates, electric field, and net doping values extracted from devsim post-build                | VERIFIED | Live run 1D: `run_field(DeviceConfig(), bias_V=-50)` → `mesh` is a `MeshData` instance, 327 nodes, `node_values` has `NetDoping`/`Potential`/`ElectricField` all length-327 and finite, `y_coords is None`, `max                                                                                                                                                                                                                                                                                                                                | E   | `=115042 V/cm (non-trivial, real solve). Live run 2D: `run_field(DeviceConfig(half_width_um=50.0), bias_V=-10)`→ 8502 nodes,`y_coords`populated (len matches x_coords),`max | E   | `=65288 V/cm finite. CR-01 fix confirmed live: 2D `result.x`/`result.y` are empty arrays (not mislabeled lateral coordinates) |
| 4   | `python examples/cv_example.py` runs end-to-end without error and prints a capacitance array with physically reasonable values (C decreasing with reverse bias)                                                       | VERIFIED | Executed `.venv/bin/python examples/cv_example.py` — exit 0, printed bias array (7 converged points, 0V to -63.2V — `cv_sweep`'s pre-existing partial-convergence behavior documented in 36-02-SUMMARY.md, not a Phase 36 defect since PROJECT.md forbids solver changes) and capacitance array C=[5.06e-13 ... 8.60e-14] F, final printed line: "C at 0.0V = 5.0608e-13 F, C at -63.2V = 8.6003e-14 F (decreasing with reverse bias)"                                                                                                          |

**Score:** 4/4 truths verified

**Requirement-level note (LIB-01):** LIB-01 is functionally satisfied in the codebase (see Truth 1 evidence above and Requirements Coverage table below), but the checkbox in `.planning/REQUIREMENTS.md` line 18 still reads `- [ ]` (Pending), unlike LIB-02/LIB-03/LIB-05 which were explicitly ticked in dedicated `docs(36-0X): mark ... complete` commits (`720e520`, `93213ae`). Plan 01's completion commit (`6801a06`) did not include the corresponding REQUIREMENTS.md tick for LIB-01. This is a documentation-hygiene gap, not a code gap — the underlying capability was verified working live. Recommend ticking `LIB-01` in REQUIREMENTS.md to match the verified implementation (no code change needed).

### Required Artifacts

| Artifact                     | Expected                                                    | Status   | Details                                                                                                                                                                                                                                                                                        |
| ---------------------------- | ----------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `etna/api/__init__.py`   | Package marker                                              | VERIFIED | Exists, package importable                                                                                                                                                                                                                                                                     |
| `etna/api/results.py`    | `SimResult`, `MeshData` dataclasses (spec sections 3.2/3.3) | VERIFIED | Both dataclasses present, exact field order (`x_coords, y_coords, node_values, regions, contacts` for MeshData; `config, sim_type, x, y, metadata, mesh` for SimResult), `TYPE_CHECKING`-only `DeviceConfig` import (no runtime cycle)                                                         |
| `etna/api/device.py`     | `DeviceConfig` dataclass + `build_device()` facade          | VERIFIED | 11 fields with spec-correct defaults; `build_device()` dispatches 1D (`create_dd_device`) vs 2D (`create_sic_2d_device` + `setup_poisson` + `solve_equilibrium` + `setup_sic_drift_diffusion` + `dd_initialized=True`); um→cm conversions present for epi/substrate thickness and L_transition |
| `etna/__init__.py`       | Public re-exports                                           | VERIFIED | Re-exports `DeviceConfig, SimResult, MeshData, run_cv, run_field, __version__`; no duplicate `DeviceConfig` definition; `__all__` matches design spec section 4 export set exactly                                                                                                             |
| `etna/api/simulation.py` | `run_cv()`, `run_field()` facades                           | VERIFIED | Both functions present, substantive (not stubs), call through to core physics modules, live-tested successfully                                                                                                                                                                                |
| `examples/cv_example.py`     | LIB-05 vertical-slice script                                | VERIFIED | Imports only from public `etna` package; runs end-to-end (exit 0) via `.venv/bin/python`; prints monotonic-decrease summary                                                                                                                                                                |
| `tests/test_api_cv.py`       | run_cv output-shape + physics test                          | VERIFIED | 2 tests pass (`test_run_cv_output_shape_and_physics`, `test_run_cv_rejects_2d_config` added in review-fix cc87c08)                                                                                                                                                                             |
| `tests/test_api_field.py`    | run_field MeshData/physical-sanity test                     | VERIFIED | 2 tests pass (1D + required 2D regression guard, not skipped)                                                                                                                                                                                                                                  |
| `tests/test_api_device.py`   | build_device() unit tests (review-fix addition)             | VERIFIED | 3 tests pass — 1D/2D `dd_initialized` assertions + unique device-name generation                                                                                                                                                                                                               |

### Key Link Verification

| From                                    | To                                                                    | Via                                            | Status | Details                                                                                                                                                                                                      |
| --------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `etna/api/device.py::build_device`  | `etna/core/device.py::create_sic_device` (via `create_dd_device`) | 1D branch, um→cm conversion                    | WIRED  | Live-exercised: 1D `build_device()` call in `run_cv`/`run_field` produced a real, DD-initialized 1D device (327 nodes at bias_V=-50)                                                                         |
| `etna/api/device.py::build_device`  | `etna/core/device2d.py::create_sic_2d_device`                     | 2D branch                                      | WIRED  | Live-exercised: 2D `build_device()` call produced a real DD-initialized 2D device (8502 nodes)                                                                                                               |
| `etna/api/device.py::build_device`  | `etna/core/drift_diffusion.py::setup_sic_drift_diffusion`         | 2D branch DD-init                              | WIRED  | Confirmed both by source (`grep` shows `setup_sic_drift_diffusion` + `dd_initialized`) and live behavior — 2D device successfully ramped by `ramp_bias` in `run_field`, which requires `dd_initialized=True` |
| `etna/__init__.py`                  | `etna/api/device.py`                                              | re-export                                      | WIRED  | `from etna import DeviceConfig` succeeds, `is` identical to `etna.api.device.DeviceConfig`                                                                                                           |
| `etna/api/simulation.py::run_cv`    | `etna/api/device.py::build_device`                                | build DD device                                | WIRED  | Live: `run_cv` produced a real solved device before sweeping                                                                                                                                                 |
| `etna/api/simulation.py::run_cv`    | `etna/core/cv_analysis.py::cv_sweep`                              | bias sweep → capacitance                       | WIRED  | Live: capacitance array returned, monotonically decreasing                                                                                                                                                   |
| `etna/__init__.py`                  | `etna/api/simulation.py`                                          | re-export run_cv                               | WIRED  | `from etna import run_cv` succeeds                                                                                                                                                                       |
| `etna/api/simulation.py::run_field` | `etna/api/device.py::build_device`                                | build + solve device                           | WIRED  | Live: both 1D and 2D devices built and ramped successfully                                                                                                                                                   |
| `etna/api/simulation.py::run_field` | `etna/core/drift_diffusion.py::ramp_bias`                         | ramp to reverse bias, cathode, sign convention | WIRED  | Live: ramp succeeded for both dimensions; sign convention (`V_target=-bias_V`, `contact="cathode"`) confirmed matching `cv_analysis.py:171` per REVIEW-FIX and source read                                   |
| `etna/api/simulation.py::run_field` | `devsim.get_node_model_values`                                        | extract x/(y)/NetDoping/Potential node arrays  | WIRED  | Live: extracted arrays are finite, correct length, non-trivial values                                                                                                                                        |
| `etna/__init__.py`                  | `etna/api/simulation.py`                                          | re-export run_field                            | WIRED  | `from etna import run_field` succeeds                                                                                                                                                                    |

### Data-Flow Trace (Level 4)

| Artifact         | Data Variable                                    | Source                                                                           | Produces Real Data                                                            | Status  |
| ---------------- | ------------------------------------------------ | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------- |
| `run_cv()`       | `result.y` (capacitance)                         | `cv_sweep(device_info, ...)` — real devsim iterative solve                       | Yes — non-trivial, physically decreasing values from a converged Newton solve | FLOWING |
| `run_field()` 1D | `mesh.node_values["ElectricField"]`              | `extract_electric_field(device_info)` (devsim edge model) interpolated to nodes  | Yes — max                                                                     | E       | =115042 V/cm, finite, non-zero at real reverse bias | FLOWING |
| `run_field()` 2D | `mesh.node_values["ElectricField"]`              | `LinearNDInterpolator` + `np.gradient` on real Potential node values from devsim | Yes — max                                                                     | E       | =65288 V/cm, finite, non-zero                       | FLOWING |
| `run_field()`    | `mesh.node_values["NetDoping"]`, `["Potential"]` | `devsim.get_node_model_values(...)` direct post-build reads                      | Yes — real per-node devsim state, not static/hardcoded                        | FLOWING |

### Behavioral Spot-Checks

| Behavior                                        | Command                                                                                  | Result                                                                                               | Status |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------ |
| DeviceConfig import + named-param instantiation | `python -c "from etna import DeviceConfig; DeviceConfig(epi_thickness_um=8.0, ...)"` | Instantiated successfully with all fields                                                            | PASS   |
| run_cv returns monotonically decreasing C       | `run_cv(DeviceConfig(), v_start=0, v_stop=-30, n_points=4)`                              | C=[5.06e-13, 9.03e-14, 8.61e-14, 8.60e-14], strictly decreasing, all finite/positive                 | PASS   |
| run_field 1D MeshData populated with real field | `run_field(DeviceConfig(), bias_V=-50)`                                                  | 327-node mesh, ElectricField finite, max=115042 V/cm                                                 | PASS   |
| run_field 2D MeshData populated with real field | `run_field(DeviceConfig(half_width_um=50.0), bias_V=-10)`                                | 8502-node mesh, y_coords populated, ElectricField finite, max=65288 V/cm                             | PASS   |
| CR-01 fix (2D x/y not mislabeled)               | Inspect `result.x`/`result.y` for 2D `run_field` call                                    | Both empty arrays (not lateral-coordinate-mislabeled-as-depth)                                       | PASS   |
| examples/cv_example.py end-to-end               | `.venv/bin/python examples/cv_example.py`                                                | Exit 0, prints "C at 0.0V = 5.0608e-13 F, C at -63.2V = 8.6003e-14 F (decreasing with reverse bias)" | PASS   |
| tests/test_api_cv.py                            | `.venv/bin/python -m pytest tests/test_api_cv.py -q`                                     | 2 passed                                                                                             | PASS   |
| tests/test_api_field.py                         | `.venv/bin/python -m pytest tests/test_api_field.py -q`                                  | 2 passed (17.10s, includes required 2D regression guard)                                             | PASS   |
| tests/test_api_device.py                        | `.venv/bin/python -m pytest tests/test_api_device.py -q`                                 | 3 passed                                                                                             | PASS   |
| tests/test_cv.py (regression)                   | `.venv/bin/python -m pytest tests/test_cv.py -q`                                         | 13 passed, no regression                                                                             | PASS   |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared for this phase or found in the repository. SKIPPED (no runnable probe scripts for this phase; verification instead relied on direct behavioral spot-checks against the public API, which is a stronger check for a library-facade phase).

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                                                   | Status                                               | Evidence                                                                                                                     |
| ----------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| LIB-01      | 36-01       | Developer can import `DeviceConfig` from `etna` and instantiate with named geometry/doping/operating parameters           | SATISFIED (code) / stale checkbox in REQUIREMENTS.md | Live import + instantiation with named params verified (Truth 1). REQUIREMENTS.md line 18 still shows `[ ]` — see note below |
| LIB-02      | 36-02       | Developer can import `run_cv()` and call with `DeviceConfig` to get `SimResult` (bias, capacitance, depletion width, 1/C²)    | SATISFIED                                            | Live call verified (Truth 2); REQUIREMENTS.md correctly ticked `[x]`                                                         |
| LIB-03      | 36-03       | Developer can import `run_field()` and call with `DeviceConfig` + bias to get `SimResult` with populated `.mesh` (`MeshData`) | SATISFIED                                            | Live call verified for both 1D and 2D (Truth 3); REQUIREMENTS.md correctly ticked `[x]`                                      |
| LIB-05      | 36-02       | Developer can run `examples/cv_example.py` without errors; capacitance physically reasonable (decreasing with reverse bias)   | SATISFIED                                            | Live execution verified (Truth 4); REQUIREMENTS.md correctly ticked `[x]`                                                    |

**Orphaned requirements check:** `.planning/REQUIREMENTS.md`'s phase-mapping table (lines 193-199) maps exactly LIB-01, LIB-02, LIB-03, LIB-05 to Phase 36 — matching the phase's declared requirement IDs and all three plans' `requirements:` frontmatter fields combined (`[LIB-01]` + `[LIB-02, LIB-05]` + `[LIB-03]`). No orphaned requirements found.

### Anti-Patterns Found

None. Scanned all 9 phase-modified/created files (`etna/api/__init__.py`, `etna/api/results.py`, `etna/api/device.py`, `etna/api/simulation.py`, `etna/__init__.py`, `examples/cv_example.py`, `tests/test_api_cv.py`, `tests/test_api_field.py`, `tests/test_api_device.py`) for debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER), placeholder language, and empty-implementation patterns (`return null`, `return {}`, `return []`, `=> {}`). No matches. Code review (`36-REVIEW.md`) identified 1 critical (CR-01) and 3 warnings (WR-01/02/03), all 4 fixed and verified in `36-REVIEW-FIX.md` and confirmed live in this verification pass (CR-01 fix confirmed by direct execution, not just source inspection).

### Human Verification Required

None. All four ROADMAP success criteria are operationalized as programmatically checkable conditions (import succeeds, SimResult fields populated, capacitance monotonically decreasing, field finite and non-trivial, script exits 0) and were verified via live execution in this pass. No visual, UX, or external-service-dependent behavior in this phase's scope.

### Gaps Summary

No functional gaps. One documentation-traceability note: `LIB-01` remains unticked (`[ ]`) in `.planning/REQUIREMENTS.md` despite being fully implemented and verified working — Plan 01's completion commit did not include the `docs: mark LIB-01 complete` step that Plans 02/03 each performed for their requirements. This does not affect phase-goal achievement (the code satisfies LIB-01 per live verification above) and does not block progression to Phase 37; recommend a small doc-only follow-up ticking the box.

---

_Verified: 2026-07-02T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
