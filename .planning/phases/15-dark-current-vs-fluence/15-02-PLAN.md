---
phase: 15-dark-current-vs-fluence
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - notebooks/05_dark_current_vs_fluence.ipynb
autonomous: true
requirements: [DCRR-02]

must_haves:
  truths:
    - "User can see dark current vs fluence curve with baseline and radiation-induced contributions"
    - "User can see component decomposition (SRH, TAT, SRV) vs fluence"
    - "User can see delta-J (radiation-induced increase) clearly separated from pristine baseline"
    - "User can observe the counterintuitive SiC behavior where dark current may plateau or decrease at high fluence"
  artifacts:
    - path: "notebooks/05_dark_current_vs_fluence.ipynb"
      provides: "Publication-quality dark current vs fluence notebook"
      contains: "dark_current_vs_fluence"
  key_links:
    - from: "notebooks/05_dark_current_vs_fluence.ipynb"
      to: "src/dark_current.py"
      via: "dark_current_vs_fluence() and plot_dark_current_vs_fluence()"
      pattern: "dark_current_vs_fluence"
---

<objective>
Create a publication-quality notebook showing dark current evolution under proton irradiation with component decomposition, delta-J analysis, and the counterintuitive SiC high-fluence behavior.

Purpose: Deliver the user-facing dark current vs fluence visualization with scientific analysis (DCRR-02).
Output: notebooks/05_dark_current_vs_fluence.ipynb
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dark-current-vs-fluence/15-RESEARCH.md
@.planning/phases/15-dark-current-vs-fluence/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From src/dark_current.py:
```python
def dark_current_vs_fluence(
    fluence_range, V_bias=-30.0, area=0.04, epi_thickness_cm=10e-4,
    energy_MeV=62.0, lifetime_model="linear", damage_params=None,
    N_t=None, S_n=None, S_p=None,
) -> dict
    # Returns: fluences, I_total, I_SRH, I_TAT, I_SRV (arrays),
    #          I_baseline (float), delta_I (array), V_bias, energy_MeV, lifetime_model

def plot_dark_current_vs_fluence(result, ax=None, title=None) -> ax

````
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dark current vs fluence publication notebook</name>
  <files>notebooks/05_dark_current_vs_fluence.ipynb</files>
  <action>
Create a Jupyter notebook with the following sections. Follow the same style as existing notebooks (04_cce_vs_fluence.ipynb for structure reference).

**Cell 1: Title and introduction (Markdown)**
- "Dark Current vs Proton Fluence in 4H-SiC Detector"
- Explain additive delta-J model: J_dark(Phi) = J_dark(0) + delta_J(Phi)
- Note that J_dark(0) is the v1.1 calibrated pristine baseline
- Mention the counterintuitive SiC behavior at high fluence

**Cell 2: Imports and setup**
```python
import numpy as np
import matplotlib.pyplot as plt
from src.dark_current import dark_current_vs_fluence, plot_dark_current_vs_fluence
from src.radiation_damage import RadiationDamageParams
````

Set matplotlib rcParams for publication quality (matching existing notebooks).

**Cell 3: Dark current vs fluence sweep**
Run `dark_current_vs_fluence()` with:

- `fluence_range = np.concatenate([[0.0], np.geomspace(1e10, 5e13, 12)])`
- `V_bias = -30.0` (standard operating point)
- `area = 0.04` (4 mm^2 dosimetry detector)
- Print summary table: fluence, I_total (pA), delta_I (pA) for each point

**Cell 4: Main plot — total dark current vs fluence**
Use `plot_dark_current_vs_fluence(result)` for the standard decomposition plot.
Save as `figures/dark_current_vs_fluence.png` (dpi=150).

**Cell 5: Delta-J analysis plot**
Create a second plot showing only `delta_I` vs fluence (the radiation-induced increase above baseline):

- Log-log axes
- Show absolute delta_I values
- Annotate the baseline value in pA
- Title: "Radiation-Induced Dark Current Increase (Delta-J)"

**Cell 6: Multi-bias comparison (optional, if time permits)**
Run at V_bias = -10V, -30V, -50V (3 curves). Overlay on single plot.

- Shows how dark current depends on both fluence and operating voltage
- Use different colors per bias voltage, same component decomposition

**Cell 7: Discussion (Markdown)**

- Summarize key findings: how much dark current increases at moderate fluence (~1e12-1e13)
- Note the SiC-specific physics: dark current dominated by TAT effective generation, not thermal n_i^2
- Discuss the high-fluence behavior: carrier removal changes field/depletion geometry
- Compare with silicon (where dark current always increases monotonically)
- Note that exact pristine baseline is preserved by construction (Phi=0 uses calibrated device)

**Cell 8: Summary table**
Print a formatted summary: fluence, I_total, I_SRH, I_TAT, I_SRV, delta_I in a pandas DataFrame (or formatted print if pandas not available).

Use `plt.tight_layout()` before saving. Create `figures/` directory if it doesn't exist.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -c "import json; nb=json.load(open('notebooks/05_dark_current_vs_fluence.ipynb')); print(f'Cells: {len(nb[\"cells\"])}'); assert len(nb['cells']) >= 6, 'Need at least 6 cells'; print('OK')"</automated>
</verify>
<done>Publication notebook exists with at least 6 cells covering: introduction, sweep execution, component-decomposed plot, delta-J analysis, discussion, and summary. Figures saved to figures/ directory.</done>
</task>

</tasks>

<verification>
1. Notebook file exists: `notebooks/05_dark_current_vs_fluence.ipynb`
2. Notebook is valid JSON with >= 6 cells
3. Notebook imports `dark_current_vs_fluence` from `src.dark_current`
4. Figure file exists: `figures/dark_current_vs_fluence.png` (after notebook execution)
</verification>

<success_criteria>

- Publication-quality notebook showing dark current vs fluence with component decomposition
- Pristine baseline clearly marked as horizontal reference line
- Delta-J (radiation-induced increase) shown separately
- High-fluence behavior visible (plateau or decrease from carrier removal)
- Discussion explaining the SiC-specific physics vs silicon behavior
- Consistent area=0.04 cm^2 throughout
  </success_criteria>

<output>
After completion, create `.planning/phases/15-dark-current-vs-fluence/15-02-SUMMARY.md`
</output>
