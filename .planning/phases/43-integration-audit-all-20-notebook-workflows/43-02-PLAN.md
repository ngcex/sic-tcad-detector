---
phase: 43-integration-audit-all-20-notebook-workflows
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified: []
autonomous: false
requirements: [FEAT-05]

must_haves:
  truths:
    - "A tester has click-tested all 21 notebook workflows against the nearest Streamlit page"
    - "CCE/field workflows were click-tested at a converging bias (bias_V=-20.0), not the default -100.0"
    - "The documented FULL/PARTIAL/NONE coverage matrix from the audit doc is confirmed against the running app"
  artifacts: []
  key_links:
    - from: "human tester (browser)"
      to: ".planning/milestones/v5.0-MILESTONE-AUDIT.md"
      via: "confirms the coverage matrix produced in plan 43-01"
      pattern: "FEAT-05"
---

<objective>
Human-verify the notebook-to-UI coverage matrix produced in plan 43-01 by click-testing each of the 21 notebook workflows against the running Streamlit app. This CONFIRMS the (MEDIUM-confidence) FULL/PARTIAL classifications in the audit doc — it does not produce new documentation and closes no gaps.

Purpose: Success Criterion 1 of Phase 43 requires that a tester can reproduce each notebook's primary observable using only the UI. The coverage matrix was derived from source in research and written into the audit doc in plan 43-01; this plan is the browser confirmation step.

Output: A human sign-off (approved, or a list of discrepancies). No file is written by this plan — the complete audit doc already exists from 43-01. If the click-through reveals a discrepancy vs the documented matrix, that is a gap-closure exception handled separately, NOT an implementation task in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v5.0-MILESTONE-AUDIT.md
@.planning/phases/43-integration-audit-all-20-notebook-workflows/43-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Click-test all 21 notebook workflows against the Streamlit UI</name>
  <what-built>
    Plan 43-01 produced `.planning/milestones/v5.0-MILESTONE-AUDIT.md` containing a 21-row
    notebook-to-page coverage matrix (FULL=4, PARTIAL=10, NONE=7) and corrected REQUIREMENTS.md
    checkboxes. This checkpoint confirms that documented matrix against the actually-running app.
  </what-built>
  <action>
    Human-driven verification only — no code is written. Launch the running Streamlit app and
    click-test each of the 21 notebook workflows against its nearest page, confirming the
    FULL/PARTIAL/NONE coverage matrix documented in `.planning/milestones/v5.0-MILESTONE-AUDIT.md`.
    Use `bias_V = -20.0` for the CCE and Field Map pages (the default -100.0 hits the known
    `ramp_bias` non-convergence and is the documented upstream blocker, not a UI defect). Do NOT fix
    gaps, build pages, or touch source/physics code — see <how-to-verify> for the per-page steps.
  </action>
  <how-to-verify>
    1. Launch the app: `streamlit run app/main.py` (from the project root, with the editable
       `petringa` package installed; `uv sync --extra dev` first if imports fail).
    2. Open the coverage matrix in `.planning/milestones/v5.0-MILESTONE-AUDIT.md` (21 rows).
    3. For each FULL or PARTIAL row, navigate to the nearest page and confirm the notebook's primary
       observable renders:
       - C-V (notebook 02): C-V + Mott-Schottky plot + CSV download.
       - CCE (notebook 03) and Field Map (notebooks 01/15): USE `bias_V = -20.0` in the sidebar,
         NOT the default -100.0. The default deep bias hits the known `ramp_bias` non-convergence
         and shows a graceful `st.error` — that is the documented upstream blocker (UI-04/05), NOT a
         UI defect. At -20.0 the pages render correctly.
       - Radiation Damage (notebooks 09/10): CCE-vs-fluence curve + persistent kappa warning banner.
       - Dark Current (notebook 07): J_SRH/J_TAT/J_SRV decomposition vs T.
       - Microdosimetry (notebooks 17/18): upload an MC CSV → y·d(y) spectrum + y_F/y_D readouts.
       - Batch Sweep (notebooks 05b/06/13): pick a parameter + sim type → overlaid curves + bulk CSV.
    4. For each NONE row (notebooks 04, 08, 12, 14, 16, 19, 20), confirm there is indeed no page
       exposing that workflow — these are the documented v6.0 tech-debt gaps, expected to be absent.
    5. Confirm the FULL=4 / PARTIAL=10 / NONE=7 tally matches what you observe. If any cell differs
       from the documented matrix, note the specific notebook # and the discrepancy.

    Do NOT fix any gap, build any page, or touch physics/solver code during verification. This is
    read-and-confirm only.

  </how-to-verify>
  <acceptance_criteria>
    - Every FULL/PARTIAL workflow was click-tested and its primary observable confirmed to render
      (CCE/field at bias_V=-20.0).
    - Every NONE workflow confirmed to have no corresponding page.
    - The observed coverage tally matches the documented FULL=4 / PARTIAL=10 / NONE=7, OR any
      discrepancy is recorded per notebook # for follow-up.
  </acceptance_criteria>
  <verify>
    <human-check>Tester confirms every FULL/PARTIAL workflow's primary observable renders (CCE/field at bias_V=-20.0) and every NONE workflow has no page; observed tally matches FULL=4/PARTIAL=10/NONE=7 or discrepancies are recorded per notebook #.</human-check>
  </verify>
  <done>Human sign-off received (approved, or a recorded list of per-notebook discrepancies); the documented coverage matrix is confirmed against the running app.</done>
  <resume-signal>Type "approved" if the coverage matrix matches the running app, or list the notebook numbers whose observed coverage differs from the documented matrix.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                 |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| user→Streamlit CSV upload (pre-existing) | Microdosimetry page `st.file_uploader(type=["csv"])` → tempfile, from Phase 42. Exercised (not modified) during click-test. |

## STRIDE Threat Register

| Threat ID | Category  | Component                            | Disposition | Mitigation Plan                                                                                                                   |
| --------- | --------- | ------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-43-01   | Tampering | Microdosimetry CSV upload (Phase 42) | accept      | Read-only verification adds no new attack surface; upload restricted to CSV and unchanged by this phase. No ASVS control changes. |

No installs occur; supply-chain (T-43-SC) row intentionally omitted (zero installs).
</threat_model>

<verification>
- All 21 notebook workflows click-tested against the running app (CCE/field at bias_V=-20.0).
- Documented coverage matrix (FULL=4/PARTIAL=10/NONE=7) confirmed or discrepancies recorded.
- No page built, no gap closed, no source touched.
</verification>

<success_criteria>

- Human sign-off confirms the 21-workflow coverage matrix matches the running app, satisfying Phase 43 Success Criterion 1.
- Any discrepancy is recorded per notebook # (handled as a gap-closure exception, not implemented here).
  </success_criteria>

<output>
Create `.planning/phases/43-integration-audit-all-20-notebook-workflows/43-02-SUMMARY.md` when done, recording the click-through outcome (approved, or the list of discrepancies).
</output>
