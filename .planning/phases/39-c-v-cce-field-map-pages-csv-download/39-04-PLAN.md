---
phase: 39-c-v-cce-field-map-pages-csv-download
plan: 04
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - app/workflows/field_map.py
  - tests/test_app_field_page.py
autonomous: true
requirements: [UI-05, UI-06]
must_haves:
  truths:
    - "User clicks Run on the field map page and sees Plotly E-field vs depth AND potential vs depth, then can download a CSV"
    - "A 2D device config shows a 1D-only warning and does not run or crash (run_field returns empty arrays for 2D, so the guard must be a pre-check, not try/except)"
    - "The result survives a rerun (Download-button click) without re-solving devsim"
  artifacts:
    - path: "app/workflows/field_map.py"
      provides: "Field map page: 1D guard, Run→cache→render (E-field + potential)→download"
      contains: "petringa.run_field"
    - path: "tests/test_app_field_page.py"
      provides: "AppTest with mocked run_field: no-crash, cache, 2D guard"
      contains: "monkeypatch.setattr"
  key_links:
    - from: "app/workflows/field_map.py"
      to: "petringa.run_field"
      via: "petringa.run_field(cfg) inside the Run button handler"
      pattern: "petringa\\.run_field"
    - from: "app/workflows/field_map.py"
      to: "app.components.results"
      via: "build_field_figures / to_csv_bytes"
      pattern: "from app.components.results import"
---

<objective>
Turn the placeholder `app/workflows/field_map.py` into a working field-map page:
empty-state guard → 1D-only pre-check → "Run simulation" button → cache the
`SimResult` in `st.session_state["field_result"]` → render TWO Plotly charts
(E-field vs depth AND electrostatic potential vs depth) from the single cached
`run_field` result → `st.download_button` with pre-built CSV bytes. Add an
AppTest that mocks `petringa.run_field`.

Purpose: Delivers UI-05 (E-field + potential vs depth) and the download half of
UI-06 for this page. The 1D guard here is CRITICAL and subtly different: for a 2D
config `run_field` does NOT raise — it silently returns empty x/y arrays — so the
guard MUST be a pre-check, never a try/except (an exception handler catches
nothing). Both required charts come from ONE `run_field(cfg)` call (RESEARCH §6).
Output: field_map.py, test_app_field_page.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-RESEARCH.md
@.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-PATTERNS.md
@app/workflows/home.py
@app/workflows/field_map.py
@tests/test_app_pages.py

<interfaces>
<!-- Consumed from app/components/results.py (defined in plan 39-02): -->
  build_field_figures(result: SimResult) -> tuple[go.Figure, go.Figure]  # (efield_fig, potential_fig)
  to_csv_bytes(result: SimResult) -> bytes
<!-- Facade (reference as MODULE ATTRIBUTE for mockability — proven in 39-01): -->
  import petringa
  petringa.run_field(cfg, bias_V=-100.0) -> SimResult   # sim_type="field"
    # 1D (cfg.half_width_um is None): x = depth in µm (already *1e4), y = ElectricField V/cm,
    #   metadata["potential"] (V), metadata["net_doping"] (cm^-3), metadata["bias_V"]
    # 2D (cfg.half_width_um is not None): DOES NOT RAISE — returns x=np.array([]), y=np.array([])
<!-- session_state: device_config (input), field_result (output) -->
</interfaces>
</context>

<assumptions>
Adopted from RESEARCH.md (no CONTEXT.md — for user review):
- A5: 2D config → `st.warning` + `st.stop()` as a PRE-CHECK. Especially load-bearing
  here because run_field does NOT raise for 2D (returns empty arrays), so try/except
  would silently plot nothing (RESEARCH Pitfall 1).
- A4: bonus net-doping-vs-depth plot offered opt-in via an expander (metadata["net_doping"]).
- Pitfall 3: result.x is ALREADY µm — do NOT multiply by 1e4; ignore mesh (Phase 40).
- Facade referenced as `petringa.run_field` per proven 39-01 spike.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Field map page + AppTest — 1D pre-check, Run→cache→render(E-field + potential)→download</name>
  <files>app/workflows/field_map.py, tests/test_app_field_page.py</files>
  <read_first>
    - app/workflows/field_map.py (current placeholder — note the caption to REMOVE and that the geometry viewer explicitly arrives in Phase 40 on this same page)
    - app/workflows/home.py lines 1-6, 20-25 (docstring style + verbatim empty-state guard)
    - app/components/results.py (build_field_figures returns a 2-tuple; to_csv_bytes)
    - tests/test_app_pages.py lines 78-90 (AppTest.from_function wrapper with imports INSIDE the body)
    - tests/test_app_run_mockability.py (proven monkeypatch.setattr(petringa, "run_*", fake) seam from 39-01)
    - petringa/api/results.py (SimResult fields for the fake)
    - petringa/api/simulation.py lines 279-306 (run_field 1D: x=µm, metadata potential/net_doping) and 285-294 (2D returns empty arrays, does NOT raise)
    - .planning/phases/39-c-v-cce-field-map-pages-csv-download/39-RESEARCH.md §6 (both E-field AND potential from ONE call), Pitfall 1/3, and §Validation (AppTest 1.55 has NO plotly_chart / download_button accessor)
  </read_first>
  <behavior>
    - Empty session_state → st.info("Configure a device in the sidebar to begin.") + st.stop(); no exception.
    - cfg.half_width_um is not None (2D) → st.warning (1D-only message) + st.stop() BEFORE run_field; no "field_result"; no exception; nothing plotted.
    - 1D cfg + Run → petringa.run_field(cfg) cached under st.session_state["field_result"].
    - Cached result renders BOTH the E-field figure and the potential figure + download on rerun without re-solve.
  </behavior>
  <action>
    Write the AppTest FIRST (test_app_field_page.py), then the page (field_map.py) to make it pass —
    this keeps the verify command self-contained (no sibling task owns the test file).

    tests/test_app_field_page.py: `from streamlit.testing.v1 import AppTest`,
    `from petringa import DeviceConfig, SimResult`, `import petringa`, `import numpy as np`.
    Each test takes the `monkeypatch` fixture; set
    `monkeypatch.setattr(petringa, "run_field", fake_run_field)` BEFORE `at.run()` (module-attribute
    seam proven in 39-01). fake_run_field returns SimResult(config=cfg, sim_type="field",
    x=np.array([0.0, 1.0, 2.0]) (depth µm), y=np.array([1e5, 8e4, 5e4]) (V/cm),
    metadata={"bias_V": -100.0, "potential": np.array([0.0, -20.0, -45.0]),
    "net_doping": np.array([3e15, 2e15, 9e13])}, mesh=None). Use the from_function wrapper
    importing render inside its body. Three tests, asserting ONLY on `at.exception`,
    `at.session_state`, `at.button`, `at.warning`, `at.info` (NO plotly_chart/download_button accessor):
      1. test_run_caches_field_result: seed `at.session_state["device_config"] = DeviceConfig()` (1D),
         click the "Run simulation" button and rerun (verify the exact AppTest 1.55 button click+rerun
         API during execution), assert `at.exception == []`,
         `at.session_state["field_result"].sim_type == "field"`, and `len(at.session_state["field_result"].x) == 3`
         (proves the fake ran, not a real devsim solve).
      2. test_2d_config_warns_and_skips: seed `DeviceConfig(half_width_um=50.0)`, run, assert
         `at.exception == []`, `len(at.warning) >= 1`, and `"field_result" not in at.session_state`.
         This specifically guards the run_field-returns-empty-arrays trap: the pre-check must stop BEFORE the call.
      3. test_empty_state_guard: no device_config; assert `at.exception == []` and the guard info
         string is in `[el.value for el in at.info]`.

    Then rewrite app/workflows/field_map.py to pass. Replace the docstring with a home.py-style
    docstring documenting render()/no-side-effects + AppTest dual-use, and noting the 2D geometry
    viewer arrives in Phase 40 on this page. REMOVE the placeholder caption
    "Running this simulation is implemented in Phase 39." Imports:
    `from __future__ import annotations`, `import streamlit as st`, `import petringa`,
    `from app.components.results import build_field_figures, to_csv_bytes`.
    Inside render():
    1. st.title("Field Map").
    2. Empty-state guard VERBATIM (`st.info("Configure a device in the sidebar to begin.")` + st.stop()).
    3. 1D-only PRE-CHECK (NOT try/except — critical here because run_field returns empty arrays
       instead of raising for 2D): if `cfg.half_width_um is not None` →
       `st.warning("These workflows are 1D-only. 2D field visualization arrives in Phase 40 (geometry viewer). Set Dimensionality to 1D in the sidebar.")`
       then `st.stop()`.
    4. `if st.button("Run simulation"): st.session_state["field_result"] = petringa.run_field(cfg)`
       (MODULE ATTRIBUTE `petringa.run_field`).
    5. `result = st.session_state.get("field_result")`; `if result is not None:`
       `efield_fig, potential_fig = build_field_figures(result)`; render `st.plotly_chart(efield_fig)`
       and `st.plotly_chart(potential_fig)` (BOTH — the two required UI-05 charts come from this single
       result, RESEARCH §6), then
       `st.download_button("Download CSV", data=to_csv_bytes(result), file_name="field_result.csv", mime="text/csv")`.
    6. Optional bonus (A4): an `st.expander("Net doping vs depth")` plotting metadata["net_doping"]
       vs result.x — opt-in inside the expander.
    Use result.x directly for the depth axis (already µm — do NOT multiply by 1e4).
    Do NOT touch result.mesh (reserved for Phase 40; mesh.x_coords is cm, not µm).
  </action>
  <verify>
    <automated>uv run python -c "import app.workflows.field_map" && uv run pytest tests/test_app_field_page.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_app_field_page.py` contains `monkeypatch.setattr(petringa, "run_field"` set BEFORE `at.run()`; has the three named tests
    - The 2D test asserts `len(at.warning) >= 1` AND `"field_result" not in at.session_state` AND `at.exception == []`
    - Happy-path test asserts `at.session_state["field_result"].sim_type == "field"` and `len(...x) == 3` (fake ran, not devsim)
    - No test asserts on a plotly_chart or download_button accessor
    - `app/workflows/field_map.py` contains literal `petringa.run_field(cfg)` (module-attribute style), NOT `from petringa import run_field`
    - Contains `from app.components.results import` referencing build_field_figures and to_csv_bytes
    - Renders TWO `st.plotly_chart` calls (E-field AND potential) from the one cached result
    - Empty-state guard string and 2D warning string present verbatim; 2D case is a PRE-CHECK — run_field is NOT wrapped in try/except
    - `st.download_button` with `file_name="field_result.csv"` and `data=to_csv_bytes(result)`
    - No `* 1e4` / `*1e4` applied to result.x; no reference to `result.mesh` for plotting
    - Placeholder caption "Running this simulation is implemented in Phase 39." is GONE
    - `uv run pytest tests/test_app_field_page.py -x` exits 0
  </acceptance_criteria>
  <done>Field map page runs run_field on click, caches, renders BOTH E-field and potential Plotly charts from one result, offers CSV download without re-solving on rerun; its AppTest (happy path + 2D guard + empty-state) passes with a mocked facade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar widgets → run_field | Numeric config; a 2D config must be guarded before run_field silently returns empty arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Denial of Service | 2D config reaching run_field | mitigate | 1D-only PRE-CHECK (not try/except) before run_field — run_field returns empty x/y for 2D WITHOUT raising, so an exception handler would silently plot nothing; the pre-check + st.stop() is the only correct guard. Verified by test_2d_config_warns_and_skips. |
| T-39-01 | Tampering | CSV download bytes | accept | Serializer (39-02) uses numeric values + hardcoded headers only; no free-text cell. Risk LOW. |
</threat_model>

<verification>
- `uv run pytest tests/test_app_field_page.py -x` passes
- `uv run python -c "import app.workflows.field_map"` exits 0
- Existing suite still green: `uv run pytest tests/test_app_pages.py -x`
</verification>

<success_criteria>

- Field map page: Run→Plotly E-field vs depth AND potential vs depth (both from one call)→CSV download (UI-05, UI-06)
- 2D guard warns + skips as a pre-check, never silently plotting empty arrays
- Result survives download rerun without re-solving devsim
  </success_criteria>

<output>
Create `.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-04-SUMMARY.md` when done
</output>
