---
phase: 26
plan: 02
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - src/devsim_reset.py
  - src/optimization.py
  - src/poisson.py
  - tests/test_device2d.py
autonomous: true
requirements:
  - CONS-01
must_haves:
  truths:
    - "User can `from src.devsim_reset import reset_devsim_fully` and call it to delete all devices, clear the seven cylindrical-axis devsim globals (raxis_zero, raxis_variable, node_volume_model, edge_couple_model, element_edge_couple_model, element_node0_volume_model, element_node1_volume_model), save+restore the direct_solver and solver_callback, then call devsim.reset_devsim()"
    - "User can `from src.poisson import extract_depletion_width_2d_center` and pass a 2D device_info to obtain the depletion width measured along x ~= 0 (center column), reading y as the depth axis"
    - "User can run alt-structure-then-planar in sequence and the planar dark current matches the fresh-session reference within 0.1 % (cylindrical-leak regression canary, success criterion #3)"
    - "src/optimization.py no longer contains the inline reset_devsim() block; it imports reset_devsim_fully from src.devsim_reset"
  artifacts:
    - path: "src/devsim_reset.py"
      provides: "reset_devsim_fully(preserve_solver=True)"
      exports: ["reset_devsim_fully", "_CYLINDRICAL_GLOBALS"]
      min_lines: 50
    - path: "src/poisson.py"
      provides: "extract_depletion_width_2d_center(device_info) returning W in cm"
      contains: "def extract_depletion_width_2d_center"
    - path: "src/optimization.py"
      provides: "Uses reset_devsim_fully instead of inline devsim.reset_devsim() block"
      contains: "from src.devsim_reset import reset_devsim_fully"
    - path: "tests/test_device2d.py"
      provides: "TestResetStateLeak and TestGradedDopingSmoothness with real assertions (xfail removed)"
      contains: "test_reset_after_alt_structures"
  key_links:
    - from: "src/optimization.py"
      to: "src/devsim_reset.py"
      via: "import reset_devsim_fully"
      pattern: "from src\\.devsim_reset import reset_devsim_fully"
    - from: "tests/test_device2d.py::TestResetStateLeak"
      to: "src/devsim_reset.py::reset_devsim_fully"
      via: "import and call between alt-structures and planar"
      pattern: "reset_devsim_fully\\(\\)"
    - from: "tests/test_device2d.py::TestCalibrationCV"
      to: "src/poisson.py::extract_depletion_width_2d_center"
      via: "called on 2D device_info"
      pattern: "extract_depletion_width_2d_center"
---

<objective>
Land the two infrastructure pieces that Plan 03 (calibration) cannot proceed without:

1. A single-responsibility `src/devsim_reset.py` module exposing `reset_devsim_fully()` that nukes ALL devsim global state — devices + cylindrical-axis parameters + assembly-model names — replacing the inline `devsim.reset_devsim()` block in `src/optimization.py:153`. PITFALLS P03/P30 (HIGH severity) document the exact globals that leak from `src/alternative_structures.py:579-598`; this module closes that leak.

2. A 2D-aware depletion-width extractor `extract_depletion_width_2d_center(device_info)` in `src/poisson.py`. Research assumption A6 ("`cv_sweep` works on 2D unchanged") is FALSE: verified by inspection of `src/poisson.py:362-413`, the existing `extract_depletion_width_numerical` reads `x` as the depth axis (1D convention), but 2D modules use `y` as depth and `x` as lateral. The new function filters nodes to `x < 1e-6` (center column) and applies the same junction-side depletion-edge logic along the `y` axis.

Purpose: Plan 03's calibration loop needs both pieces — `reset_devsim_fully` between calibration trials (~50 device builds) and `extract_depletion_width_2d_center` to compute the cost function (W at center column vs the 1D-twin C-V targets). Plan 04's cylindrical-leak regression also depends on `reset_devsim_fully`.

Output: New module `src/devsim_reset.py`, new function in `src/poisson.py`, `src/optimization.py` refactored to import from `src/devsim_reset`, and two previously-xfail tests (`test_reset_after_alt_structures`, `test_graded_doping_smoothness_no_kinks`) now passing with real assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-graded-doping-2d-calibration/26-RESEARCH.md
@.planning/phases/26-graded-doping-2d-calibration/26-01-SUMMARY.md
@.planning/research/PITFALLS.md
@src/optimization.py
@src/alternative_structures.py
@src/poisson.py
@src/device2d.py
@src/charge_collection_2d.py
@tests/test_device2d.py
@tests/test_alternative_structures.py

<interfaces>
Concrete signatures the executor MUST replicate or consume.

From src/alternative_structures.py lines 573-598 — the EXACT globals to clear (verified by Read). Function `_activate_cylindrical_coords` calls:

- `devsim.set_parameter(name="raxis_zero", value=0.0)`
- `devsim.set_parameter(name="raxis_variable", value="x")`
- `devsim.set_parameter(name="node_volume_model", value="CylindricalNodeVolume")`
- `devsim.set_parameter(name="edge_couple_model", value="CylindricalEdgeCouple")`
- `devsim.set_parameter(name="element_edge_couple_model", value="ElementCylindricalEdgeCouple")`
- `devsim.set_parameter(name="element_node0_volume_model", value="ElementCylindricalNodeVolume@en0")`
- `devsim.set_parameter(name="element_node1_volume_model", value="ElementCylindricalNodeVolume@en1")`

From src/alternative_structures.py lines 601-620 — Cartesian defaults (values to restore). Function `restore_cartesian_coords` calls:

- `devsim.set_parameter(name="node_volume_model", value="NodeVolume")`
- `devsim.set_parameter(name="edge_couple_model", value="EdgeCouple")`
- `devsim.set_parameter(name="element_edge_couple_model", value="ElementEdgeCouple")`
- `devsim.set_parameter(name="element_node0_volume_model", value="ElementNodeVolume@en0")`
- `devsim.set_parameter(name="element_node1_volume_model", value="ElementNodeVolume@en1")`

From src/optimization.py lines 96-158 — the EXACT existing reset block to replace: lines 96-104 save direct_solver and solver_callback; line 153 is the bare `devsim.reset_devsim()` call inside the loop's `except` handler; lines 154-158 restore the solver parameters.

From src/poisson.py lines 334-417 — the 1D extractor whose logic is mirrored. It reads `x` as the depth axis (1D convention) and returns wrong W for 2D devices.

From src/charge_collection_2d.py — already correct: x is lateral, y is depth in 2D. The new extractor filters `abs(x) < 1e-6` (center column, since x=0 is symmetry plane) and treats `y` as the depth axis.

From src/alternative_structures.py — existing 3D-electrode constructor signature for the reset test:

```
create_3d_electrode_device(
    device_name="elec3d",
    region_name="sic",
    outer_radius_um=50.0,
    column_radius_um=5.0,
    epi_thickness_cm=10e-4,
    substrate_thickness_cm=1e-4,
    N_A=1e19,
    T=300,
    doping_profile="graded",
    N_D_junction=None,
    N_D_bulk=None,
    L_transition=None,
)
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/devsim_reset.py and refactor src/optimization.py to use it</name>
  <files>src/devsim_reset.py, src/optimization.py</files>
  <read_first>
    - src/devsim_reset.py (verify file does NOT yet exist — `ls src/devsim_reset.py` should fail)
    - src/optimization.py (full file, particularly lines 80-170): see the EXACT inline reset block being replaced
    - src/alternative_structures.py lines 568-625: source of all seven cylindrical globals + the Cartesian-restore values
    - .planning/research/PITFALLS.md sections P03, P20, P30
    - tests/test_optimization.py (full file): existing tests must still pass after refactor
  </read_first>
  <behavior>
    - Test 1: `from src.devsim_reset import reset_devsim_fully, _CYLINDRICAL_GLOBALS` succeeds
    - Test 2: `_CYLINDRICAL_GLOBALS` is a tuple containing exactly these 7 strings: "raxis_zero", "raxis_variable", "node_volume_model", "edge_couple_model", "element_edge_couple_model", "element_node0_volume_model", "element_node1_volume_model"
    - Test 3: After calling `reset_devsim_fully()` in a session that previously activated cylindrical coords, `devsim.get_parameter(name="node_volume_model")` returns the Cartesian default "NodeVolume" (NOT "CylindricalNodeVolume")
    - Test 4: After calling `reset_devsim_fully()`, `devsim.get_device_list()` returns an empty list
    - Test 5: With `preserve_solver=True` (default), the `direct_solver` parameter is preserved across the reset
    - Test 6: After refactor, `src/optimization.py` no longer contains the inline `devsim.reset_devsim()` call; it imports `reset_devsim_fully` and calls it once
    - Test 7: `uv run pytest tests/test_optimization.py -x` exits 0 (refactor is behavior-preserving)
  </behavior>
  <action>
**Step A — Create src/devsim_reset.py** as a brand-new module. The required content is:

1. Module docstring referencing PITFALLS P03/P30, naming `src/alternative_structures.py:573-598` as the leak source, and listing the consumers (src/optimization.py, the regression canary, Phase 31 tensor-mobility extension, the Phase 26 calibration loop).

2. Imports: `import logging` and `import devsim`; module-level `logger = logging.getLogger(__name__)`.

3. Module-level constant `_CYLINDRICAL_GLOBALS` defined as a tuple of exactly seven string literals, in this order: "raxis_zero", "raxis_variable", "node_volume_model", "edge_couple_model", "element_edge_couple_model", "element_node0_volume_model", "element_node1_volume_model".

4. Module-level constant `_CARTESIAN_DEFAULTS` defined as a dict mapping the five model-name globals to their Cartesian defaults (per src/alternative_structures.py:601-620): "node_volume_model" -> "NodeVolume", "edge_couple_model" -> "EdgeCouple", "element_edge_couple_model" -> "ElementEdgeCouple", "element_node0_volume_model" -> "ElementNodeVolume@en0", "element_node1_volume_model" -> "ElementNodeVolume@en1". (raxis_zero and raxis_variable have no Cartesian counterpart — they are cleared to the empty string.)

5. Public function `reset_devsim_fully(preserve_solver=True)` with full docstring describing the five-step workflow. Steps:
   - **Step 1** (only if preserve_solver=True): snapshot `direct_solver` and `solver_callback` parameters. Wrap each `devsim.get_parameter(name=...)` in try/except (devsim.error OR generic Exception) — on exception, set the saved variable to None.

   - **Step 2**: enumerate devices via `list(devsim.get_device_list())` (wrap in try/except, default to empty list). Loop and call `devsim.delete_device(device=dev)` for each, wrapped in try/except logging the failure at debug level. This is PITFALLS P20 — never hardcode device names.

   - **Step 3**: for each name in `_CYLINDRICAL_GLOBALS`: if name is a key in `_CARTESIAN_DEFAULTS`, call `devsim.set_parameter(name=name, value=_CARTESIAN_DEFAULTS[name])`; else call `devsim.set_parameter(name=name, value="")`. Each call wrapped in try/except (silently swallow).

   - **Step 4**: call `devsim.reset_devsim()` wrapped in try/except (log warning on exception).

   - **Step 5** (only if preserve_solver=True): if `saved_solver is not None`, call `devsim.set_parameter(name="direct_solver", value=saved_solver)`. If `saved_callback is not None`, call `devsim.set_parameter(name="solver_callback", value=saved_callback)`. Each wrapped in try/except.

6. The function must be idempotent — safe to call when no devices and no cylindrical state exist.

**Step B — Refactor src/optimization.py to use the new module.**

1. Add to the imports at the top of the file: `from src.devsim_reset import reset_devsim_fully`. Put it near the other src.\* imports, not mid-function.

2. Locate lines 96-104 (the `try: _saved_solver = devsim.get_parameter(name="direct_solver") ...` save block before the loop) and DELETE those lines and the corresponding usage of `_saved_solver` / `_saved_callback` elsewhere in the function — they are now encapsulated inside `reset_devsim_fully`.

3. Locate lines 148-158 (inside the loop's exception handler, the inline `try: devsim.reset_devsim() ... except Exception: pass` block) and REPLACE the entire block with a single line: `reset_devsim_fully(preserve_solver=True)`. Preserve the surrounding indentation.

4. After the edit, confirm `grep "devsim.reset_devsim" src/optimization.py` returns no matches and `grep "from src.devsim_reset import reset_devsim_fully" src/optimization.py` returns exactly one match.

5. Run `uv run pytest tests/test_optimization.py -x --tb=short`. All tests must pass. If anything fails, the refactor changed behavior — fix before declaring done.

**Step C — Sanity check the import path.** From the repo root, run `python -c "from src.devsim_reset import reset_devsim_fully, _CYLINDRICAL_GLOBALS; print(len(_CYLINDRICAL_GLOBALS))"` and confirm it prints exactly `7`.

DO NOT modify src/alternative_structures.py — the existing `_activate_cylindrical_coords` / `restore_cartesian_coords` helpers remain; the new module is the canonical reset for cross-module callers.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && test -f src/devsim_reset.py && python -c "from src.devsim_reset import reset_devsim_fully, \_CYLINDRICAL_GLOBALS; assert len(\_CYLINDRICAL_GLOBALS)==7; assert 'raxis_zero' in \_CYLINDRICAL_GLOBALS; assert 'node_volume_model' in \_CYLINDRICAL_GLOBALS; assert 'element_node1_volume_model' in \_CYLINDRICAL_GLOBALS; print('OK')" && ! grep "devsim.reset_devsim()" src/optimization.py && grep -q "from src.devsim_reset import reset_devsim_fully" src/optimization.py && uv run pytest tests/test_optimization.py -x --tb=short</automated>
</verify>
<acceptance_criteria> - `test -f src/devsim_reset.py` succeeds - `grep -c "def reset_devsim_fully" src/devsim_reset.py` returns 1 - `grep -c "_CYLINDRICAL_GLOBALS = (" src/devsim_reset.py` returns 1 - `grep -c '"raxis_zero"' src/devsim_reset.py` returns at least 1 - `grep -c '"raxis_variable"' src/devsim_reset.py` returns at least 1 - `grep -c '"node_volume_model"' src/devsim_reset.py` returns at least 1 - `grep -c '"edge_couple_model"' src/devsim_reset.py` returns at least 1 - `grep -c '"element_edge_couple_model"' src/devsim_reset.py` returns at least 1 - `grep -c '"element_node0_volume_model"' src/devsim_reset.py` returns at least 1 - `grep -c '"element_node1_volume_model"' src/devsim_reset.py` returns at least 1 - `grep -c "from src.devsim_reset import reset_devsim_fully" src/optimization.py` returns 1 - `grep -c "devsim.reset_devsim()" src/optimization.py` returns 0 (refactor removed all inline calls) - `uv run pytest tests/test_optimization.py -x --tb=short` exits 0 - `python -c "from src.devsim_reset import reset_devsim_fully; reset_devsim_fully()"` exits 0 (idempotent on empty state)
</acceptance_criteria>
<done>src/devsim_reset.py exists with reset_devsim_fully() that clears all seven cylindrical-axis globals and restores Cartesian defaults; src/optimization.py imports it and no longer contains inline devsim.reset_devsim() calls; existing optimization tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add extract_depletion_width_2d_center to src/poisson.py and wire smoothness test</name>
  <files>src/poisson.py, tests/test_device2d.py</files>
  <read_first>
    - src/poisson.py lines 334-417: the 1D extractor whose logic is mirrored (note it reads `x` as depth)
    - src/device2d.py lines 1-30: confirms the coordinate convention `x = lateral, y = depth`
    - src/device2d.py lines 215-220: confirms `air_buffer = 1e-8 cm` and the `junction_pos = substrate_thickness_cm` convention
    - src/charge_collection_2d.py lines 67-113: shows the canonical pattern for extracting per-node fields by name from a 2D device
    - tests/test_device2d.py (full file): existing test style + the TestGradedDopingSmoothness skeleton from Plan 01 Task 3
  </read_first>
  <behavior>
    - Test 1: `from src.poisson import extract_depletion_width_2d_center` succeeds
    - Test 2: Called on a 2D device built at 0 V with the v3.0 graded profile and 100 µm SV, returns a W within +/- 30 % of 1.7e-4 cm (the 1D-twin equilibrium value from src/device.py:519). The 30% band is loose because the unrefitted v3.0 profile in 2D is the very thing Plan 03 will tighten.
    - Test 3: Called on a 2D device built at V_bias=5 V (mild reverse bias), returns W greater than the 0 V value (depletion expands under reverse bias)
    - Test 4: Raises `ValueError` when called on a 1D device_info (dimension != 2)
    - Test 5: The previously-xfail `test_graded_doping_smoothness_no_kinks` in `tests/test_device2d.py` now passes (xfail decorator removed; real assertion added against Donors at center column)
  </behavior>
  <action>
**Step A — APPEND `extract_depletion_width_2d_center` to src/poisson.py** immediately AFTER the existing `extract_depletion_width_numerical` function (after the end of its body at approximately line 417). The function spec:

1. Signature: `def extract_depletion_width_2d_center(device_info, center_x_tol_cm=1e-6):`

2. Docstring: explain that the 1D `extract_depletion_width_numerical` reads `x` as depth (1D convention) and cannot be reused for 2D, where x is lateral and y is depth. Document the two parameters and that it returns the W in cm measured from the metallurgical junction (y = junction_pos) into the n-epi (y > junction_pos). State the function raises `ValueError` if `device_info["dimension"]` is not 2 or if no nodes are within `center_x_tol_cm` of the symmetry plane.

3. Implementation:
   - Validate `device_info.get("dimension") == 2`; if not, raise `ValueError` with a message including the actual dimension value.
   - Read `device = device_info["device_name"]`, `region = device_info["region_name"]`, `junction_pos = device_info["junction_pos"]`, `epi_thickness = device_info["epi_thickness_cm"]`.
   - Use `numpy.array(devsim.get_node_model_values(device=device, region=region, name="x"))` to read x. Same for `y`, `Donors`. For electrons, try `name="Electrons"` first, fall back to `name="IntrinsicElectrons"` if that raises (mirrors the 1D extractor's logic for both Poisson-only and DD-solved devices).
   - Filter nodes: `center_mask = numpy.abs(x_nodes) < center_x_tol_cm`. If `not numpy.any(center_mask)`, raise `ValueError` reporting `min |x|`.
   - Sort the masked arrays by y (ascending).
   - Filter to n-side: `n_mask = y_c >= junction_pos`. If empty, return 0.0.
   - Compute `ratio = numpy.where(donors_n > 0, n_n / donors_n, 0.0)`.
   - Find first index where `ratio >= 0.5`. If no such index, return `float(epi_thickness)`.
   - If idx > 0, linearly interpolate the depletion edge between y[idx-1] and y[idx] using (0.5 - r0) / (r1 - r0). If idx == 0 or r1 == r0, use `y_n[idx]`.
   - `W = float(edge - junction_pos)`; clamp to `[0.0, epi_thickness]`; return.

4. The function uses the existing module-level `numpy as np` and `devsim` imports — do NOT add new imports.

**Step B — Replace the TestGradedDopingSmoothness body in tests/test_device2d.py** (added by Plan 01 Task 3 as xfail). Remove the `@pytest.mark.xfail(...)` decorator from `test_graded_doping_smoothness_no_kinks` and replace the `raise NotImplementedError(...)` body with a real test. The replacement test must:

1. Import `numpy as np`, `from src.charge_collection_2d import create_2d_dd_device`, and `from src.poisson import extract_depletion_width_2d_center`.

2. Build a 2D device via `create_2d_dd_device(device_name=_unique_name(), half_width_um=50.0, V_bias=0.0, doping_profile="graded")`.

3. Call `W0 = extract_depletion_width_2d_center(dev)` and assert `W0 > 0.0` and `W0 < dev["epi_thickness_cm"]`.

4. Read x, y, and Donors from the device via `devsim.get_node_model_values(...)`. Filter to center column (`np.abs(x) < 1e-6`), sort by y, and select nodes in the n-epi (`y_c > dev["junction_pos"]` and `d_c > 0`).

5. Assert there are at least 4 nodes in the filtered array.

6. Compute `rel_jumps = np.abs(np.diff(d_in)) / d_in[:-1]` and assert `np.max(rel_jumps) < 0.5` — graded donor profile must have no >50% relative jump between adjacent center-column nodes (PITFALLS P27 verification).

7. Keep the `@pytest.mark.slow` decorator? — NO. This is a single-device build at 0 V, ~5 seconds total. Leave it as a regular (fast) test.

DO NOT touch the other three new test classes (`TestReverseBiasConvergence`, `TestCalibrationCV`, `TestResetStateLeak`) in this task — Task 3 wires `TestResetStateLeak`; Plan 03 wires the other two.

After the edit, run `uv run pytest tests/test_device2d.py::TestGradedDopingSmoothness -x --tb=short` and confirm it passes. Also run `uv run pytest tests/test_device2d.py::TestDevice2DCreation tests/test_device2d.py::TestDoping2D -x` to confirm no regression on existing classes.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -c "from src.poisson import extract_depletion_width_2d_center; print('OK')" && uv run pytest tests/test_device2d.py::TestDevice2DCreation tests/test_device2d.py::TestDoping2D tests/test_device2d.py::TestGradedDopingSmoothness -x --tb=short</automated>
</verify>
<acceptance_criteria> - `grep -c "def extract_depletion_width_2d_center" src/poisson.py` returns 1 - `grep -c 'device_info.get("dimension") != 2' src/poisson.py` returns at least 1 (or equivalent dim check) - `grep -c "center_x_tol_cm" src/poisson.py` returns at least 2 (parameter + usage) - `python -c "from src.poisson import extract_depletion_width_2d_center; print(extract_depletion_width_2d_center.__doc__[:50])"` prints a non-empty docstring snippet - `grep -c "def test_graded_doping_smoothness_no_kinks" tests/test_device2d.py` returns 1 - The TestGradedDopingSmoothness class block in tests/test_device2d.py does NOT contain `@pytest.mark.xfail` — verify with: `awk '/class TestGradedDopingSmoothness/,/^class /' tests/test_device2d.py | grep -c "@pytest.mark.xfail"` returns 0 - `awk '/class TestGradedDopingSmoothness/,/^class /' tests/test_device2d.py | grep -c "raise NotImplementedError"` returns 0 - `awk '/class TestGradedDopingSmoothness/,/^class /' tests/test_device2d.py | grep -c "extract_depletion_width_2d_center"` returns at least 1 - `uv run pytest tests/test_device2d.py::TestGradedDopingSmoothness -x --tb=short` exits 0 - `uv run pytest tests/test_device2d.py::TestDevice2DCreation tests/test_device2d.py::TestDoping2D -x` exits 0 (no regression on existing tests)
</acceptance_criteria>
<done>src/poisson.py exports extract_depletion_width_2d_center which correctly returns center-column W on 2D devices (y as depth axis); TestGradedDopingSmoothness no longer xfail and passes against the v3.0 graded profile; existing tests in tests/test_device2d.py continue to pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire TestResetStateLeak with real cylindrical-leak regression canary</name>
  <files>tests/test_device2d.py</files>
  <read_first>
    - tests/test_device2d.py (full file): locate the TestResetStateLeak skeleton from Plan 01 Task 3
    - src/devsim_reset.py (created by Task 1 of this plan)
    - src/alternative_structures.py lines 568-650: signature of `create_3d_electrode_device` (uses cylindrical coords)
    - src/charge_collection_2d.py lines 116-165: `create_2d_dd_device` signature for the planar reference
    - tests/test_alternative_structures.py (full file): existing patterns for testing cylindrical devices
  </read_first>
  <behavior>
    - Test 1: The previously-xfail `test_reset_after_alt_structures` in tests/test_device2d.py now passes (xfail decorator removed; real assertion added)
    - Test 2: Without `reset_devsim_fully` between the cylindrical and planar builds, the planar dark current would be wrong; with it, the planar dark current matches the fresh-session reference within 0.1 %
    - Test 3: Test is marked `@pytest.mark.slow` (it builds three devsim devices and runs short solves; total runtime ~30-60 s)
  </behavior>
  <action>
**REPLACE the body of the `TestResetStateLeak` class in tests/test_device2d.py.** Remove the `@pytest.mark.xfail(...)` decorator from `test_reset_after_alt_structures` and replace the `raise NotImplementedError(...)` body with a real regression canary. The replacement must:

1. Be decorated `@pytest.mark.slow` (single decorator on the test method).

2. Import inside the test (deferred imports — keep collection cheap): `from src.devsim_reset import reset_devsim_fully`, `from src.charge_collection_2d import create_2d_dd_device`, `from src.alternative_structures import create_3d_electrode_device`.

3. Define a small helper `_dark_current_at_cathode(device_info)` inside the test body (or as a module-private helper above the class) that:
   - Reads the contact current at the cathode of a DD-initialized device via `devsim.get_contact_current(device=device_info["device_name"], contact="cathode", equation="ElectronContinuityEquation")` (combine with `HoleContinuityEquation` for total — use the same pattern as src/charge_collection_2d.py if it has a helper, else use the two-equation sum). The test only needs a numerically reproducible scalar; absolute physical correctness is not asserted here.

4. Workflow (the canary):
   - **Stage 1 (reference)**: `reset_devsim_fully()`. Build a planar 2D device via `create_2d_dd_device(device_name=_unique_name(), half_width_um=50.0, V_bias=10.0, doping_profile="graded")`. Capture `I_ref = _dark_current_at_cathode(dev_ref)`. `reset_devsim_fully()`.

   - **Stage 2 (contaminate)**: Build a 3D-electrode cylindrical device via `create_3d_electrode_device(device_name=_unique_name(), outer_radius_um=50.0, column_radius_um=5.0)`. This call internally invokes `_activate_cylindrical_coords` which sets the seven globals. Do not need to solve — just creating the device is enough to set the globals. `reset_devsim_fully()`.

   - **Stage 3 (verify reset cleared the leak)**: Confirm `devsim.get_parameter(name="node_volume_model")` returns `"NodeVolume"` (the Cartesian default) — assert this directly. Then build another planar 2D device with the same parameters as Stage 1; capture `I_after = _dark_current_at_cathode(dev_after)`.

   - **Assertion**: `assert abs(I_after - I_ref) / abs(I_ref) < 1e-3`, with a helpful failure message reporting both values and the relative difference.

5. Wrap each stage in try/finally that calls `reset_devsim_fully()` even on failure, to avoid poisoning subsequent tests.

6. If `create_3d_electrode_device` is unavailable (e.g., import fails), `pytest.skip("3D electrode constructor not available")`. Do NOT silently mark it xfail.

After editing, run `uv run pytest tests/test_device2d.py::TestResetStateLeak -x --tb=short -m slow`. Confirm it passes. If the dark-current reading approach raises (e.g., no contact equation registered), fall back to reading `Donors` integrated over the mesh at the contact — the key is a NUMERICALLY reproducible value that would shift if cylindrical assembly weights leaked. Document the chosen scalar in a comment.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && uv run pytest tests/test_device2d.py::TestResetStateLeak -x --tb=short -m slow</automated>
</verify>
<acceptance_criteria> - `grep -c "def test_reset_after_alt_structures" tests/test_device2d.py` returns 1 - The TestResetStateLeak class in tests/test_device2d.py does NOT contain `@pytest.mark.xfail` — verify with: `awk '/class TestResetStateLeak/,/^class /' tests/test_device2d.py | grep -c "@pytest.mark.xfail"` returns 0 - `awk '/class TestResetStateLeak/,/^class /' tests/test_device2d.py | grep -c "raise NotImplementedError"` returns 0 - `awk '/class TestResetStateLeak/,/^class /' tests/test_device2d.py | grep -c "reset_devsim_fully"` returns at least 2 (Stage 1 reset, Stage 2 reset, Stage 3 verify) - `awk '/class TestResetStateLeak/,/^class /' tests/test_device2d.py | grep -c "create_3d_electrode_device"` returns at least 1 - `awk '/class TestResetStateLeak/,/^class /' tests/test_device2d.py | grep -c "node_volume_model" ` returns at least 1 (Stage 3 explicit check) - `awk '/class TestResetStateLeak/,/^class /' tests/test_device2d.py | grep -c "@pytest.mark.slow"` returns 1 - `uv run pytest tests/test_device2d.py::TestResetStateLeak -x --tb=short -m slow` exits 0 - `uv run pytest tests/test_device2d.py::TestDevice2DCreation -x` still exits 0 (no regression)
</acceptance_criteria>
<done>TestResetStateLeak::test_reset_after_alt_structures passes as a real regression canary: it builds a planar reference, contaminates devsim with a cylindrical 3D-electrode device, calls reset_devsim_fully, verifies node_volume_model is restored to "NodeVolume", builds a second planar device, and asserts the cathode current matches the reference within 0.1 %. CONS-01 success criterion #3 is now under automated test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

Pure scientific code with no network input, no PII, no authentication. No trust boundaries.

## STRIDE Threat Register

| Threat ID | Category       | Component                            | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | -------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-26-03   | Tampering      | src/devsim_reset.py global constants | accept      | Module is committed to git; the seven cylindrical globals are documented constants tied to devsim's API. Tampering caught by code review and the cylindrical-leak canary test. |
| T-26-04   | (other STRIDE) | n/a                                  | accept      | No trust boundary in scope.                                                                                                                                                    |

</threat_model>

<verification>
- `uv run pytest tests/test_optimization.py -x` exits 0 (Task 1 behavior preservation)
- `uv run pytest tests/test_device2d.py::TestGradedDopingSmoothness -x` exits 0 (Task 2 wired)
- `uv run pytest tests/test_device2d.py::TestResetStateLeak -x -m slow` exits 0 (Task 3 wired)
- `uv run pytest tests/test_device2d.py::TestDevice2DCreation tests/test_device2d.py::TestDoping2D -x` exits 0 (no regression on existing classes)
- `grep "devsim.reset_devsim()" src/optimization.py` returns no matches (refactor complete)
- `python -c "from src.devsim_reset import reset_devsim_fully; from src.poisson import extract_depletion_width_2d_center; print('OK')"` exits 0
</verification>

<success_criteria>

- src/devsim_reset.py exists and exports reset_devsim_fully + \_CYLINDRICAL_GLOBALS (tuple of 7 strings)
- src/poisson.py exports extract_depletion_width_2d_center which returns center-column W on 2D devices
- src/optimization.py no longer contains inline devsim.reset_devsim() — uses reset_devsim_fully
- TestGradedDopingSmoothness and TestResetStateLeak no longer xfail; both pass with real assertions
- All existing tests in tests/test_device2d.py and tests/test_optimization.py continue to pass
  </success_criteria>

<output>
After completion, create `.planning/phases/26-graded-doping-2d-calibration/26-02-SUMMARY.md` recording:
- The exact module path and signature of `reset_devsim_fully`
- The list of cylindrical globals it clears (verbatim from `_CYLINDRICAL_GLOBALS`)
- The signature of `extract_depletion_width_2d_center` and the assumed coordinate convention (x lateral, y depth, x ~= 0 is center)
- Which previously-xfail tests now pass (TestGradedDopingSmoothness, TestResetStateLeak) and which remain xfail for Plan 03 (TestReverseBiasConvergence, TestCalibrationCV)
- Pointer that Plan 03 consumes both helpers in the calibration loop
</output>
