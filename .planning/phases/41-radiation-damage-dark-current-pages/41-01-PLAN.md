---
phase: 41-radiation-damage-dark-current-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/results.py
  - tests/test_app_csv_export.py
  - .planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md
autonomous: true
requirements: [FEAT-01, FEAT-02]

must_haves:
  truths:
    - "A ParametricSweep over config.T at a fixed operating bias, calling petringa.run_dark_current per temperature with a minimal single-point bias request, returns a list[SimResult] without raising"
    - "build_damage_figure and build_dark_current_figure exist as pure Plotly builders in app/components/results.py, callable without a Streamlit runtime"
    - "to_csv_bytes handles sim_type='damage' and sim_type='dark_current' with the documented column schemas"
    - "test_unknown_sim_type_raises_value_error no longer uses sim_type='damage' as its unimplemented-type fixture"
  artifacts:
    - path: "app/components/results.py"
      provides: "build_damage_figure, build_dark_current_figure, to_csv_bytes damage/dark_current branches"
    - path: "tests/test_app_csv_export.py"
      provides: "damage + dark_current CSV column/header tests, fixed unknown-sim_type test"
    - path: ".planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md"
      provides: "confirmed safe widget defaults (V_bias for radiation damage, V_bias/T range for dark current) for Wave 2 page plans"
  key_links:
    - from: "app/components/results.py::to_csv_bytes"
      to: "SimResult.sim_type"
      via: "elif dispatch branch"
      pattern: "result\\.sim_type == \"(damage|dark_current)\""
    - from: "41-01-SPIKE-NOTES.md"
      to: "41-02-PLAN.md / 41-03-PLAN.md widget defaults"
      via: "confirmed convergent V_bias / T_min / T_max values"
      pattern: "V_bias|T_min|T_max"
---

<objective>
Establish the shared foundation both Phase 41 result pages depend on: (1) a live-devsim spike
that proves `petringa.ParametricSweep` sweeping `config.T` with `petringa.run_dark_current` as
`sim_fn` returns a valid `list[SimResult]` at safe widget defaults (per RESEARCH.md's Decision
Addendum — the dark current page sweeps TEMPERATURE, not bias), and (2) the two new pure Plotly
figure builders + `to_csv_bytes` CSV branches in `app/components/results.py` that both Wave 2
page plans (41-02 radiation damage, 41-03 dark current) will import and call.

Purpose: De-risk the single largest unverified assumption in RESEARCH.md (whether
`run_dark_current` tolerates `v_start == v_stop` / `n_points=1`, required for the per-temperature
ParametricSweep call) BEFORE two page plans are built around it, and give both Wave 2 plans a
single, already-tested `results.py` dependency instead of each duplicating figure-building code
(which would also create a same-wave file conflict).

Output: `app/components/results.py` extended with `build_damage_figure`,
`build_dark_current_figure`, and two new `to_csv_bytes` branches; `tests/test_app_csv_export.py`
extended and its Pitfall-4 regression fixed; a spike-notes file recording confirmed safe defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-RESEARCH.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-UI-SPEC.md
@app/components/results.py
@tests/test_app_csv_export.py
@petringa/api/simulation.py
@petringa/api/sweep.py
@petringa/api/results.py
</context>

<interfaces>
<!-- petringa.api.sweep.ParametricSweep — sweeps one DeviceConfig field, calling sim_fn per value -->
```python
@dataclass
class ParametricSweep:
    base_config: "DeviceConfig"
    param: str
    values: list
    sim_fn: Callable
    sim_kwargs: dict = field(default_factory=dict)

    def run(self) -> "list[SimResult]": ...

````

<!-- petringa.api.simulation.run_dark_current exact signature (petringa/api/simulation.py:665) -->
```python
def run_dark_current(
    config: DeviceConfig,
    v_start: float = 0.0,
    v_stop: float = -100.0,
    n_points: int = 20,
    N_t: "float | None" = None,
    S_n: "float | None" = None,
    S_p: "float | None" = None,
) -> SimResult:
    # Returns: sim_type="dark_current", x=voltages (V), y=I_total (A),
    # metadata={"I_SRH", "I_TAT", "I_SRV", "area_cm2"}.
    # Calls reset_devsim_fully() unconditionally at entry, owns full device
    # lifecycle (build -> try -> finally delete_device).
````

<!-- petringa.api.simulation.run_radiation_damage exact signature (petringa/api/simulation.py:447) -->

```python
def run_radiation_damage(
    config: DeviceConfig,
    fluences: "np.ndarray | None" = None,
    V_bias: float = -40.0,
    proton_energy_MeV: float = 5.6,
) -> SimResult:
    # Returns: sim_type="damage", x=fluences (cm^-2), y=CCE values,
    # metadata={"V_bias", "energy_MeV"}.
```

<!-- petringa.api.results.SimResult / MeshData -->

```python
@dataclass
class SimResult:
    config: "DeviceConfig"
    sim_type: str
    x: np.ndarray
    y: np.ndarray
    metadata: dict = field(default_factory=dict)
    mesh: "MeshData | None" = None
```

<!-- Existing app/components/results.py::to_csv_bytes dispatch shape (results.py:88-157) —
     new branches must follow this exact elif-chain + header-line convention. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Live-devsim spike — confirm ParametricSweep(param="T") + run_dark_current tolerates a minimal single-point bias request</name>
  <files>.planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md</files>
  <read_first>
    petringa/api/sweep.py, petringa/api/simulation.py (run_dark_current, run_radiation_damage),
    petringa/api/device.py (DeviceConfig defaults), .planning/phases/41-radiation-damage-dark-current-pages/41-RESEARCH.md
    (Pitfall 2, Decision Addendum), .planning/phases/41-radiation-damage-dark-current-pages/41-UI-SPEC.md
    (widget defaults table: radiation damage V_bias=-20.0; dark current T_min=250.0, T_max=400.0,
    n_temperatures=6, V_bias=-20.0)
  </read_first>
  <action>
    Using a throwaway Python script (run via `uv run python -c "..."` or a scratch file under
    /private/tmp — do NOT commit a scratch script into the repo), execute two live-devsim checks
    and record the results in the new file .planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md:

    Check A (dark current, temperature sweep): call
    `petringa.ParametricSweep(base_config=petringa.DeviceConfig(), param="T", values=[250.0, 325.0, 400.0], sim_fn=petringa.run_dark_current, sim_kwargs={"v_start": -20.0, "v_stop": -20.0, "n_points": 1}).run()`
    and confirm: (a) no exception raised, (b) returns a list of length 3, (c) each element is a
    SimResult with sim_type="dark_current" and len(result.x) >= 1, (d) each element's metadata
    contains I_SRH/I_TAT/I_SRV of the same length as result.x. If `v_start == v_stop` with
    `n_points=1` raises (e.g. from `np.linspace(v_start, v_stop, 1)` edge behavior or a devsim
    ramp_bias assumption that start != stop), retry with `n_points=2` and the same v_start/v_stop
    (this produces a 2-point degenerate sweep at the same voltage — acceptable, use `result.y[0]`
    downstream) and record whichever kwargs combination actually works.

    Check B (radiation damage, single call): call
    `petringa.run_radiation_damage(petringa.DeviceConfig(), V_bias=-20.0)` (facade defaults for
    fluences and proton_energy_MeV) and confirm: (a) no exception raised, (b) returns a 6-element
    result.y array, (c) record whether any element is NaN (per RESEARCH.md Pitfall 2, the facade
    default `V_bias=-40.0` produced one NaN at fluence~=3.98e13 — confirm whether the UI-SPEC's
    shallower default `V_bias=-20.0` avoids this or still produces a NaN).

    Record in 41-01-SPIKE-NOTES.md, as plain markdown: the exact kwargs that worked for Check A
    (confirm or amend the UI-SPEC's `n_points=1` assumption for the per-temperature
    `sim_kwargs`), and the NaN outcome for Check B. If either check reveals the UI-SPEC's stated
    default does NOT converge cleanly, state the corrected safe default in this file — Wave 2
    plans 41-02/41-03 must read this file and use its confirmed values instead of the UI-SPEC
    defaults if they differ.

  </action>
  <acceptance_criteria>
    - File .planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md exists
    - File states explicitly, in prose, whether ParametricSweep(param="T", sim_fn=run_dark_current)
      with a single-point bias request (v_start==v_stop) raised or succeeded, and the exact
      sim_kwargs (v_start, v_stop, n_points) that were confirmed to work without raising
    - File states explicitly whether run_radiation_damage(DeviceConfig(), V_bias=-20.0) returned
      any NaN in result.y, with the specific fluence value if so
    - No scratch/spike Python script is committed to the repository (git status shows only the
      markdown notes file and no stray .py file under app/, petringa/, or repo root)
  </acceptance_criteria>
  <done>Spike notes file committed with both Check A and Check B outcomes documented in prose, confirmed kwargs recorded for Wave 2 to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Extend app/components/results.py with build_damage_figure, build_dark_current_figure, and two new to_csv_bytes branches</name>
  <files>app/components/results.py</files>
  <read_first>
    app/components/results.py (current full file — existing build_cv_figure/build_cce_figure/
    build_field_figures/to_csv_bytes pattern to replicate exactly), .planning/phases/41-radiation-damage-dark-current-pages/41-UI-SPEC.md
    (Plot Contract section: exact trace colors, axis types, titles for both new figures),
    .planning/phases/41-radiation-damage-dark-current-pages/41-RESEARCH.md (Pitfall 3: abs() +
    zero-guard pattern for dark current decomposition, referencing
    petringa.core.dark_current.plot_dark_current_decomposition)
  </read_first>
  <action>
    Add `build_damage_figure(result: SimResult) -> go.Figure` to app/components/results.py,
    placed after the existing `build_cce_figure`. Single `go.Scatter(x=result.x, y=result.y,
    mode="lines+markers")` trace, color `#1F6FEB` (set via `line=dict(color="#1F6FEB")` on the
    Scatter). Add `fig.add_hline(y=1.0, line_dash="dash", line_color="#9AA0A6", opacity=0.5)`.
    Layout: title "CCE vs Proton Fluence", xaxis_title "Proton Fluence (p/cm²)",
    xaxis_type="log", yaxis_title "Charge Collection Efficiency", yaxis_range=[0, 1.1]. Do not
    call `.dropna()` or otherwise filter NaN — Plotly renders a NaN y-value as a line gap
    natively (per UI-SPEC Plot Contract "NaN tolerance" row).

    Add `build_dark_current_figure(result: SimResult) -> go.Figure` to app/components/results.py,
    placed after `build_damage_figure`. Per the UI-SPEC's Decision Addendum-aligned contract,
    this function's INPUT is a single AGGREGATED `SimResult` with `sim_type="dark_current"`,
    `x` = temperature array (K), `y` = I_total array (A), and
    `metadata={"I_SRH": array, "I_TAT": array, "I_SRV": array}` — one value per temperature. (The
    aggregation from `list[SimResult]` returned by ParametricSweep into this single-SimResult
    shape happens in the Wave 2 dark current PAGE code, not in this builder — this builder stays
    a pure single-SimResult-in / go.Figure-out function, consistent with every other builder in
    this module.) Build exactly 4 traces in this fixed order: ("Total", result.y, "#1A1A1A"),
    ("SRH (bulk)", result.metadata["I_SRH"], "#1F6FEB"), ("TAT (effective)",
    result.metadata["I_TAT"], "#D32F2F"), ("SRV (surface)", result.metadata["I_SRV"], "#2E7D32").
    For each: `I = np.abs(np.asarray(values))`; only add the trace via `fig.add_trace(go.Scatter(x=result.x,
    y=I, mode="lines+markers", name=label, line=dict(color=color)))` if `np.any(I > 0)`. Layout:
    title "Dark Current Decomposition vs Temperature", xaxis_title "Temperature (K)" (linear,
    do not set xaxis_type), yaxis_title "Absolute Dark Current (A)", yaxis_type="log".

    Extend `to_csv_bytes` with two new `elif` branches inserted after the existing `elif
    result.sim_type == "field":` branch and before the final `else: raise ValueError(...)`.
    Branch `elif result.sim_type == "damage":` builds
    `pd.DataFrame({"fluence_p_per_cm2": result.x, "CCE": result.y})`; `extra_header_lines =
    [f"# V_bias: {result.metadata['V_bias']}", f"# energy_MeV: {result.metadata['energy_MeV']}",
    "# WARNING: kappa (NIEL hardness factor) is a data-blocked placeholder; absolute Phi_crit
    numbers are unvalidated."]`. Branch `elif result.sim_type == "dark_current":` builds
    `pd.DataFrame({"T_K": result.x, "I_total_A": result.y, "I_SRH_A": result.metadata["I_SRH"],
    "I_TAT_A": result.metadata["I_TAT"], "I_SRV_A": result.metadata["I_SRV"]})` (column name
    `T_K`, NOT `bias_V` — the aggregated dark-current SimResult's x-axis is temperature per the
    Decision Addendum, not bias); `extra_header_lines = []`. Both branches follow the exact same
    `df` + `extra_header_lines` variable-naming convention already used by the `cv`/`cce`/`field`
    branches so the shared header-assembly code below the if/elif chain (device_fields,
    generated_at, header_lines, csv_text) needs no modification.

  </action>
  <acceptance_criteria>
    - `grep -n "def build_damage_figure" app/components/results.py` returns exactly one match
    - `grep -n "def build_dark_current_figure" app/components/results.py` returns exactly one match
    - `grep -n 'sim_type == "damage"' app/components/results.py` returns exactly one match inside `to_csv_bytes`
    - `grep -n 'sim_type == "dark_current"' app/components/results.py` returns exactly one match inside `to_csv_bytes`
    - `python -c "import ast; ast.parse(open('app/components/results.py').read())"` succeeds (no syntax errors)
    - `build_dark_current_figure` contains `np.abs` and `np.any(I > 0)` guard logic (grep -c 'np.abs' app/components/results.py shows at least 1 new occurrence beyond any pre-existing use)
  </acceptance_criteria>
  <done>Both new pure builders and both new to_csv_bytes branches exist in app/components/results.py, importable and callable without a Streamlit runtime or live devsim.</done>
</task>

<task type="auto">
  <name>Task 3: Add CSV export tests for damage/dark_current + fix the Pitfall-4 regression in test_unknown_sim_type_raises_value_error</name>
  <files>tests/test_app_csv_export.py</files>
  <read_first>
    tests/test_app_csv_export.py (current full file — test_cv_csv_columns_and_header /
    test_cce_csv_columns_and_header / test_field_csv_columns_and_header as the exact pattern to
    replicate; test_unknown_sim_type_raises_value_error as the test requiring the Pitfall-4 fix),
    app/components/results.py (after Task 2's edit — read the new damage/dark_current branches
    to confirm exact column names and header line content)
  </read_first>
  <action>
    Add `test_damage_csv_columns_and_header()`: build a `SimResult(config=DeviceConfig(),
    sim_type="damage", x=np.array([1e13, 1e14, 1e15]), y=np.array([0.99, 0.95, 0.80]),
    metadata={"V_bias": -20.0, "energy_MeV": 5.6})`. Decode via `_decode`. Assert
    `non_comment_lines[0] == "fluence_p_per_cm2,CCE"`. Assert a header line starting with `"#
    V_bias:"` exists and contains `-20.0`. Assert a header line starting with `"# WARNING:"`
    exists and contains the substring `"data-blocked"`. Parse with
    `pd.read_csv(StringIO(text), comment="#")` and assert `list(df.columns) ==
    ["fluence_p_per_cm2", "CCE"]` and `len(df) == 3`.

    Add `test_dark_current_csv_columns_and_header()`: build a `SimResult(config=DeviceConfig(),
    sim_type="dark_current", x=np.array([250.0, 325.0, 400.0]), y=np.array([1e-13, 5e-12,
    2e-10]), metadata={"I_SRH": np.array([1e-13, 5e-12, 2e-10]), "I_TAT": np.array([-1e-14,
    -3e-13, -5e-12]), "I_SRV": np.array([0.0, 0.0, 0.0])})`. Decode via `_decode`. Assert
    `non_comment_lines[0] == "T_K,I_total_A,I_SRH_A,I_TAT_A,I_SRV_A"`. Parse with
    `pd.read_csv(StringIO(text), comment="#")` and assert `list(df.columns) == ["T_K",
    "I_total_A", "I_SRH_A", "I_TAT_A", "I_SRV_A"]` and `len(df) == 3`.

    Fix `test_unknown_sim_type_raises_value_error`: change the fixture's `sim_type="damage"` to
    `sim_type="not_a_real_sim_type"` (a string that will never become a real `to_csv_bytes`
    branch in this or any future phase, avoiding the Phase 42 `"microdosimetry"` collision risk
    flagged in RESEARCH.md Pitfall 4). Leave the rest of the test body (the
    `pytest.raises(ValueError)` assertion) unchanged.

  </action>
  <acceptance_criteria>
    <automated>uv run pytest tests/test_app_csv_export.py -q</automated>
    - All 6 tests in tests/test_app_csv_export.py pass (3 existing cv/cce/field + 2 new damage/dark_current + 1 fixed unknown-type)
    - `grep -c 'sim_type="damage"' tests/test_app_csv_export.py` returns exactly 1 (only in the new test_damage_csv_columns_and_header, not in test_unknown_sim_type_raises_value_error)
    - `grep -c 'sim_type="not_a_real_sim_type"' tests/test_app_csv_export.py` returns exactly 1
  </acceptance_criteria>
  <done>tests/test_app_csv_export.py has 6 passing tests; the Pitfall-4 regression is fixed by using a genuinely-unimplemented sim_type string.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                                                             |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Streamlit widget input -> facade kwargs | Numeric widget values (constructed in Wave 2 pages, not this plan) flow into `run_dark_current`/`run_radiation_damage`/`ParametricSweep` kwargs                                         |
| SimResult metadata -> CSV bytes         | `to_csv_bytes` serializes arbitrary metadata dict values into a downloadable file; no external/untrusted input reaches this plan directly (all fixtures in this plan are test-authored) |

## STRIDE Threat Register

| Threat ID       | Category                  | Component                                                                                                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                          |
| --------------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-01         | Denial of Service (local) | Future Wave 2 `n_points`/`n_temperatures` widgets (not built in this plan, but the array-construction code this plan's builders will consume) | accept      | This plan's builders (`build_damage_figure`, `build_dark_current_figure`) are pure and operate on already-constructed arrays; they do not construct arrays from raw widget input themselves, so a degenerate n_points=0 upstream would surface as an empty-array rendering (Plotly handles empty traces without crashing), not a DoS in this plan's code |
| T-41-02         | Tampering                 | `to_csv_bytes` metadata dict access (`result.metadata["V_bias"]`, `result.metadata["I_SRH"]`, etc.)                                           | accept      | `SimResult` is constructed only by trusted in-process facade code (`petringa.api.simulation`) or trusted test fixtures in this phase; no external/user-uploaded data reaches `SimResult.metadata` in this plan (CSV upload arrives in Phase 42, out of scope here)                                                                                       |
| T-41-SC         | Tampering                 | npm/pip/cargo installs                                                                                                                        | accept      | No new package installs in this plan — all imports (`streamlit`, `plotly`, `pandas`, `numpy`, `petringa`) already declared in `pyproject.toml` and used by prior phases                                                                                                                                                                                  |
| </threat_model> |

<verification>
Run `uv run pytest tests/test_app_csv_export.py -q` and confirm all 6 tests pass (per-file
isolation convention, STATE.md Phase 35 finding). Confirm
`.planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md` exists and
documents both Check A and Check B outcomes in prose. Confirm no stray scratch `.py` file was
committed (`git status` clean apart from the two modified files + the new spike-notes file).
</verification>

<success_criteria>

- `app/components/results.py` has `build_damage_figure`, `build_dark_current_figure`, and
  `to_csv_bytes` branches for `"damage"` and `"dark_current"` sim_types
- `tests/test_app_csv_export.py` has 6 passing tests, with the Pitfall-4 regression fixed
- Spike notes file records confirmed-safe kwargs for the Wave 2 dark current page's
  `ParametricSweep(param="T", sim_fn=petringa.run_dark_current, sim_kwargs={...})` call and the
  radiation damage page's `V_bias` default
  </success_criteria>

<output>
Create `.planning/phases/41-radiation-damage-dark-current-pages/41-01-SUMMARY.md` when done
</output>
