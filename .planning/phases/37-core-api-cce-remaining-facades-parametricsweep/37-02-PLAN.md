---
phase: 37-core-api-cce-remaining-facades-parametricsweep
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - etna/api/simulation.py
  - etna/__init__.py
  - tests/test_api_facades.py
  - tests/test_api_microdosimetry.py
autonomous: true
requirements: [LIB-06]

must_haves:
  truths:
    - "from etna import run_radiation_damage, run_dark_current, run_temperature_sweep, run_flash_recombination, run_transient, run_microdosimetry all import without error"
    - "each of the 6 facades accepts a DeviceConfig as its first positional argument"
    - "bucket-a facades (run_radiation_damage, run_temperature_sweep, run_flash_recombination) do NOT call build_device/reset_devsim_fully/delete_device — the core fn self-cleans"
    - "run_dark_current owns the full reset->try->finally delete lifecycle (builds via create_dark_current_device)"
    - "run_transient wraps the self-cleaning transient_cce_vs_dose_rate and therefore adds NO facade-level device cleanup"
    - "run_microdosimetry is a pure data pipeline (bucket-c) — no devsim calls at all — producing a y*d(y) spectrum from data/synthetic_mc_events.csv"
    - "each facade returns a SimResult with the correct sim_type and the primary swept variable on x, CCE (or spectrum) on y"
  artifacts:
    - path: "etna/api/simulation.py"
      provides: "6 run_* facades appended"
      contains: "def run_microdosimetry"
    - path: "etna/__init__.py"
      provides: "6 facade re-exports"
      contains: "run_dark_current"
    - path: "tests/test_api_facades.py"
      provides: "fast import + signature contract tests for the 5 devsim facades"
      contains: "inspect.signature"
    - path: "tests/test_api_microdosimetry.py"
      provides: "fast data-pipeline test against data/synthetic_mc_events.csv"
      contains: "synthetic_mc_events.csv"
  key_links:
    - from: "etna/api/simulation.py::run_dark_current"
      to: "etna/core/dark_current.py::create_dark_current_device + dark_current_sweep"
      via: "bucket-b build+setup lifecycle (reset->try->finally delete)"
      pattern: "dark_current_sweep\\("
    - from: "etna/api/simulation.py::run_microdosimetry"
      to: "etna/core/microdosimetry.py::lineal_energy_spectrum"
      via: "load_mc_events_csv -> groupby event_id -> lineal_energy_spectrum (no devsim)"
      pattern: "lineal_energy_spectrum\\("
---

<objective>
Add the 6 remaining simulation facades to `etna/api/simulation.py`, classified into the three-bucket lifecycle from RESEARCH. Every facade uniformly forwards `DeviceConfig` fields (geometry, doping, area) into its core function — anchored on the D-01 decision established for `run_cce` in Plan 01 — so the whole API is self-consistent (`run_radiation_damage(DeviceConfig())` uses the same doping as `run_cce(DeviceConfig())`, not the core's older hardcoded calibration).

Purpose: LIB-06 — `from etna import run_radiation_damage, run_dark_current, run_temperature_sweep, run_flash_recombination, run_transient, run_microdosimetry` all import, and each accepts a `DeviceConfig` first arg. The acceptance bar is weak (import + signature), so 5 devsim facades get FAST contract tests and microdosimetry gets a FAST data-pipeline test.
Output: 6 facades appended to `simulation.py`, re-exported from `__init__.py`, and covered by `tests/test_api_facades.py` (contract) + `tests/test_api_microdosimetry.py` (data pipeline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-RESEARCH.md
@.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-PATTERNS.md
@.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-VALIDATION.md
@.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-01-SUMMARY.md

<interfaces>
<!-- All signatures + return keys verified from source. Use directly. -->

--- BUCKET-A (core builds+deletes own device; facade adds NO cleanup) ---

etna/core/charge_collection.py::cce_vs_fluence (charge_collection.py:597-608):
def cce_vs_fluence(fluence_range, V_bias=-40.0, epi_thickness_cm=10e-4,
N_D_junction=2.90e15, N_D_bulk=8.50e13, L_transition=1.0e-4,
alpha_range_cm=15e-4, generation_rate=1e18, energy_MeV=62.0,
lifetime_model="linear", damage_params=None) -> dict
Return keys: "fluences", "cce_values", "V_bias", "energy_MeV", "lifetime_model"
(builds a fresh device per fluence, self-cleaning)

etna/core/temperature_sweep.py::sweep_cce_vs_temperature (temperature_sweep.py:215-219):
def sweep_cce_vs_temperature(temperatures, voltages=None, method="hecht", **device_kwargs) -> pd.DataFrame
Return: pd.DataFrame long-format columns T, V, CCE (NOT a dict — Pitfall 3)
device_kwargs forwarded to create_dd_device (accepts N_D_junction, N_D_bulk, L_transition, epi_thickness_cm)

etna/core/flash_recombination.py::cce_vs_dose_rate (flash_recombination.py:263-272):
def cce_vs_dose_rate(dose_rates_Gy_s, V_bias=-30.0, epi_thickness_cm=10e-4, E_MeV=62,
n_continuation_steps=5, N_D_junction=2.90e15, N_D_bulk=8.50e13,
L_transition=1.0e-4) -> dict
Return keys include: "dose_rates", "cce_values", "cce_no_auger_ref", "V_bias", "epi_thickness_cm", "E_MeV"

--- BUCKET-B (facade owns full lifecycle: reset -> try -> finally delete) ---

etna/core/dark_current.py::create_dark_current_device (dark_current.py:538):
def create_dark_current_device(T=300, N_t=None, S_n=None, S_p=None, **kwargs) -> device_info
(internally: create_dd_device(T=T, **kwargs) + setup_tat_model + setup_surface_recombination;
**kwargs forwards geometry/doping to create_dd_device — do NOT use bare build_device())
etna/core/dark_current.py::dark_current_sweep (dark_current.py:435):
def dark_current_sweep(device_info, V_range, area=0.05, V_step=0.5) -> dict
Return keys: "voltages", "I_total", "I_SRH", "I_TAT", "I_SRV", "J_total", "J_SRH", "J_TAT", "J_SRV"
device_info dict has key "device_name" (used for delete_device in finally)

etna/core/transient.py::transient_cce_vs_dose_rate (transient.py:545):
def transient_cce_vs_dose_rate(V_bias=-30.0, dose_rates=None, t_rise=1e-6, t_duration=1e-3,
t_fall=1e-6, dt_min=1e-8, dt_max=1e-4, epi_thickness_cm=10e-4) -> pd.DataFrame
Return: pd.DataFrame columns ["dose_rate_Gy_s", "transient_cce"] (self-building, bucket-a shaped
internally — so run_transient does NOT need its own delete_device; it adds no build_device call)

--- BUCKET-C (no devsim) ---

etna/core/mc_coupling.py::load_mc_events_csv (mc_coupling.py:107):
def load_mc_events_csv(filepath, column_map=None, pos_unit="cm", energy_unit="keV") -> pd.DataFrame
Returns per-STEP DataFrame columns: event_id, x_cm, y_cm, z_cm, edep_keV
(multiple rows per event_id — see synthetic fixture)
etna/core/microdosimetry.py::mean_chord_length (microdosimetry.py:36):
def mean_chord_length(sv_thickness_um, sv_width_um=None, sv_depth_um=None) -> float (um)
etna/core/microdosimetry.py::lineal_energy_spectrum (microdosimetry.py:117):
def lineal_energy_spectrum(collected_energies_keV, l_bar_um, y_min=0.01, y_max=1e4, bins_per_decade=50) -> dict
Return keys: "bin_edges", "bin_centers", "bin_widths", "f_y", "d_y", "y_F", "y_D", "n_events", "y_values"

data/synthetic_mc_events.csv: header `event_id,x,y,z,edep` (NOTE: columns are x/y/z/edep, not x_cm/...;
load_mc_events_csv default column_map maps x->x_cm, edep->edep_keV). edep is in keV, positions in cm.

--- SHARED (from Plan 01 / run_cv analog) ---

etna/api/simulation.py::run_cv: lifecycle template (reset->build->try->finally delete->fallback reset)
at simulation.py:86-123; logger already defined at simulation.py:24; reset_devsim_fully + devsim already imported.
etna/api/results.py::SimResult(config, sim_type, x, y, metadata={}, mesh=None)
um->cm: epi_thickness_cm = config.epi_thickness_um * 1e-4 ; L_transition = config.L_transition_um * 1e-4
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — facade + microdosimetry test scaffolds (RED)</name>
  <files>tests/test_api_facades.py, tests/test_api_microdosimetry.py</files>
  <read_first>
    - tests/test_api_device.py (fast plain-assert test style to mirror for contract tests)
    - tests/test_mc_coupling.py and tests/test_microdosimetry.py (how the synthetic CSV + spectrum pipeline are exercised)
    - .planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-VALIDATION.md (LIB-06 test map)
  </read_first>
  <behavior>
    - test_api_facades.py (FAST, no devsim solve): for each of the 5 devsim facades (run_radiation_damage, run_dark_current, run_temperature_sweep, run_flash_recombination, run_transient): the name imports from etna, is callable, and its `inspect.signature` first parameter is named "config". Do NOT actually call the devsim facades (avoids devsim exhaustion).
    - test_api_microdosimetry.py (FAST, no devsim): run_microdosimetry(DeviceConfig(), mc_csv_path="data/synthetic_mc_events.csv", sv_thickness_um=10, sv_width_um=150) returns a SimResult with sim_type=="microdosimetry", len(x)==len(y)>0, x and y finite, metadata contains "y_F" and "y_D" with y_D >= y_F.
  </behavior>
  <action>Create `tests/test_api_facades.py`: import `inspect` and `from etna import DeviceConfig` plus the 5 facade names. Use a parametrized test (or one test iterating a list of the 5 imported functions) asserting `callable(fn)` and `list(inspect.signature(fn).parameters)[0] == "config"`. These are pure introspection — no facade is called, so no devsim. Create `tests/test_api_microdosimetry.py`: import `from etna import DeviceConfig, run_microdosimetry` and `from etna.api.results import SimResult`. Resolve the CSV path relative to the repo root (use `pathlib.Path(__file__).parent.parent / "data" / "synthetic_mc_events.csv"` so it is CWD-independent). Assert the SimResult contract from behavior above. This is RED — the 6 facades do not exist yet, so imports fail; expected until Tasks 2-4.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_api_facades.py tests/test_api_microdosimetry.py --collect-only -q 2>&1 | grep -qE "ImportError|cannot import name" && echo "RED-OK: facades not yet defined"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_api_facades.py` exists and references `inspect.signature`.
    - `tests/test_api_microdosimetry.py` exists and references the synthetic CSV via a `Path(__file__).parent.parent` construction (CWD-independent) — verify: `grep -c "parent.parent" tests/test_api_microdosimetry.py` >= 1 and `grep -c "synthetic_mc_events.csv" tests/test_api_microdosimetry.py` >= 1.
    - Both files fail collection only on the missing facade imports, not on syntax errors.
  </acceptance_criteria>
  <done>Both test files exist; fail at import because the 6 facades are not yet defined (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Bucket-a facades — run_radiation_damage, run_temperature_sweep, run_flash_recombination</name>
  <files>etna/api/simulation.py</files>
  <read_first>
    - etna/api/simulation.py (run_cce from Plan 01 as the bucket-a template; run_cv for SimResult packaging)
    - etna/core/charge_collection.py lines 597-660 (cce_vs_fluence signature + return keys)
    - etna/core/temperature_sweep.py lines 215-240 (sweep_cce_vs_temperature — returns pd.DataFrame with T,V,CCE)
    - etna/core/flash_recombination.py lines 263-310 (cce_vs_dose_rate signature + return keys)
  </read_first>
  <action>Append three bucket-a facades to `etna/api/simulation.py`. NONE of them call `build_device()`, `reset_devsim_fully()`, or `devsim.delete_device()` — the core fns self-clean (bucket-a rule; double-cleanup is forbidden).

(1) `def run_radiation_damage(config, fluences=None, V_bias=-40.0, proton_energy_MeV=5.6) -> SimResult`: if `fluences is None`, default to `np.geomspace(1e13, 1e16, 6)`. Call `cce_vs_fluence(fluence_range=fluences, V_bias=V_bias, epi_thickness_cm=config.epi_thickness_um * 1e-4, N_D_junction=config.N_D_junction, N_D_bulk=config.N_D_bulk, L_transition=config.L_transition_um * 1e-4, energy_MeV=proton_energy_MeV)` — forward config doping per the D-01 uniform-forwarding rule. Return `SimResult(config=config, sim_type="damage", x=result["fluences"], y=result["cce_values"], metadata={"V_bias": result["V_bias"], "energy_MeV": result["energy_MeV"]}, mesh=None)`. Docstring MUST note (RESEARCH Pitfall 4): NIEL kappa hardness factors in radiation_damage.py are data-blocked placeholders; absolute Phi_crit numbers are unvalidated — do not present outputs as validated.

(2) `def run_temperature_sweep(config, temperatures=None, voltage=-30.0, method="hecht") -> SimResult`: if `temperatures is None`, default to `np.linspace(250.0, 350.0, 5)`. Call `sweep_cce_vs_temperature(temperatures=temperatures, voltages=[voltage], method=method, epi_thickness_cm=config.epi_thickness_um * 1e-4, N_D_junction=config.N_D_junction, N_D_bulk=config.N_D_bulk, L_transition=config.L_transition_um * 1e-4)`. Pass a SINGLE-element `voltages=[voltage]` so x=T is a clean 1D curve (per D-04 axis choice). Do NOT forward config.T — T is the swept axis. The return is a `pd.DataFrame` (Pitfall 3) — extract `x = df["T"].to_numpy()`, `y = df["CCE"].to_numpy()`. Return `SimResult(config=config, sim_type="temperature", x=x, y=y, metadata={"voltage_V": voltage, "method": method}, mesh=None)`.

(3) `def run_flash_recombination(config, dose_rates_Gy_s=None, V_bias=-30.0, E_MeV=62) -> SimResult`: if `dose_rates_Gy_s is None`, default to `np.array([20.0, 50.0, 100.0, 150.0, 200.0, 230.0])`. Call `cce_vs_dose_rate(dose_rates_Gy_s=dose_rates_Gy_s, V_bias=V_bias, epi_thickness_cm=config.epi_thickness_um * 1e-4, E_MeV=E_MeV, N_D_junction=config.N_D_junction, N_D_bulk=config.N_D_bulk, L_transition=config.L_transition_um * 1e-4)`. Return `SimResult(config=config, sim_type="flash", x=result["dose_rates"], y=result["cce_values"], metadata={"V_bias": result["V_bias"], "E_MeV": result["E_MeV"]}, mesh=None)` (per D-04: x=dose_rate, y=CCE). Docstring MUST note the module's SCOPE/LIMITATIONS: outputs are a numerical sensitivity bound, not validated FLASH physics.</action>
<verify>
<automated>python -c "from etna.api.simulation import run_radiation_damage, run_temperature_sweep, run_flash_recombination; import inspect; assert all(list(inspect.signature(f).parameters)[0]=='config' for f in (run_radiation_damage, run_temperature_sweep, run_flash_recombination)); print('OK')"</automated>
</verify>
<acceptance_criteria> - `grep -Ec "def run_radiation_damage|def run_temperature_sweep|def run_flash_recombination" etna/api/simulation.py` returns 3. - None of the 3 new facade bodies contain `build_device(`, `reset_devsim_fully(`, or `delete_device(` (bucket-a). Verify via inspecting each function body between its `def` and the next `def`. - Each of the 3 forwards `N_D_junction`, `N_D_bulk`, and `L_transition` from `config` into its core call (grep the function bodies). - `run_temperature_sweep` extracts arrays from a DataFrame via `.to_numpy()` and passes `voltages=[voltage]` (single element). - The verify command prints `OK` (all 3 import, first param named `config`).
</acceptance_criteria>
<done>Three bucket-a facades defined with uniform config-forwarding, no facade-level device cleanup, correct axis mapping; import check prints OK.</done>
</task>

<task type="auto">
  <name>Task 3: Bucket-b facades — run_dark_current, run_transient</name>
  <files>etna/api/simulation.py</files>
  <read_first>
    - etna/api/simulation.py::run_cv lines 86-123 (EXACT reset->build->try->finally delete->fallback reset lifecycle to copy)
    - etna/core/dark_current.py lines 435-470 (dark_current_sweep return keys) and 538-564 (create_dark_current_device signature)
    - etna/core/transient.py lines 545-585 (transient_cce_vs_dose_rate signature + DataFrame return columns)
  </read_first>
  <action>Append two facades to `etna/api/simulation.py`.

(1) `def run_dark_current(config, v_start=0.0, v_stop=-100.0, n_points=20, N_t=None, S_n=None, S_p=None) -> SimResult` — bucket-b, full lifecycle copied from run_cv (simulation.py:86-123). Body: `bias_array = np.linspace(v_start, v_stop, n_points)`; `reset_devsim_fully()`; `device_info = create_dark_current_device(T=config.T, N_t=N_t, S_n=S_n, S_p=S_p, epi_thickness_cm=config.epi_thickness_um * 1e-4, N_D_junction=config.N_D_junction, N_D_bulk=config.N_D_bulk, L_transition=config.L_transition_um * 1e-4)` (forward config T + geometry/doping via **kwargs to create_dd_device; do NOT use bare build_device — it omits TAT/SRV setup). Then `try:` call `result = dark_current_sweep(device_info, V_range=bias_array, area=config.area_cm2)`; return `SimResult(config=config, sim_type="dark_current", x=result["voltages"], y=result["I_total"], metadata={"I_SRH": result["I_SRH"], "I_TAT": result["I_TAT"], "I_SRV": result["I_SRV"], "area_cm2": config.area_cm2}, mesh=None)`; `finally:` `try: devsim.delete_device(device=device_info["device_name"]) except Exception:` log a warning via the module `logger` with `exc_info=True` and call `reset_devsim_fully()` — copy the exact run_cv finally block, changing only the log message prefix to "run_dark_current".

(2) `def run_transient(config, dose_rates=None, V_bias=-30.0, epi_thickness_cm=None) -> SimResult` — wraps the self-building `transient_cce_vs_dose_rate` (D-03: minimal satisfier; D-04: x=dose_rate, y=CCE curve → use the LIST form `dose_rates=` so the axis is coherent). Since `transient_cce_vs_dose_rate` builds AND deletes its own devices internally per dose rate, run_transient adds NO build_device/delete_device (this facade is bucket-a-shaped despite the bucket-b label; DO NOT add device cleanup). Body: if `dose_rates is None`, default to `np.array([20.0, 50.0, 100.0, 150.0, 200.0, 230.0])`; resolve `epi_cm = config.epi_thickness_um * 1e-4 if epi_thickness_cm is None else epi_thickness_cm` (forward config epi per uniform rule). Call `df = transient_cce_vs_dose_rate(V_bias=V_bias, dose_rates=dose_rates, epi_thickness_cm=epi_cm)`; the return is a pd.DataFrame (Pitfall 3) — `x = df["dose_rate_Gy_s"].to_numpy()`, `y = df["transient_cce"].to_numpy()`. Return `SimResult(config=config, sim_type="transient", x=x, y=y, metadata={"V_bias": V_bias}, mesh=None)`. Docstring MUST state: run_transient's signature is not defined in the design spec (D-03) — this wraps transient_cce_vs_dose_rate as the minimal satisfier of the LIB-06 criterion (import + DeviceConfig first arg), returning a dose-rate→transient-CCE curve.</action>
<verify>
<automated>python -c "from etna.api.simulation import run_dark_current, run_transient; import inspect; assert list(inspect.signature(run_dark_current).parameters)[0]=='config' and list(inspect.signature(run_transient).parameters)[0]=='config'; print('OK')"</automated>
</verify>
<acceptance_criteria> - `grep -Ec "def run_dark_current|def run_transient" etna/api/simulation.py` returns 2. - `run_dark_current` body contains `reset_devsim_fully(`, `create_dark_current_device(`, a `finally:` block, and `delete_device(` (full bucket-b lifecycle). It does NOT call `build_device(`. - `run_transient` body contains NO `build_device(`, NO `create_dark_current_device(`, and NO `delete_device(` (self-building core; adds no cleanup), and extracts arrays via `.to_numpy()` from a DataFrame with columns `dose_rate_Gy_s` and `transient_cce`. - `run_dark_current` forwards `config.T` and config doping into `create_dark_current_device`. - The verify command prints `OK`.
</acceptance_criteria>
<done>run_dark_current (full bucket-b lifecycle) and run_transient (self-building wrapper, no facade cleanup) defined; import check prints OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Bucket-c facade run_microdosimetry + re-export all 6 + GREEN</name>
  <files>etna/api/simulation.py, etna/__init__.py</files>
  <read_first>
    - etna/core/mc_coupling.py lines 107-160 (load_mc_events_csv: default column_map maps CSV x/y/z/edep -> x_cm/.../edep_keV; returns per-step DataFrame)
    - etna/core/microdosimetry.py lines 36-60 (mean_chord_length) and 117-220 (lineal_energy_spectrum return keys)
    - data/synthetic_mc_events.csv (header: event_id,x,y,z,edep — multiple rows per event_id)
    - etna/__init__.py (import + __all__ to extend with all 6 facades)
  </read_first>
  <action>Append `def run_microdosimetry(config, mc_csv_path, sv_thickness_um=10.0, sv_width_um=150.0) -> SimResult` to `etna/api/simulation.py` — bucket-c, NO devsim: no reset_devsim_fully, no build_device, no delete_device. Body: `events = load_mc_events_csv(mc_csv_path)` (returns per-step DataFrame with columns event_id, x_cm, y_cm, z_cm, edep_keV). Bridge per-step → per-event collected energy (this aggregation has NO existing analog — define it explicitly): `collected_energies_keV = events.groupby("event_id")["edep_keV"].sum().to_numpy()`. Compute `l_bar = mean_chord_length(sv_thickness_um, sv_width_um=sv_width_um)`. Compute `spec = lineal_energy_spectrum(collected_energies_keV, l_bar)`. Return `SimResult(config=config, sim_type="microdosimetry", x=spec["bin_centers"], y=spec["d_y"] * spec["bin_centers"], metadata={"y_F": spec["y_F"], "y_D": spec["y_D"], "f_y": spec["f_y"], "l_bar_um": l_bar}, mesh=None)` — y is the y·d(y) representation per D-04 / design spec §3.4. Note in the docstring: `mc_csv_path` is a caller-supplied local file parsed by pandas read_csv (no eval/pickle) — see threat model.

Then update `etna/__init__.py`: extend the `from etna.api.simulation import (...)` line to include all 6 new names (run_radiation_damage, run_dark_current, run_temperature_sweep, run_flash_recombination, run_transient, run_microdosimetry) alongside run_cv, run_field, run_cce; add all 6 to `__all__`.</action>
<verify>
<automated>.venv/bin/pytest tests/test_api_facades.py tests/test_api_microdosimetry.py -q 2>&1 | tail -6</automated>
</verify>
<acceptance_criteria> - `grep -c "def run_microdosimetry" etna/api/simulation.py` returns 1. - `run_microdosimetry` body contains the aggregation `groupby("event_id")` and `["edep_keV"].sum()`, and calls `lineal_energy_spectrum(`; it contains NO `devsim`, NO `reset_devsim_fully`, NO `build_device` (bucket-c) — verify: `sed -n '/def run_microdosimetry/,/^def /p' etna/api/simulation.py | grep -Ec 'devsim\.|reset_devsim_fully\(|build_device\('` returns 0. - `python -c "from etna import run_radiation_damage, run_dark_current, run_temperature_sweep, run_flash_recombination, run_transient, run_microdosimetry"` exits 0. - `.venv/bin/pytest tests/test_api_facades.py tests/test_api_microdosimetry.py -q` passes all tests (5 contract + microdosimetry data-pipeline).
</acceptance_criteria>
<done>run_microdosimetry defined as a bucket-c data pipeline with the explicit event aggregation; all 6 facades re-exported; both test files green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                         |
| --------------------------- | --------------------------------------------------------------------------------------------------- |
| caller → run_microdosimetry | `mc_csv_path` is a caller-supplied local filesystem path — the only file-input surface in the phase |
| caller → other 5 facades    | Numeric config/sweep args only; no filesystem/network                                               |

## STRIDE Threat Register

| Threat ID       | Category          | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                               |
| --------------- | ----------------- | ------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-37-02-V5      | Tampering         | run_microdosimetry mc_csv_path | mitigate    | Path parsed by `mc_coupling.load_mc_events_csv` → pandas `read_csv` (no eval/pickle/code exec). Not a new surface — existing MC tests already parse the same CSV. No deserialization of untrusted binary.                     |
| T-37-02-DoS     | Denial of Service | devsim process-global registry | mitigate    | Bucket-a/c facades add no device; bucket-b (run_dark_current) uses reset->finally-delete. Contract tests do NOT call devsim facades (signature-only). Microdosimetry test is devsim-free. Per-file test isolation (STATE.md). |
| T-37-02-SC      | Tampering         | npm/pip/cargo installs         | accept      | N/A — zero external packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable).                                                                                                                        |
| </threat_model> |

<verification>
- `.venv/bin/pytest tests/test_api_facades.py -q` and `.venv/bin/pytest tests/test_api_microdosimetry.py -q` — green in per-file isolation (NEVER monolithic `pytest -q`).
- `python -c "from etna import run_radiation_damage, run_dark_current, run_temperature_sweep, run_flash_recombination, run_transient, run_microdosimetry"` exits 0.
</verification>

<success_criteria>

- LIB-06 satisfied: all 6 facades import from etna; each accepts DeviceConfig as first arg.
- Bucket lifecycle correct per facade: bucket-a (radiation_damage, temperature_sweep, flash_recombination) + self-building run_transient add NO device cleanup; bucket-b run_dark_current owns the full reset->finally-delete lifecycle; bucket-c run_microdosimetry touches no devsim.
- Uniform config-forwarding applied (D-01 anchor) — every facade maps config doping/geometry into its core call (except T for run_temperature_sweep, which is the swept axis).
- Both test files pass in isolation.
  </success_criteria>

<output>
Create `.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-02-SUMMARY.md` when done.
</output>
