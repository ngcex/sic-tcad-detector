---
phase: 39-c-v-cce-field-map-pages-csv-download
plan: 03
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - app/workflows/cv.py
  - app/workflows/cce.py
  - tests/test_app_cv_page.py
  - tests/test_app_cce_page.py
autonomous: true
requirements: [UI-03, UI-04, UI-06]
must_haves:
  truths:
    - "User clicks Run on the C-V page and sees a Plotly C-V curve + Mott-Schottky plot, then can download a CSV"
    - "User clicks Run on the CCE page and sees a Plotly CCE vs bias plot, then can download a CSV"
    - "A 2D device config shows a 1D-only warning and does not run or crash on either page"
    - "The result survives a rerun (Download-button click) without re-solving devsim"
  artifacts:
    - path: "app/workflows/cv.py"
      provides: "C-V page: 1D guard, Run→cache→render→download"
      contains: "petringa.run_cv"
    - path: "app/workflows/cce.py"
      provides: "CCE page: 1D guard, Run→cache→render→download"
      contains: "petringa.run_cce"
    - path: "tests/test_app_cv_page.py"
      provides: "AppTest with mocked run_cv: no-crash, session_state cache, 2D guard"
      contains: "monkeypatch.setattr"
    - path: "tests/test_app_cce_page.py"
      provides: "AppTest with mocked run_cce: no-crash, session_state cache, 2D guard"
      contains: "monkeypatch.setattr"
  key_links:
    - from: "app/workflows/cv.py"
      to: "petringa.run_cv"
      via: "petringa.run_cv(cfg) inside the Run button handler"
      pattern: "petringa\\.run_cv"
    - from: "app/workflows/cv.py"
      to: "app.components.results"
      via: "build_cv_figure / build_mott_schottky_figure / to_csv_bytes"
      pattern: "from app.components.results import"
    - from: "app/workflows/cce.py"
      to: "petringa.run_cce"
      via: "petringa.run_cce(cfg) inside the Run button handler"
      pattern: "petringa\\.run_cce"
---

<objective>
Turn the two placeholder pages `app/workflows/cv.py` and `app/workflows/cce.py`
into working simulation pages: empty-state guard → 1D-only pre-check → "Run
simulation" button → cache the `SimResult` in `st.session_state` → render Plotly
charts from the cache → `st.download_button` with pre-built CSV bytes. Each page
is written together with its own mocked-facade AppTest in the SAME task (test
first → RED → GREEN), so no task's verify references a file a sibling produces.

Purpose: Delivers UI-03 (C-V curve + Mott-Schottky), UI-04 (CCE vs bias), and
the download half of UI-06 for these two pages. Uses the results.py builders and
serializer from plan 39-02 (no chart/CSV logic is written here).
Output: cv.py + test_app_cv_page.py, cce.py + test_app_cce_page.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-RESEARCH.md
@.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-PATTERNS.md
@app/workflows/home.py
@app/workflows/cv.py
@app/workflows/cce.py
@tests/test_app_pages.py

<interfaces>
<!-- Consumed from app/components/results.py (defined in plan 39-02): -->
  build_cv_figure(result: SimResult) -> go.Figure
  build_mott_schottky_figure(result: SimResult) -> go.Figure
  build_cce_figure(result: SimResult) -> go.Figure
  to_csv_bytes(result: SimResult) -> bytes
<!-- Facade calls (reference as MODULE ATTRIBUTE so tests can monkeypatch — proven in 39-01): -->
  import petringa
  petringa.run_cv(cfg) -> SimResult   # sim_type="cv"; raises NotImplementedError if cfg.half_width_um is not None
  petringa.run_cce(cfg) -> SimResult  # sim_type="cce"; raises NotImplementedError if cfg.half_width_um is not None
<!-- session_state key convention: device_config (input), cv_result / cce_result (outputs) -->
</interfaces>
</context>

<assumptions>
Adopted from RESEARCH.md (no CONTEXT.md — for user review):
- A5: 2D config → `st.warning` + `st.stop()` (a pre-check, not try/except; run_cce raises but run_field does not, so the whole phase standardizes on a pre-check).
- A4: bonus depletion-width plot offered opt-in via an expander (additive, removable).
- Facade referenced as `petringa.run_cv` / `petringa.run_cce` per proven 39-01 spike.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C-V page + AppTest — 1D guard, Run→cache→render→download</name>
  <files>app/workflows/cv.py, tests/test_app_cv_page.py</files>
  <read_first>
    - app/workflows/cv.py (current placeholder — see the exact empty-state guard string and the caption to REMOVE)
    - app/workflows/home.py lines 1-6, 20-25 (docstring style, empty-state guard verbatim string, config-echo idiom)
    - app/components/results.py (build_cv_figure, build_mott_schottky_figure, to_csv_bytes signatures from plan 39-02)
    - tests/test_app_pages.py lines 78-90 (AppTest.from_function wrapper with imports INSIDE the body; empty-state guard string that MUST stay verbatim)
    - tests/test_app_run_mockability.py (proven monkeypatch.setattr(petringa, "run_*", fake) seam from 39-01)
    - petringa/api/results.py (SimResult fields for building the fake)
    - .planning/phases/39-c-v-cce-field-map-pages-csv-download/39-RESEARCH.md §5 (Run→cache→render→download, load-bearing) and §Validation (AppTest 1.55 has NO plotly_chart / download_button accessor)
  </read_first>
  <behavior>
    - Empty session_state → st.info("Configure a device in the sidebar to begin.") + st.stop(); no exception.
    - cfg.half_width_um is not None (2D) → st.warning (1D-only message) + st.stop(); run_cv NOT called; no "cv_result" written; no exception.
    - 1D cfg + Run clicked → petringa.run_cv(cfg) result stored in st.session_state["cv_result"].
    - After result cached, a rerun with Run NOT clicked still renders charts + download (result read from session_state).
  </behavior>
  <action>
    Write the AppTest FIRST (test_app_cv_page.py), then the page (cv.py) to make it pass.

    tests/test_app_cv_page.py: `from streamlit.testing.v1 import AppTest`,
    `from petringa import DeviceConfig, SimResult`, `import petringa`, `import numpy as np`.
    Each test takes the `monkeypatch` fixture and sets
    `monkeypatch.setattr(petringa, "run_cv", fake_run_cv)` BEFORE `at.run()` (module-attribute
    patch — the seam proven in 39-01). fake_run_cv returns SimResult(config=cfg,
    sim_type="cv", x=np.array([0.0, -10.0]), y=np.array([1e-9, 2e-9]),
    metadata={"one_over_C_squared": np.array([1e18, 2.5e17]), "depletion_widths": np.array([1e-4, 2e-4]),
    "area_cm2": 1e-4}, mesh=None). Use the from_function wrapper importing render inside its body.
    Three tests, asserting ONLY on the accessible surface (RESEARCH §Validation:
    `at.exception`, `at.session_state`, `at.button`, `at.warning`, `at.info` — NO
    plotly_chart/download_button accessor):
      1. test_run_caches_result: seed `at.session_state["device_config"] = DeviceConfig()`,
         click the "Run simulation" button and rerun (verify the exact AppTest 1.55 button
         click+rerun API during execution), assert `at.exception == []`,
         `at.session_state["cv_result"].sim_type == "cv"`, and `len(at.session_state["cv_result"].x) == 2`
         (proves the fake ran, not a real 40-point devsim solve).
      2. test_2d_config_warns_and_skips: seed `DeviceConfig(half_width_um=50.0)`, run, assert
         `at.exception == []`, `len(at.warning) >= 1`, and `"cv_result" not in at.session_state`.
      3. test_empty_state_guard: no device_config; assert `at.exception == []` and the guard
         info string is in `[el.value for el in at.info]`.

    Then rewrite app/workflows/cv.py to pass. Replace the module docstring with a
    home.py-style docstring documenting render()/no-side-effects + AppTest dual-use;
    REMOVE the placeholder caption "Running this simulation is implemented in Phase 39."
    Imports: `from __future__ import annotations`, `import streamlit as st`,
    `import petringa`,
    `from app.components.results import build_cv_figure, build_mott_schottky_figure, to_csv_bytes`.
    Inside render():
    1. st.title("C-V Analysis").
    2. Empty-state guard VERBATIM: `cfg = st.session_state.get("device_config")`; if cfg
       is None → `st.info("Configure a device in the sidebar to begin.")` then `st.stop()`.
    3. 1D-only PRE-CHECK (not try/except): if `cfg.half_width_um is not None` →
       `st.warning("These workflows are 1D-only. 2D field visualization arrives in Phase 40 (geometry viewer). Set Dimensionality to 1D in the sidebar.")`
       then `st.stop()`.
    4. `if st.button("Run simulation"): st.session_state["cv_result"] = petringa.run_cv(cfg)`
       (MODULE ATTRIBUTE `petringa.run_cv`).
    5. `result = st.session_state.get("cv_result")`; `if result is not None:`
       `st.plotly_chart(build_cv_figure(result))`, `st.plotly_chart(build_mott_schottky_figure(result))`,
       then `st.download_button("Download CSV", data=to_csv_bytes(result), file_name="cv_result.csv", mime="text/csv")`.
    6. Optional bonus (A4): an `st.expander("Depletion width vs V")` plotting
       metadata["depletion_widths"]*1e4 (cm→µm) — opt-in inside the expander.
    Do NOT wrap run_cv in try/except for the 2D case — the pre-check handles it.

  </action>
  <verify>
    <automated>uv run python -c "import app.workflows.cv" && uv run pytest tests/test_app_cv_page.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_app_cv_page.py` contains `monkeypatch.setattr(petringa, "run_cv"` set BEFORE `at.run()`; has the three named tests
    - The 2D test asserts `len(at.warning) >= 1` AND `"cv_result" not in at.session_state` AND `at.exception == []`
    - Happy-path test asserts `at.session_state["cv_result"].sim_type == "cv"` and `len(...x) == 2` (fake ran, not devsim)
    - No test asserts on a plotly_chart or download_button accessor
    - `app/workflows/cv.py` contains literal `petringa.run_cv(cfg)` (module-attribute style), NOT `from petringa import run_cv`
    - Contains `from app.components.results import` referencing build_cv_figure, build_mott_schottky_figure, to_csv_bytes
    - Empty-state guard string `"Configure a device in the sidebar to begin."` and the 2D warning string present verbatim; 2D case is a pre-check (run_cv not wrapped in try/except)
    - Placeholder caption "Running this simulation is implemented in Phase 39." is GONE (grep returns 0)
    - `st.download_button` present with `file_name="cv_result.csv"` and `data=to_csv_bytes(result)`
    - `uv run pytest tests/test_app_cv_page.py -x` exits 0
  </acceptance_criteria>
  <done>C-V page runs run_cv on click, caches to session_state, renders both Plotly figures from the cache, offers a CSV download without re-solving on rerun; its AppTest (happy path + 2D guard + empty-state) passes with a mocked facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CCE page + AppTest — 1D guard, Run→cache→render→download</name>
  <files>app/workflows/cce.py, tests/test_app_cce_page.py</files>
  <read_first>
    - app/workflows/cce.py (current placeholder)
    - app/workflows/cv.py (the page written in Task 1 — mirror its structure exactly, swapping cv→cce)
    - tests/test_app_cv_page.py (the test written in Task 1 — mirror its structure, swapping cv→cce)
    - app/components/results.py (build_cce_figure, to_csv_bytes)
    - tests/test_app_run_mockability.py (monkeypatch seam)
    - petringa/api/simulation.py lines 401-411 (run_cce metadata keys: I_collected, I_generated)
  </read_first>
  <behavior>
    - Empty session_state → st.info(guard string) + st.stop(); no exception.
    - 2D cfg → st.warning + st.stop(); run_cce NOT called; no "cce_result"; no exception.
    - 1D cfg + Run → petringa.run_cce(cfg) cached under st.session_state["cce_result"].
    - Cached result renders CCE Plotly figure + download on rerun without re-solve.
  </behavior>
  <action>
    Write the AppTest FIRST (test_app_cce_page.py), then the page (cce.py) to pass it.

    tests/test_app_cce_page.py mirrors test_app_cv_page.py, substituting cce for cv:
    `monkeypatch.setattr(petringa, "run_cce", fake_run_cce)` set BEFORE `at.run()`.
    fake_run_cce returns SimResult(config=cfg, sim_type="cce", x=np.array([-10.0, -100.0]),
    y=np.array([0.85, 0.98]), metadata={"I_collected": np.array([1e-6, 2e-6]),
    "I_generated": 5e-6}, mesh=None) — note I_generated is a scalar to match the
    upstream shape. Three tests: test_run_caches_result (assert
    `at.session_state["cce_result"].sim_type == "cce"`, `len(...x) == 2`),
    test_2d_config_warns_and_skips (`DeviceConfig(half_width_um=50.0)` → `len(at.warning) >= 1`,
    `"cce_result" not in at.session_state`), test_empty_state_guard. Assert only on
    exception/session_state/button/warning/info (no plotly_chart/download_button accessor).

    Then rewrite app/workflows/cce.py mirroring Task-1 cv.py, substituting cce for cv:
    home.py-style docstring; remove placeholder caption. Imports:
    `from __future__ import annotations`, `import streamlit as st`, `import petringa`,
    `from app.components.results import build_cce_figure, to_csv_bytes`.
    render(): st.title("Charge Collection (CCE)"); verbatim empty-state guard; same 1D-only
    pre-check (`if cfg.half_width_um is not None:` → same warning string → st.stop());
    `if st.button("Run simulation"): st.session_state["cce_result"] = petringa.run_cce(cfg)`;
    `result = st.session_state.get("cce_result")`; if result is not None →
    `st.plotly_chart(build_cce_figure(result))` then
    `st.download_button("Download CSV", data=to_csv_bytes(result), file_name="cce_result.csv", mime="text/csv")`.
    Surface metadata["I_collected"]/["I_generated"] as an `st.caption` (small caption, NOT a
    required plot per RESEARCH Extra Plots). Reference the facade as `petringa.run_cce`.

  </action>
  <verify>
    <automated>uv run python -c "import app.workflows.cce" && uv run pytest tests/test_app_cce_page.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_app_cce_page.py` contains `monkeypatch.setattr(petringa, "run_cce"` set BEFORE `at.run()`; has the three named tests
    - The 2D test asserts `len(at.warning) >= 1` AND `"cce_result" not in at.session_state` AND `at.exception == []`
    - Happy-path test asserts `at.session_state["cce_result"].sim_type == "cce"` and `len(...x) == 2`
    - `app/workflows/cce.py` contains literal `petringa.run_cce(cfg)` (module-attribute style)
    - Contains `from app.components.results import` referencing build_cce_figure and to_csv_bytes
    - Empty-state guard string and 2D warning string present verbatim; 2D case is a pre-check (no try/except around run_cce)
    - `st.download_button` with `file_name="cce_result.csv"` and `data=to_csv_bytes(result)`
    - Placeholder caption "Running this simulation is implemented in Phase 39." is GONE
    - `uv run pytest tests/test_app_cce_page.py -x` exits 0
  </acceptance_criteria>
  <done>CCE page runs run_cce on click, caches, renders the Plotly CCE figure, offers CSV download without re-solving on rerun; its AppTest (happy path + 2D guard + empty-state) passes with a mocked facade.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------ |
| sidebar widgets → run_* | Numeric, min_value-bounded config; a 2D config is guarded before reaching a 1D-only facade |

## STRIDE Threat Register

| Threat ID       | Category          | Component                         | Disposition | Mitigation Plan                                                                                                                                                                                |
| --------------- | ----------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-02         | Denial of Service | 2D config reaching run_cv/run_cce | mitigate    | 1D-only pre-check (`if cfg.half_width_um is not None: st.warning(...); st.stop()`) BEFORE the facade call — prevents uncaught NotImplementedError; verified by test_2d_config_warns_and_skips. |
| T-39-01         | Tampering         | CSV download bytes                | accept      | Serializer (39-02) uses numeric values + hardcoded headers only; no free-text cell. Risk LOW.                                                                                                  |
| </threat_model> |

<verification>
- `uv run pytest tests/test_app_cv_page.py tests/test_app_cce_page.py -x` passes
- `uv run python -c "import app.workflows.cv, app.workflows.cce"` exits 0
- Existing suite still green: `uv run pytest tests/test_app_pages.py -x`
</verification>

<success_criteria>

- C-V page: Run→Plotly C-V + Mott-Schottky→CSV download (UI-03, UI-06)
- CCE page: Run→Plotly CCE vs bias→CSV download (UI-04, UI-06)
- Both: 2D guard warns + skips without crashing; result survives download rerun
  </success_criteria>

<output>
Create `.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-03-SUMMARY.md` when done
</output>
