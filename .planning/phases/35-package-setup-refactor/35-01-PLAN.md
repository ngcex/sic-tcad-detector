---
phase: 35-package-setup-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - etna/__init__.py
  - etna/_version.py
  - README.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02

must_haves:
  truths:
    - "pyproject.toml exists with hatchling build backend, all runtime deps, and dev extras"
    - "etna/ package directory is importable after `uv pip install -e .`"
    - "`python -c 'from etna import DeviceConfig'` exits 0"
    - "`python -c 'import etna; print(etna.__version__)'` prints 5.0.0"
    - "requirements.txt is removed and README.md install instruction updated to uv pip install -e ."
  artifacts:
    - path: "pyproject.toml"
      provides: "Build metadata, dependency declarations, wheel target"
      contains: "[build-system]"
    - path: "etna/__init__.py"
      provides: "DeviceConfig stub + __version__ re-export"
      contains: "class DeviceConfig"
    - path: "etna/_version.py"
      provides: "Single-source version string"
      contains: "__version__"
  key_links:
    - from: "pyproject.toml"
      to: "etna/"
      via: '[tool.hatch.build.targets.wheel] packages = ["etna"]'
      pattern: "packages.*etna"
    - from: "etna/__init__.py"
      to: "etna._version"
      via: "from etna._version import __version__"
      pattern: "from etna._version import"
---

<objective>
Create the etna package scaffold: pyproject.toml with hatchling, etna/__init__.py with
DeviceConfig stub, etna/_version.py, remove requirements.txt, and perform the editable install.

Purpose: Establish the installable package foundation (PKG-01, PKG-02) that all subsequent
v5.0 phases depend on. After this plan, `from etna import DeviceConfig` works and
`uv pip install -e ".[dev]"` succeeds.

Output: pyproject.toml, etna/**init**.py, etna/\_version.py, editable install registered
in the active .venv. requirements.txt removed (replaced by pyproject.toml per PKG-01).

Note: etna/core/ is intentionally NOT created here. Plan 02 uses `git mv src etna/core`
to rename the directory; that command only works correctly when the destination does not already
exist. Creating etna/core/ in Plan 01 would cause `git mv` to nest src/ inside it.
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ngcex/projects/physics/etna/.planning/PROJECT.md
@/Users/ngcex/projects/physics/etna/.planning/ROADMAP.md
@/Users/ngcex/projects/physics/etna/.planning/STATE.md
@/Users/ngcex/projects/physics/etna/.planning/phases/35-package-setup-refactor/35-RESEARCH.md
@/Users/ngcex/projects/physics/etna/.planning/phases/35-package-setup-refactor/35-PATTERNS.md

<interfaces>
<!-- Key files the executor must understand before creating new files. -->

From requirements.txt (current dependency list — cross-check when writing pyproject.toml):
devsim>=2.10.0
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
pytest>=7.0

Note: pyproject.toml adds plotly>=5.0, streamlit>=1.30, pandas>=2.0 to runtime deps
(not in requirements.txt). pytest moves to [dev] optional extra. jupyter added to [dev].

From src/sic_material.py (dataclass style convention for DeviceConfig stub):
@dataclass style with field-per-line and inline comments
`from dataclasses import dataclass` (not dataclasses.dataclass)
No **post_init** in stub — that is Phase 36 scope

DeviceConfig fields (from design spec §3.1, confirmed in 35-RESEARCH.md §Pattern 4):
epi_thickness_um: float = 10.0
substrate_thickness_um: float = 1.0
half_width_um: Optional[float] = None
N_A: float = 1e19
doping_profile: str = "graded"
N_D: Optional[float] = None
N_D_junction: float = 2.93e15
N_D_bulk: float = 8.82e13
L_transition_um: float = 0.987
T: float = 300.0
area_cm2: float = 1e-4

README.md line 45 (current): pip install -r requirements.txt # or: uv pip install -r requirements.txt
README.md line 45 (target): uv pip install -e ".[dev]"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and etna package scaffold</name>
  <files>pyproject.toml, etna/__init__.py, etna/_version.py, README.md</files>
  <read_first>
    - /Users/ngcex/projects/physics/etna/requirements.txt — current dep list; cross-check every dep is carried over
    - /Users/ngcex/projects/physics/etna/src/sic_material.py — lines 1-35; dataclass style convention to replicate in DeviceConfig
    - /Users/ngcex/projects/physics/etna/README.md — line 45; find the install instruction to update
    - /Users/ngcex/projects/physics/etna/.planning/phases/35-package-setup-refactor/35-RESEARCH.md — §Pattern 1 (pyproject.toml with hatchling), §Pattern 4 (DeviceConfig stub), §Anti-Patterns to Avoid
    - /Users/ngcex/projects/physics/etna/.planning/phases/35-package-setup-refactor/35-PATTERNS.md — §pyproject.toml section (full content), §etna/__init__.py section (stub + field list)
  </read_first>
  <action>
    Create three new files and update one existing file. Use uv (not pip) for all install steps.
    Do NOT create etna/core/ — Plan 02 relies on `git mv src etna/core` creating it fresh;
    if etna/core/ exists beforehand, git mv nests src/ inside it instead of renaming.

    (1) Create pyproject.toml at project root with:
    - [build-system] section: requires = ["hatchling"], build-backend = "hatchling.build"
    - [project] section: name = "etna", version = "5.0.0", description = "SiC TCAD Simulator Library for the Petringa group", requires-python = ">=3.13"
    - [project] dependencies: devsim>=2.10.0, numpy>=1.24, scipy>=1.11, matplotlib>=3.7, plotly>=5.0, streamlit>=1.30, pandas>=2.0
    - [project.optional-dependencies] dev: pytest>=7.0, jupyter>=1.0
    - [tool.hatch.build.targets.wheel] packages = ["etna"] — must be explicit because tests/__init__.py
      exists and hatchling auto-discovery would include tests/ in the wheel without this key
    - No [tool.pytest.ini_options] section — pytest.ini stays unchanged

    (2) Create etna/_version.py containing only: __version__ = "5.0.0"

    (3) Create etna/__init__.py with:
    - Module docstring: "etna: SiC TCAD Simulator Library."
    - from __future__ import annotations
    - from dataclasses import dataclass, field
    - from typing import Optional
    - from etna._version import __version__
    - DeviceConfig dataclass with all 11 fields listed in the interface block above, following
      the sic_material.py style (field-per-line, inline unit comments for units/source)
    - Docstring on DeviceConfig: "Device configuration stub — full implementation in Phase 36 (LIB-01)."
    - __all__ = ["DeviceConfig", "__version__"]
    - No __post_init__, no build_device(), no physics — this is a stub only

    (4) Update README.md: replace the `pip install -r requirements.txt` install instruction
    (line ~45) with `uv pip install -e ".[dev]"`. This is the only README change; do not alter
    other content.

    (5) Remove requirements.txt: `git rm requirements.txt`
    PKG-01 says pyproject.toml "replaces" requirements.txt. No code references requirements.txt
    at runtime (grep confirmed: only README.md at line 45 and design spec docs mention it;
    README is updated in step 4 above).

    (6) Run: uv pip install -e ".[dev]"
    This registers the package in editable mode. plotly and streamlit will be pulled from PyPI.
    The BLAS warning "libopenblas.dylib: MISSING DLL / liblapack.dylib: ALL BLAS/LAPACK LOADED"
    is expected and benign on macOS arm64 — LAPACK loading is sufficient.

  </action>
  <verify>
    <automated>
      cd /Users/ngcex/projects/physics/etna && \
      uv pip install -e ".[dev]" || exit 1 && \
      python -c "from etna import DeviceConfig; c = DeviceConfig(); print('DeviceConfig OK:', c.epi_thickness_um)" || exit 1 && \
      python -c "import etna; print('version:', etna.__version__)" || exit 1 && \
      grep -q 'packages.*etna' pyproject.toml || exit 1 && \
      grep -q 'hatchling' pyproject.toml || exit 1 && \
      grep -q 'plotly' pyproject.toml || exit 1 && \
      grep -q 'streamlit' pyproject.toml || exit 1 && \
      grep -q 'pandas' pyproject.toml || exit 1 && \
      grep -q 'class DeviceConfig' etna/__init__.py || exit 1 && \
      test ! -f requirements.txt || (echo "FAIL: requirements.txt still exists" && exit 1) && \
      echo "ALL PLAN 01 CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists and contains [build-system], hatchling, [project], packages = ["etna"], plotly>=5.0, streamlit>=1.30, pandas>=2.0, [project.optional-dependencies] dev section with pytest and jupyter
    - etna/__init__.py exists and contains "class DeviceConfig"
    - etna/_version.py exists and contains '__version__ = "5.0.0"'
    - etna/core/ does NOT exist (deliberately deferred to Plan 02)
    - requirements.txt does NOT exist (git rm'd — replaced by pyproject.toml per PKG-01)
    - `uv pip install -e ".[dev]"` exits 0
    - `python -c "from etna import DeviceConfig"` exits 0
    - `python -c "import etna; print(etna.__version__)"` prints "5.0.0"
    - `grep -q 'packages.*etna' pyproject.toml` exits 0
  </acceptance_criteria>
  <done>
    pyproject.toml with hatchling and all deps exists; requirements.txt removed; etna package
    is installed in editable mode; DeviceConfig stub and __version__ are importable from etna;
    README updated; Plan 02 can safely run `git mv src etna/core` without nesting.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                          |
| ----------------- | ------------------------------------------------------------------------------------ |
| PyPI → local venv | New packages pulled during uv pip install -e ".[dev]" (plotly, streamlit, hatchling) |

## STRIDE Threat Register

| Threat ID | Category  | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                       |
| --------- | --------- | -------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-01   | Tampering | pyproject.toml packages discovery      | mitigate    | Set packages = ["etna"] explicitly; tests/**init**.py exists and hatchling auto-discovery would include tests/ without this key                                                                                                                                                                                                                   |
| T-35-SC   | Tampering | pip/cargo installs (plotly, streamlit) | accept      | Package legitimacy audit in 35-RESEARCH.md §Package Legitimacy Audit reviewed and approved all packages. plotly and streamlit are tagged [ASSUMED] for download counts but are tier-1 established PyPI libraries (6-8 year history, github.com/plotly, github.com/streamlit). No slopcheck flag raised. Auto-advance acceptable per advisor guidance. |

</threat_model>

<verification>
Phase-gate checks for Plan 01 (run in order):

1. `uv pip install -e ".[dev]"` exits 0
2. `python -c "from etna import DeviceConfig; print('OK')"` prints OK
3. `python -c "import etna; print(etna.__version__)"` prints 5.0.0
4. `grep 'packages.*etna' /Users/ngcex/projects/physics/etna/pyproject.toml` returns a match
5. `test ! -f /Users/ngcex/projects/physics/etna/requirements.txt` exits 0
6. `test ! -d /Users/ngcex/projects/physics/etna/etna/core` exits 0
   </verification>

<success_criteria>

- pyproject.toml created with hatchling backend, all 7 runtime deps, dev extras (PKG-01, PKG-02)
- etna/ package directory with **init**.py (DeviceConfig stub + **version**) and \_version.py exists
- requirements.txt removed (PKG-01: "replacing requirements.txt")
- `uv pip install -e ".[dev]"` completes without error
- `from etna import DeviceConfig` works without error
- `etna.__version__` == "5.0.0"
- etna/core/ does NOT exist — Plan 02 will create it via `git mv src etna/core`
  </success_criteria>

<output>
Create `.planning/phases/35-package-setup-refactor/35-01-SUMMARY.md` when done.
</output>
