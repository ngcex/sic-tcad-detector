---
phase: 37-core-api-cce-remaining-facades-parametricsweep
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - petringa/api/sweep.py
  - petringa/__init__.py
  - tests/test_api_sweep.py
autonomous: true
requirements: [LIB-07]

must_haves:
  truths:
    - "from petringa import ParametricSweep imports without error"
    - "ParametricSweep(base_config, param, values, sim_fn, sim_kwargs).run() returns a list[SimResult] of length == len(values)"
    - "each run clones base_config via dataclasses.replace(base_config, **{param: value}) so the swept field is injected into each config"
    - "ParametricSweep raises TypeError (not silent attribute injection) when param is not a DeviceConfig field"
    - "the sweep unit test uses a fake sim_fn (canned SimResult) and never builds a devsim device"
  artifacts:
    - path: "petringa/api/sweep.py"
      provides: "ParametricSweep dataclass with .run()"
      contains: "dataclasses"
    - path: "petringa/__init__.py"
      provides: "ParametricSweep public re-export"
      contains: "ParametricSweep"
    - path: "tests/test_api_sweep.py"
      provides: "fast unit tests with a fake sim_fn (no devsim)"
      contains: "def fake_sim"
  key_links:
    - from: "petringa/api/sweep.py::ParametricSweep.run"
      to: "dataclasses.replace"
      via: "config cloning with field injection (TypeError on unknown field)"
      pattern: "replace\\(.*self\\.param"
    - from: "petringa/__init__.py"
      to: "petringa/api/sweep.py::ParametricSweep"
      via: "import + __all__ entry"
      pattern: "ParametricSweep"
---

<objective>
Add the `ParametricSweep` utility class — pure Python orchestration over any facade `sim_fn`, devsim-agnostic. For each value in `values`, it clones `base_config` via `dataclasses.replace(base_config, **{param: value})`, calls `sim_fn(cloned_config, **sim_kwargs)`, and collects the results into a `list[SimResult]`.

Purpose: LIB-07 — `from petringa import ParametricSweep`; `.run()` over 2+ parameter values returns a `list[SimResult]` of correct length. Design spec §3.5 fully specifies the API. Tests use a FAKE sim_fn so they never touch devsim (sidesteps the process-exhaustion blocker).
Output: `ParametricSweep` in a new `petringa/api/sweep.py`, re-exported from `petringa/__init__.py`, covered by `tests/test_api_sweep.py` (fast, fake sim_fn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-RESEARCH.md
@.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-PATTERNS.md
@.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-VALIDATION.md

<interfaces>
<!-- Design spec §3.5 — implement exactly. Verified against RESEARCH Code Examples. -->

petringa/api/sweep.py::ParametricSweep (design spec §3.5):
@dataclass
class ParametricSweep:
base_config: DeviceConfig
param: str # a DeviceConfig field name
values: list
sim_fn: Callable # any facade, e.g. run_cce
sim_kwargs: dict = field(default_factory=dict)

      def run(self) -> list[SimResult]:
          # for each value: cfg_i = dataclasses.replace(base_config, **{param: value})
          #                 results.append(sim_fn(cfg_i, **sim_kwargs))

petringa/api/results.py::SimResult(config, sim_type, x, y, metadata={}, mesh=None) (frozen dataclass)
petringa/api/device.py::DeviceConfig — a dataclass; dataclasses.replace raises TypeError on unknown field
(fields include epi_thickness_um, N_D_junction, N_D_bulk, L_transition_um, T, area_cm2, half_width_um)

Security: dataclasses.replace(cfg, **{param: value}) raises TypeError on an unknown field —
NEVER use setattr/getattr/eval (RESEARCH Security V5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — ParametricSweep unit test with fake sim_fn (RED)</name>
  <files>tests/test_api_sweep.py</files>
  <read_first>
    - tests/test_api_device.py (fast plain-assert unit style)
    - .planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-RESEARCH.md (the "ParametricSweep unit test with fake sim_fn" code example)
    - .planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-VALIDATION.md (LIB-07 test map)
  </read_first>
  <behavior>
    - test_returns_list_of_correct_length: a ParametricSweep over param="epi_thickness_um", values=[5, 10, 20] with a fake sim_fn returns a list of length 3.
    - test_config_injection: each returned SimResult's config has the swept value injected (e.g. results[i].config.epi_thickness_um == values[i]).
    - test_unknown_param_raises: ParametricSweep(..., param="not_a_field", values=[1]).run() raises TypeError (dataclasses.replace on an unknown field).
    - All FAST — the fake sim_fn returns a canned SimResult; never builds a devsim device.
  </behavior>
  <action>Create `tests/test_api_sweep.py`. Import `numpy as np`, `pytest`, `from petringa import DeviceConfig, ParametricSweep`, and `from petringa.api.results import SimResult`. Define a module-level `def fake_sim(cfg, **kw): return SimResult(config=cfg, sim_type="fake", x=np.array([cfg.epi_thickness_um]), y=np.array([1.0]))`. Write three tests per the behavior block. For `test_config_injection` assert `[r.config.epi_thickness_um for r in results] == [5, 10, 20]`. For `test_unknown_param_raises` use `with pytest.raises(TypeError): ParametricSweep(base_config=DeviceConfig(), param="not_a_field", values=[1], sim_fn=fake_sim).run()`. This is RED — ParametricSweep does not exist yet, so the import fails; expected until Task 2.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_api_sweep.py --collect-only -q 2>&1 | grep -qE "ImportError|cannot import name 'ParametricSweep'" && echo "RED-OK: ParametricSweep not yet defined"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_api_sweep.py` exists and defines a `fake_sim` function returning a `SimResult`.
    - The file contains tests asserting list length == len(values), config injection, and a `pytest.raises(TypeError)` for an unknown param.
    - No test imports `devsim` or calls a real facade — verify: `grep -c "devsim" tests/test_api_sweep.py` returns 0.
    - Collection fails only on the missing `ParametricSweep` import.
  </acceptance_criteria>
  <done>Test file exists with fake-sim_fn unit tests; fails at import because ParametricSweep is not yet defined (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ParametricSweep + re-export (GREEN)</name>
  <files>petringa/api/sweep.py, petringa/__init__.py</files>
  <read_first>
    - petringa/api/results.py (SimResult — for the return type hint / TYPE_CHECKING import pattern)
    - petringa/api/device.py (DeviceConfig — the base_config type)
    - petringa/__init__.py (import + __all__ to extend; note this plan adds a NEW import line for petringa.api.sweep)
    - .planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-PATTERNS.md (ParametricSweep exact implementation)
  </read_first>
  <action>Create `petringa/api/sweep.py`. Use `from __future__ import annotations`; `from dataclasses import dataclass, field, replace`; `from typing import Callable, TYPE_CHECKING`; under `if TYPE_CHECKING:` import `DeviceConfig` and `SimResult` (mirror the results.py TYPE_CHECKING pattern to avoid an import cycle). Define `@dataclass class ParametricSweep` with fields `base_config`, `param: str`, `values: list`, `sim_fn: Callable`, `sim_kwargs: dict = field(default_factory=dict)`. Implement `def run(self) -> list: results = []; for value in self.values: cfg_i = replace(self.base_config, **{self.param: value}); results.append(self.sim_fn(cfg_i, **self.sim_kwargs)); return results`. Use `dataclasses.replace` (NOT setattr/eval) so an unknown `param` raises TypeError (security V5). Add a class docstring showing the design-spec §3.5 usage. Then update `petringa/__init__.py`: add `from petringa.api.sweep import ParametricSweep` and add `"ParametricSweep"` to `__all__`.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_api_sweep.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `petringa/api/sweep.py` exists, defines `@dataclass class ParametricSweep` with a `run` method.
    - `run` uses `replace(self.base_config, **{self.param: value})` — verify: `grep -Ec "replace\(.*self\.param" petringa/api/sweep.py` >= 1. The file contains NO `setattr(` and NO `eval(`.
    - `python -c "from petringa import ParametricSweep"` exits 0 and `"ParametricSweep"` is in `petringa.__all__`.
    - `.venv/bin/pytest tests/test_api_sweep.py -q` passes all three tests (length, injection, TypeError).
  </acceptance_criteria>
  <done>ParametricSweep implemented per spec §3.5 using dataclasses.replace, re-exported from petringa; all three sweep tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                  |
| ------------------------------ | ------------------------------------------------------------ |
| caller → ParametricSweep.param | Caller supplies a field-name string used to clone the config |

## STRIDE Threat Register

| Threat ID       | Category          | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                                       |
| --------------- | ----------------- | ----------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-37-03-V5      | Tampering         | ParametricSweep.param attribute injection | mitigate    | Use `dataclasses.replace(cfg, **{param: value})` which raises `TypeError` on an unknown field. NEVER `setattr`/`getattr`+`eval`. Prevents arbitrary attribute injection into the config.              |
| T-37-03-DoS     | Denial of Service | sim_fn calling devsim facades N times     | accept      | ParametricSweep itself is devsim-agnostic; a real sweep over devsim facades inherits each facade's own cleanup (Plans 01/02). Unit tests use a fake sim_fn — no devsim. Operational, not adversarial. |
| T-37-03-SC      | Tampering         | npm/pip/cargo installs                    | accept      | N/A — zero external packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable).                                                                                                |
| </threat_model> |

<verification>
- `.venv/bin/pytest tests/test_api_sweep.py -q` — all three tests green (fast, no devsim).
- `python -c "from petringa import ParametricSweep"` exits 0.
</verification>

<success_criteria>

- LIB-07 satisfied: `from petringa import ParametricSweep`; `.run()` over 2+ values returns a `list[SimResult]` of correct length with the swept field injected into each config.
- Uses `dataclasses.replace` — TypeError on unknown field, no attribute injection.
- Tests use a fake sim_fn and never build a devsim device.
  </success_criteria>

<output>
Create `.planning/phases/37-core-api-cce-remaining-facades-parametricsweep/37-03-SUMMARY.md` when done.
</output>
