---
phase: 41-radiation-damage-dark-current-pages
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - app/workflows/radiation_damage.py
  - tests/test_app_radiation_damage_page.py
autonomous: true
requirements: [FEAT-01]

must_haves:
  truths:
    - "User sees a persistent kappa-data-blocked warning banner on the radiation damage page even before configuring a device"
    - "User can specify a fluence range (min, max, number of points) and proton energy, click Run, and see a CCE vs fluence Plotly curve"
    - "A NaN in the returned CCE array does not crash the page; it renders as a gap and an informational message appears"
    - "A CSV download button appears after a successful run"
  artifacts:
    - path: "app/workflows/radiation_damage.py"
      provides: "Full render() implementation replacing the Phase 38 placeholder"
      min_lines: 40
    - path: "tests/test_app_radiation_damage_page.py"
      provides: "AppTest coverage for happy path, persistent banner, NaN tolerance, empty state, RuntimeError handling"
  key_links:
    - from: "app/workflows/radiation_damage.py"
      to: "etna.run_radiation_damage"
      via: "module-attribute call (etna.run_radiation_damage(cfg, ...))"
      pattern: "etna\\.run_radiation_damage\\("
    - from: "app/workflows/radiation_damage.py"
      to: "app/components/results.py::build_damage_figure"
      via: "import + st.plotly_chart call"
      pattern: "build_damage_figure"
    - from: "app/workflows/radiation_damage.py"
      to: "app/components/results.py::to_csv_bytes"
      via: "import + st.download_button call"
      pattern: "to_csv_bytes"
---

<objective>
Replace the Phase 38 placeholder body of `app/workflows/radiation_damage.py` with a full
Run -> cache -> render -> download page implementing FEAT-01: fluence-range + proton-energy
widget inputs, a call to `etna.run_radiation_damage()`, a persistent kappa-data-blocked
warning banner, NaN-tolerant rendering of the CCE-vs-fluence curve via the
`build_damage_figure` builder from Plan 41-01, and CSV download via `to_csv_bytes`.

Purpose: Deliver the first of the two FEAT-01/FEAT-02 UI-wiring deliverables for Phase 41,
mirroring the exact Run->cache->render->download skeleton already proven in
`app/workflows/cce.py`/`cv.py` (Phase 39), extended with the two silent-partial-failure
tolerances (NaN-in-array) that RESEARCH.md's live-devsim verification found specific to
`run_radiation_damage`.

Output: `app/workflows/radiation_damage.py` fully implemented; `tests/test_app_radiation_damage_page.py`
created with AppTest coverage mirroring `tests/test_app_field_page.py`'s structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-RESEARCH.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-UI-SPEC.md
@.planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md
@app/workflows/cce.py
@app/workflows/radiation_damage.py
@app/components/results.py
@tests/test_app_field_page.py
@tests/test_app_run_mockability.py
</context>

<interfaces>
<!-- etna.api.simulation.run_radiation_damage exact signature (etna/api/simulation.py:447) -->
```python
def run_radiation_damage(
    config: DeviceConfig,
    fluences: "np.ndarray | None" = None,   # default np.geomspace(1e13, 1e16, 6)
    V_bias: float = -40.0,
    proton_energy_MeV: float = 5.6,
) -> SimResult:
    # Returns: sim_type="damage", x=fluences (cm^-2), y=CCE values (float array,
    # may contain np.nan mid-array — verified empirically, no exception raised),
    # metadata={"V_bias": float, "energy_MeV": float}.
```

<!-- app/components/results.py (Plan 41-01 output) — this plan's page imports these directly -->

```python
def build_damage_figure(result: SimResult) -> go.Figure: ...
def to_csv_bytes(result: SimResult) -> bytes: ...  # dispatches on result.sim_type == "damage"
```

<!-- Reference skeleton: app/workflows/cce.py render() — copy this shape exactly:
     empty-state guard -> 1D-only guard -> Run button -> try/except RuntimeError ->
     session_state cache -> truncated-metadata check -> figure + CSV download -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement app/workflows/radiation_damage.py render()</name>
  <files>app/workflows/radiation_damage.py</files>
  <read_first>
    app/workflows/radiation_damage.py (current placeholder — full file), app/workflows/cce.py
    (full file — the exact Run->cache->render->download skeleton to replicate),
    app/components/results.py (after Plan 41-01 — build_damage_figure signature and to_csv_bytes
    "damage" branch), .planning/phases/41-radiation-damage-dark-current-pages/41-UI-SPEC.md
    (Copywriting Contract: exact banner text, widget labels/defaults/format strings; Plot
    Contract: build_damage_figure usage), .planning/phases/41-radiation-damage-dark-current-pages/41-01-SPIKE-NOTES.md
    (confirmed V_bias default — use the spike-confirmed value if it differs from UI-SPEC's -20.0)
  </read_first>
  <behavior>
    - render() places an unconditional `st.warning(...)` with the kappa-data-blocked banner text
      BEFORE the `cfg is None` empty-state guard, so it is visible on every page load regardless
      of device-config or run state.
    - render() shows `st.info("Configure a device in the sidebar to begin.")` + `st.stop()` when
      `st.session_state.get("device_config")` is None (after the banner).
    - render() shows the 1D-only guard (`cfg.half_width_um is not None` -> `st.warning(...)` +
      `st.stop()`) after the empty-state guard, per RESEARCH.md Pitfall 5's defensive
      recommendation (neither run_radiation_damage nor run_dark_current has a documented 2D
      guard; add the same defensive pre-check `cv.py`/`cce.py` use).
    - Widget inputs (in this order, via `st.columns(3)` for the first three): `fluence_min`
      (`st.number_input`, format="%.3e", value=1e13), `fluence_max` (`st.number_input`,
      format="%.3e", value=1e16), `n_points` (`st.number_input`, min_value=2, step=1, value=6);
      then `proton_energy_MeV` (`st.selectbox([30, 62, 70, 150], index=1)`); then `V_bias`
      (`st.number_input`, value=-20.0, or the spike-confirmed value from 41-01-SPIKE-NOTES.md if
      different). Every widget has an explicit `key=` (per STATE.md Phase 38 lesson on widget
      key collisions) — use keys "rad_fluence_min", "rad_fluence_max", "rad_n_points",
      "rad_proton_energy", "rad_v_bias".
    - `st.button("Run simulation")` click constructs `fluences = np.geomspace(fluence_min,
      fluence_max, int(n_points))` and calls `etna.run_radiation_damage(cfg,
      fluences=fluences, V_bias=V_bias, proton_energy_MeV=proton_energy_MeV)` inside a
      `try/except RuntimeError` block identical in wording to `cce.py`'s, storing the result at
      `st.session_state["damage_result"]` on success.
    - After the button block: if `st.session_state.get("damage_result")` is not None, check
      `np.any(np.isnan(result.y))` and if true show the exact Partial-failure NaN info message
      from the UI-SPEC Copywriting Contract BEFORE the chart. Then render
      `st.plotly_chart(build_damage_figure(result))` and the CSV download button with
      `file_name="radiation_damage_result.csv"`.
  </behavior>
  <action>
    Rewrite app/workflows/radiation_damage.py's render() function per the &lt;behavior&gt; block
    above. Import `numpy as np`, `etna` (module-level, NOT `from etna import
    run_radiation_damage` — must remain monkeypatchable as `etna.run_radiation_damage`), and
    `from app.components.results import build_damage_figure, to_csv_bytes`. Keep the module
    docstring updated to describe the new render() shape (mirror `cce.py`'s docstring wording
    about module-attribute referencing for mockability). Use the exact kappa banner text and 1D
    guard text from the UI-SPEC Copywriting Contract table verbatim (do not paraphrase — FEAT-01
    criterion 2 requires the specific data-blocked/Phi_crit wording).
  </action>
  <acceptance_criteria>
    - `grep -n "etna.run_radiation_damage" app/workflows/radiation_damage.py` finds the call as a module-attribute reference (not `from etna import run_radiation_damage`)
    - `grep -n "st.warning" app/workflows/radiation_damage.py` shows the kappa banner call occurs textually before the `st.session_state.get("device_config")` check in the file
    - `grep -c "data-blocked" app/workflows/radiation_damage.py` returns at least 1
    - `grep -n "build_damage_figure\|to_csv_bytes" app/workflows/radiation_damage.py` shows both imported and called
    - `python -c "import ast; ast.parse(open('app/workflows/radiation_damage.py').read())"` succeeds
  </acceptance_criteria>
  <done>app/workflows/radiation_damage.py fully implements the Run->cache->render->download flow with persistent banner, 1D guard, NaN tolerance, and CSV download; no placeholder text remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppTest coverage for the radiation damage page</name>
  <files>tests/test_app_radiation_damage_page.py</files>
  <read_first>
    tests/test_app_field_page.py (full file — exact AppTest fixture/assertion pattern:
    _fake_run_x functions, AppTest.from_function wrapper, at.button[0].click(), at.exception,
    at.warning, at.info, at.error assertions), tests/test_app_run_mockability.py (monkeypatch
    seam proof), app/workflows/radiation_damage.py (after Task 1 — exact session_state keys and
    widget keys used)
  </read_first>
  <behavior>
    - Test 1 (`test_kappa_banner_persistent`): with NO `device_config` in session_state, run the
      page and assert the kappa-data-blocked banner text (a distinctive substring like
      "data-blocked") appears in `at.warning` even though the empty-state `st.info` guard also
      fires — proving the banner renders before the `st.stop()` guard.
    - Test 2 (`test_empty_state_guard`): with no device_config, assert
      "Configure a device in the sidebar to begin." appears in `at.info`.
    - Test 3 (`test_run_caches_damage_result`): monkeypatch `etna.run_radiation_damage` with
      a fake returning a clean 6-element SimResult (sim_type="damage", no NaN); seed
      `device_config = DeviceConfig()`; click the Run button; assert no exception, assert
      `at.session_state["damage_result"].sim_type == "damage"` and `len(...x) == 6` (proves the
      fake ran, not a real devsim solve).
    - Test 4 (`test_nan_in_result_does_not_crash`): monkeypatch with the RESEARCH.md-documented
      NaN fixture (x=6 fluences, y with one np.nan mid-array, per RESEARCH.md's
      `_fake_run_radiation_damage_with_nan` example); click Run; assert no exception and assert
      an `st.info` message mentioning "did not converge" (or the UI-SPEC's exact partial-failure
      copy) appears.
    - Test 5 (`test_solver_convergence_failure_shows_error_not_crash`): monkeypatch to raise
      `RuntimeError("...")`; click Run; assert no exception, `len(at.error) >= 1`, and
      `"damage_result" not in at.session_state`.
    - Test 6 (`test_2d_config_shows_1d_only_warning`): seed `device_config =
      DeviceConfig(half_width_um=50.0)`; run; assert a warning containing "1D-only" appears and
      no Run button interaction is needed (page stops before reaching the button).
  </behavior>
  <action>
    Create tests/test_app_radiation_damage_page.py mirroring tests/test_app_field_page.py's
    structure exactly: module docstring explaining the monkeypatch-module-attribute seam, a
    `_run_radiation_damage_page()` wrapper importing and calling
    `app.workflows.radiation_damage.render`, and the 6 test functions described in the
    &lt;behavior&gt; block, each using `monkeypatch.setattr(etna, "run_radiation_damage",
    fake)` before `AppTest.from_function(...).run()`.
  </action>
  <acceptance_criteria>
    <automated>uv run pytest tests/test_app_radiation_damage_page.py -q</automated>
    - All 6 tests pass
    - `grep -c "monkeypatch.setattr(etna, \"run_radiation_damage\"" tests/test_app_radiation_damage_page.py` returns at least 4 (one per test requiring a fake facade call)
  </acceptance_criteria>
  <done>tests/test_app_radiation_damage_page.py exists with 6 passing AppTest cases covering the happy path, persistent banner, NaN tolerance, RuntimeError handling, empty state, and 2D guard.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                                                    |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Streamlit widget input -> run_radiation_damage kwargs | User-entered fluence_min/max, n_points, proton_energy_MeV, V_bias flow into `np.geomspace` and the facade call |
| run_radiation_damage return -> rendered chart/CSV     | NaN-embedded arrays from the facade must render without crashing the page or corrupting the CSV                |

## STRIDE Threat Register

| Threat ID       | Category                  | Component                                                                    | Disposition | Mitigation Plan                                                                                                                                                           |
| --------------- | ------------------------- | ---------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-03         | Denial of Service (local) | `n_points` widget -> `np.geomspace(fluence_min, fluence_max, int(n_points))` | mitigate    | `st.number_input(min_value=2, step=1)` on `n_points` prevents a degenerate/empty geomspace call (Streamlit enforces client-side)                                          |
| T-41-04         | Information Disclosure    | Persistent kappa warning banner                                              | accept      | This is an intentional, required disclosure (FEAT-01 criterion 2) — not a security concern, listed here only because it is a "banner" pattern; no sensitive data involved |
| T-41-05         | Denial of Service (local) | NaN-embedded `result.y` reaching `build_damage_figure`/`to_csv_bytes`        | mitigate    | Plotly natively gaps NaN values (no crash); page explicitly checks `np.any(np.isnan(result.y))` and surfaces an `st.info` rather than silently guessing at the cause      |
| T-41-SC         | Tampering                 | npm/pip/cargo installs                                                       | accept      | No new package installs in this plan                                                                                                                                      |
| </threat_model> |

<verification>
Run `uv run pytest tests/test_app_radiation_damage_page.py -q` (per-file isolation convention)
and confirm all 6 tests pass. Manually confirm via `python -c "import ast;
ast.parse(open('app/workflows/radiation_damage.py').read())"` that the file has no syntax errors.
</verification>

<success_criteria>

- `app/workflows/radiation_damage.py` implements the full Run->cache->render->download flow
  satisfying FEAT-01 and ROADMAP Phase 41 Success Criteria 1 and 2
- `tests/test_app_radiation_damage_page.py` has 6 passing AppTest cases
- CSV download button present after a successful run, wired to `to_csv_bytes`
  </success_criteria>

<output>
Create `.planning/phases/41-radiation-damage-dark-current-pages/41-02-SUMMARY.md` when done
</output>
