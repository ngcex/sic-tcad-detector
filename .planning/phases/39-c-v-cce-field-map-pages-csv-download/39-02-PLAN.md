---
phase: 39-c-v-cce-field-map-pages-csv-download
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - app/components/results.py
  - tests/test_app_csv_export.py
autonomous: true
requirements: [UI-03, UI-04, UI-05, UI-06]
must_haves:
  truths:
    - "A pure to_csv_bytes(result) turns any SimResult into downloadable CSV bytes with a commented metadata header and the correct columns for its sim_type"
    - "Pure Plotly builders turn a SimResult into go.Figure objects for C-V, Mott-Schottky, CCE, E-field, and potential without any st.* calls"
  artifacts:
    - path: "app/components/results.py"
      provides: "Shared Plotly figure builders + to_csv_bytes serializer, all pure (no st.*)"
      exports:
        [
          "build_cv_figure",
          "build_mott_schottky_figure",
          "build_cce_figure",
          "build_field_figures",
          "to_csv_bytes",
        ]
      min_lines: 60
    - path: "tests/test_app_csv_export.py"
      provides: "Pure unit test of to_csv_bytes across all three sim_types + metadata header (no Streamlit, no devsim)"
      contains: "to_csv_bytes"
  key_links:
    - from: "app/components/results.py"
      to: "etna.__version__"
      via: "metadata header line software_version"
      pattern: "__version__"
    - from: "tests/test_app_csv_export.py"
      to: "app.components.results.to_csv_bytes"
      via: "direct import (pure function, no AppTest)"
      pattern: "from app.components.results import"
---

<objective>
Build the shared `app/components/results.py` module ONCE (not duplicated per
page): pure Plotly `go.Figure` builders for every chart the three pages render,
plus a `to_csv_bytes(result: SimResult) -> bytes` serializer that dispatches on
`result.sim_type` to produce the correct columns and prepends a commented `#`
traceability metadata header. Cover the serializer with a pure unit test across
all three sim_types (the only place CSV content can be asserted — AppTest 1.55
has no `download_button` accessor).

Purpose: These are the load-bearing contracts consumed by plans 39-03 and 39-04.
Defining the exact signatures here first lets the page executors build against a
blueprint with zero codebase scavenger-hunting. All functions are PURE (no `st.*`)
so the serializer is testable without a Streamlit runtime or devsim.
Output: `app/components/results.py`, `tests/test_app_csv_export.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-RESEARCH.md
@.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-PATTERNS.md
@app/components/device_sidebar.py
@tests/test_app_device_sidebar.py

<interfaces>
<!-- Contracts THIS plan defines. Plans 39-03 and 39-04 consume these verbatim. -->
<!-- SimResult envelope (etna/api/results.py) — do NOT redefine, import it: -->
<!--   SimResult(config: DeviceConfig, sim_type: str, x: np.ndarray, y: np.ndarray, metadata: dict, mesh: MeshData|None) -->

Signatures to EXPORT from app/components/results.py (all pure, no st.* calls):

build_cv_figure(result: SimResult) -> go.Figure
C-V curve: x = result.x (bias V), y = result.y (capacitance F).
build_mott_schottky_figure(result: SimResult) -> go.Figure
1/C^2 vs V: x = result.x, y = result.metadata["one_over_C_squared"].
build_cce_figure(result: SimResult) -> go.Figure
CCE vs |bias|: x = np.abs(result.x), y = result.y (CCE in [0,1]).
build_field_figures(result: SimResult) -> tuple[go.Figure, go.Figure]
Returns (efield_fig, potential_fig). Depth axis = result.x (ALREADY µm).
efield y = result.y (V/cm); potential y = result.metadata["potential"] (V).
to_csv_bytes(result: SimResult) -> bytes
Dispatches on result.sim_type -> "cv" | "cce" | "field". Returns UTF-8
encoded CSV bytes with a leading commented "#" metadata header.
</interfaces>
</context>

<assumptions>
Adopted from RESEARCH.md §4 (no CONTEXT.md — all tagged [ASSUMED], listed for user review):
- A1 CSV column names per sim_type (see task actions — verbatim).
- A2 Commented "#" metadata header format (re-reading needs pandas.read_csv(comment='#')).
- A3 Header carries ISO-8601 UTC timestamp + etna.__version__ + full asdict(config).
- CCE I_generated may be a scalar: DECISION → place I_generated in the metadata
  header (not a broadcast column); I_collected is a node/bias-aligned array column.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Plotly figure builders in results.py</name>
  <read_first>
    - app/components/device_sidebar.py (the "pure seam + thin Streamlit wrapper" module structure to mirror; note `from __future__ import annotations` header style)
    - etna/core/plotting.py lines 36-68, 339-395, 472-517 (matplotlib reference — SOURCE OF TRUTH for axis labels, titles, unit conversions, reference lines. Do NOT import this file; it is matplotlib and not usable in st.plotly_chart)
    - etna/api/results.py (SimResult / MeshData field names)
    - etna/api/simulation.py lines 106-119 (run_cv metadata keys), 401-411 (run_cce metadata keys), 279-306 (run_field x=µm, metadata potential/net_doping)
  </read_first>
  <files>app/components/results.py</files>
  <action>
    Create `app/components/results.py` starting with `from __future__ import annotations`,
    `import numpy as np`, `import plotly.graph_objects as go`, and
    `from etna import SimResult` (type hints only; DeviceConfig reached via
    result.config). Implement the five pure builder functions — NO `st.*` calls anywhere:

    build_cv_figure(result): go.Scatter of result.y (capacitance F) vs result.x
      (bias V). Layout: title "C-V Characteristic", xaxis "Voltage (V)", yaxis
      "Capacitance (F/cm²)".
    build_mott_schottky_figure(result): go.Scatter of
      result.metadata["one_over_C_squared"] vs result.x. Layout: title
      "Mott-Schottky Plot (1/C² vs V)", xaxis "Voltage (V)", yaxis "1/C² (cm⁴/F²)".
    build_cce_figure(result): go.Scatter of result.y vs np.abs(result.x). Add a
      horizontal reference line at y=1.0 (use fig.add_hline(y=1.0) or a dashed
      trace). yaxis range [0, 1.1]. Layout: title "CCE vs Reverse Bias", xaxis
      "|Reverse Bias| (V)", yaxis "Charge Collection Efficiency".
    build_field_figures(result): returns a tuple (efield_fig, potential_fig).
      efield_fig: go.Scatter of result.y (E-field V/cm) vs result.x. result.x is
      ALREADY in µm (run_field multiplies by 1e4 internally) — DO NOT multiply by
      1e4 again (RESEARCH Pitfall 3). Layout title "Electric Field vs Depth",
      xaxis "Depth (µm)", yaxis "Electric Field (V/cm)".
      potential_fig: go.Scatter of result.metadata["potential"] (V) vs result.x.
      Layout title "Electrostatic Potential vs Depth", xaxis "Depth (µm)", yaxis
      "Potential (V)".

    Keep every function a pure transform: input SimResult, output go.Figure(s),
    no session_state, no st.* — this is required so the module imports cleanly in
    a pure unit test without a Streamlit runtime.

  </action>
  <verify>
    <automated>uv run python -c "from app.components.results import build_cv_figure, build_mott_schottky_figure, build_cce_figure, build_field_figures; import plotly.graph_objects as go; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/results.py` exports build_cv_figure, build_mott_schottky_figure, build_cce_figure, build_field_figures
    - Each builder returns a `plotly.graph_objects.Figure` (import succeeds; a smoke construction from a hand-built SimResult returns a go.Figure instance)
    - `grep -c "import streamlit" app/components/results.py` returns 0 — the module has NO Streamlit import (builders and serializer are pure)
    - build_field_figures returns a 2-tuple (asserted in Task 2 test fixture)
    - The E-field builder does NOT contain `* 1e4` or `*1e4` applied to result.x (verify by grep: `grep -n "1e4" app/components/results.py` shows no depth-axis multiply)
  </acceptance_criteria>
  <done>Five pure Plotly builders exist and import without a Streamlit runtime; E-field depth axis uses result.x directly (µm).</done>
</task>

<task type="auto">
  <name>Task 2: to_csv_bytes serializer + pure unit test</name>
  <read_first>
    - app/components/results.py (the module Task 1 created — append to it)
    - tests/test_app_device_sidebar.py (the pure-function unit-test shape to mirror: direct import of a Streamlit-free seam, no AppTest, no server)
    - etna/_version.py (etna.__version__ == "5.0.0" for the header)
    - etna/api/simulation.py lines 106-119, 401-411, 279-306 (exact metadata keys per sim_type)
    - etna/api/simulation.py — the run_cce body UPSTREAM of its return (where I_collected and I_generated are computed): read it to CONFIRM whether each is an array or a scalar before finalizing the CCE column/header split (do NOT run devsim to check — read the source)
  </read_first>
  <files>app/components/results.py, tests/test_app_csv_export.py</files>
  <action>
    Append `to_csv_bytes(result: SimResult) -> bytes` to `app/components/results.py`.
    Use `import pandas as pd`, `from dataclasses import asdict`, `import etna`
    (for `etna.__version__`), `from datetime import datetime, timezone`. The
    function is PURE (no st.*, no temp file — build bytes in memory).

    Column schemas (dispatch on result.sim_type — use these EXACT header names):
      "cv":    bias_V, capacitance_F, one_over_C2_cm4_per_F2, depletion_width_cm
               (from result.x, result.y, metadata["one_over_C_squared"], metadata["depletion_widths"])
      "cce":   bias_V, CCE, I_collected_A_per_cm2
               (from result.x, result.y, metadata["I_collected"]). Do NOT add an
               I_generated column — RESEARCH flags I_generated as possibly scalar;
               place it in the metadata header (see below). BEFORE finalizing this
               split, confirm the actual shapes by reading the run_cce body upstream
               of its return in etna/api/simulation.py: if I_collected is a
               scalar (not a bias-aligned array), it too must move to the metadata
               header rather than becoming a misleading broadcast column; if
               I_generated is actually an array, it needs an array-safe header
               representation, not a single scalar line. Build the test fixture in
               tests/test_app_csv_export.py to match the confirmed shapes.
      "field": depth_um, ElectricField_V_per_cm, Potential_V, NetDoping_cm-3
               (from result.x (already µm), result.y, metadata["potential"], metadata["net_doping"])
    Build the table with `pd.DataFrame({...}).to_csv(index=False)`.

    Prepend a commented metadata header (each line starts with "# "):
      # etna SiC TCAD Simulator — <result.sim_type> result
      # software_version: <etna.__version__>
      # generated: <ISO-8601 UTC, e.g. datetime.now(timezone.utc).isoformat()>
      # device: <every field of asdict(result.config) as key=value, comma-joined>
      For "cce" only, add: # I_generated_A_per_cm2: <result.metadata["I_generated"]>
    Concatenate header + "\n" + df_csv, then `.encode("utf-8")` and return bytes.
    Guard the sim_type dispatch: raise ValueError for an unknown sim_type.

    Then create `tests/test_app_csv_export.py` mirroring test_app_device_sidebar.py
    (pure import, no AppTest). Import `from app.components.results import to_csv_bytes`
    and `from etna import DeviceConfig, SimResult`, `import numpy as np`. Write
    one test per sim_type building an in-memory SimResult fixture and asserting on
    the decoded bytes:
      - cv: header contains "# software_version: 5.0.0", first non-comment line is
        exactly "bias_V,capacitance_F,one_over_C2_cm4_per_F2,depletion_width_cm";
        a data row matches the fixture values; decoded via
        pandas.read_csv(StringIO(text), comment='#') round-trips 4 columns.
      - cce: column line is "bias_V,CCE,I_collected_A_per_cm2"; header contains a
        "# I_generated_A_per_cm2:" line; no I_generated data column present.
      - field: column line is "depth_um,ElectricField_V_per_cm,Potential_V,NetDoping_cm-3".
      - Each: assert the header contains "# device:" with an epi_thickness_um= token
        (proves asdict(config) provenance is embedded).
      - One test asserting an unknown sim_type raises ValueError.

  </action>
  <verify>
    <automated>uv run pytest tests/test_app_csv_export.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `to_csv_bytes` returns `bytes` for sim_type in {"cv","cce","field"} and raises ValueError otherwise
    - CV CSV first non-comment line is exactly `bias_V,capacitance_F,one_over_C2_cm4_per_F2,depletion_width_cm`
    - CCE CSV first non-comment line is exactly `bias_V,CCE,I_collected_A_per_cm2` and there is NO `I_generated` column; a `# I_generated_A_per_cm2:` header line is present
    - Field CSV first non-comment line is exactly `depth_um,ElectricField_V_per_cm,Potential_V,NetDoping_cm-3`
    - Every CSV header contains `# software_version: 5.0.0` and a `# device:` line containing `epi_thickness_um=`
    - `pandas.read_csv(StringIO(decoded), comment='#')` parses each CSV to the expected column count
    - `pytest tests/test_app_csv_export.py -x` exits 0 with a test for each of the three sim_types plus the ValueError case
    - `grep -c "import streamlit" tests/test_app_csv_export.py` returns 0 (pure test, no AppTest, no Streamlit runtime)
  </acceptance_criteria>
  <done>to_csv_bytes serializes all three sim_types with correct columns and a commented traceability header; the pure serializer test passes with no Streamlit or devsim dependency.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                        |
| -------------------- | ---------------------------------------------------------------------------------- |
| SimResult → CSV cell | Numeric arrays + a fixed sim_type literal + hardcoded column headers reach the CSV |

## STRIDE Threat Register

| Threat ID       | Category  | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                         |
| --------------- | --------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-01         | Tampering | `to_csv_bytes` (CSV injection) | accept      | RESEARCH §Security: only numeric config values, a fixed sim_type literal, and hardcoded column headers reach cells — no user-controlled free-text string. Risk LOW. If a string ever enters a cell in a future change, prefix-escape leading `= + - @`. |
| T-39-SC         | Tampering | plotly dependency              | accept      | Materialized in 39-01 via `uv sync` from pre-declared pyproject.toml pin — canonical PyPI package, no new-package decision.                                                                                                                             |
| </threat_model> |

<verification>
- `uv run pytest tests/test_app_csv_export.py -x` passes (all three sim_types + ValueError)
- `uv run python -c "from app.components.results import build_cv_figure, build_cce_figure, build_field_figures, build_mott_schottky_figure, to_csv_bytes"` exits 0
- `grep -c "import streamlit" app/components/results.py tests/test_app_csv_export.py` returns 0 for both (pure modules)
</verification>

<success_criteria>

- Shared results.py exists ONCE with five pure builders + to_csv_bytes
- CSV columns and metadata header match RESEARCH §4 exactly per sim_type
- Serializer fully covered by a pure unit test (the only surface where CSV content is assertable)
  </success_criteria>

<output>
Create `.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-02-SUMMARY.md` when done
</output>
