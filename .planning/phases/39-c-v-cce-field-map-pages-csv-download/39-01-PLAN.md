---
phase: 39-c-v-cce-field-map-pages-csv-download
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_app_run_mockability.py
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "plotly is importable in the project venv"
    - "AppTest + monkeypatch can intercept a call to petringa.run_cv referenced as a module attribute"
  artifacts:
    - path: "tests/test_app_run_mockability.py"
      provides: "Wave 0 spike proving petringa.run_* is mockable via monkeypatch under AppTest.from_function"
      contains: "monkeypatch.setattr"
  key_links:
    - from: "tests/test_app_run_mockability.py"
      to: "petringa.run_cv"
      via: "monkeypatch.setattr(petringa, 'run_cv', fake)"
      pattern: "monkeypatch\\.setattr\\(petringa"
---

<objective>
Establish the two Wave 0 prerequisites that all downstream Phase 39 page plans
depend on: (1) materialize the already-declared `plotly` dependency into the
venv so page imports and every AppTest do not break, and (2) prove empirically
that the `petringa.run_cv`-as-module-attribute reference style is interceptable
by `monkeypatch` under `AppTest.from_function`. This spike DICTATES the page
import structure adopted in plans 39-03 and 39-04 (call `petringa.run_cv(cfg)`,
never `from petringa import run_cv`).

Purpose: Without plotly installed, every existing and new AppTest fails at
import (RESEARCH Pitfall 2). Without a proven mock seam, page tests would hit
real, expensive devsim solves. Both are hard blockers for the phase.
Output: `plotly` in the venv; `tests/test_app_run_mockability.py` passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-RESEARCH.md
@tests/test_app_pages.py
</context>

<assumptions>
Adopted from RESEARCH.md (no CONTEXT.md exists — user skipped discuss-phase):
- A6: pages reference facades as `petringa.run_cv` (module attribute) rather than
  `from petringa import run_cv`, so tests can monkeypatch the module attribute.
  This spike is the empirical confirmation of A6 before page code is written.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Materialize plotly into the venv via uv</name>
  <read_first>
    - pyproject.toml (confirm `plotly>=5.0` is already declared in dependencies — this is `uv sync`, NOT `uv add`)
  </read_first>
  <files>(no source files — dependency materialization only)</files>
  <action>
    Run `uv sync` to install the already-declared `plotly>=5.0` dependency into
    the project venv. This project uses uv for dependency management — do NOT use
    `pip install` or `python -m venv` (persistent project constraint). Because
    `plotly>=5.0` is ALREADY present in `pyproject.toml` from a prior phase, this
    is a materialization of an existing pinned dependency, not the introduction of
    a new package — use `uv sync`, not `uv add plotly`. After syncing, confirm the
    import resolves. Do NOT edit pyproject.toml.
  </action>
  <verify>
    <automated>uv run python -c "import plotly; import plotly.graph_objects as go; print(plotly.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import plotly.graph_objects"` exits 0 (no ModuleNotFoundError)
    - `pyproject.toml` still contains `plotly>=5.0` and was NOT modified by this task (no `uv add` line rewrite)
    - `git diff --stat pyproject.toml` shows no change
  </acceptance_criteria>
  <done>plotly and plotly.graph_objects import cleanly in the project venv; pyproject.toml unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Spike — prove petringa.run_cv is mockable under AppTest.from_function</name>
  <read_first>
    - tests/test_app_pages.py (the `test_empty_state_guard` shape at lines 78-90: `AppTest.from_function` wrapper whose body imports render INSIDE itself)
    - petringa/__init__.py (confirms `run_cv` is a top-level module attribute of `petringa`)
    - petringa/api/results.py (SimResult fields: config, sim_type, x, y, metadata, mesh)
  </read_first>
  <files>tests/test_app_run_mockability.py</files>
  <action>
    Create `tests/test_app_run_mockability.py` with a single spike test that
    proves the mock seam RESEARCH A6 depends on. The test must:
    (1) Define a fake `fake_run_cv(cfg, **kwargs)` returning a hand-built
    `SimResult(config=cfg, sim_type="cv", x=np.array([0.0, -10.0]),
    y=np.array([1e-9, 2e-9]), metadata={"one_over_C_squared": np.array([1e18, 2.5e17]),
    "depletion_widths": np.array([1e-4, 2e-4]), "area_cm2": 1e-4}, mesh=None)`.
    (2) `monkeypatch.setattr(petringa, "run_cv", fake_run_cv)` — patching the
    MODULE ATTRIBUTE on the `petringa` module, confirming that a caller who does
    `import petringa; petringa.run_cv(cfg)` will resolve to the fake.
    (3) Inside a self-contained `AppTest.from_function` wrapper body (imports
    inside the body per the AppTest.from_function contract), call
    `petringa.run_cv(DeviceConfig())` and stash the result under
    `st.session_state["cv_result"]`.
    (4) Assert `at.exception == []` and that `at.session_state["cv_result"].sim_type == "cv"`
    with `x` length 2 — proving the fake (not real devsim) was invoked.
    Use `import petringa`, `import numpy as np`, `from petringa import DeviceConfig`,
    `from streamlit.testing.v1 import AppTest`. Note the monkeypatch fixture must be
    applied at the test-function level (pytest passes `monkeypatch` as an argument).
    Because `AppTest.from_function` runs the wrapper in the same process, the
    module-attribute patch is visible to the wrapper's `petringa.run_cv` lookup.
  </action>
  <verify>
    <automated>uv run pytest tests/test_app_run_mockability.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_app_run_mockability.py` contains the literal `monkeypatch.setattr(petringa, "run_cv"` seam
    - Test passes: `pytest tests/test_app_run_mockability.py -x` exits 0
    - The test references the facade as `petringa.run_cv(...)` (module-attribute style), NOT `from petringa import run_cv`
    - Test asserts `at.exception == []` and `at.session_state["cv_result"].sim_type == "cv"`
    - No real devsim solve occurs (the fake returns a 2-point array; a real run_cv would return n_points=40 by default) — assert `len(at.session_state["cv_result"].x) == 2`
  </acceptance_criteria>
  <done>Spike test passes, empirically confirming RESEARCH A6: `petringa.run_*` referenced as a module attribute is interceptable by monkeypatch under AppTest. Page plans 39-03/39-04 MUST use `import petringa; petringa.run_*(cfg)`.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary    | Description                                                          |
| ----------- | -------------------------------------------------------------------- |
| PyPI → venv | `uv sync` materializes a pre-declared dependency into the local venv |

## STRIDE Threat Register

| Threat ID       | Category  | Component          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                            |
| --------------- | --------- | ------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-SC         | Tampering | `uv sync` (plotly) | accept      | `plotly>=5.0` is a canonical PyPI package ALREADY declared in pyproject.toml from a prior phase — this materializes an existing pinned/vetted dependency, not a new-package introduction. No `## Package Legitimacy Audit` needed because no new package decision is made. |
| </threat_model> |

<verification>
- `uv run python -c "import plotly.graph_objects"` exits 0
- `uv run pytest tests/test_app_run_mockability.py -x` passes
</verification>

<success_criteria>

- plotly importable in the venv (all downstream AppTests unblocked)
- Mockability of `petringa.run_*` empirically proven; page import structure decided
  </success_criteria>

<output>
Create `.planning/phases/39-c-v-cce-field-map-pages-csv-download/39-01-SUMMARY.md` when done
</output>
