---
phase: 36-core-api-deviceconfig-c-v-field-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - petringa/api/__init__.py
  - petringa/api/results.py
  - petringa/api/device.py
  - petringa/__init__.py
autonomous: true
requirements: [LIB-01]
must_haves:
  truths:
    - "Developer can `from petringa import DeviceConfig` and instantiate with geometry, doping, and operating-condition named parameters"
    - "Developer can `from petringa import SimResult, MeshData` (dataclasses importable)"
    - "build_device(cfg) creates a devsim 1D device when half_width_um is None and a 2D device when half_width_um is set, passing every DeviceConfig field through with correct um->cm conversion"
    - "Both branches of build_device return a DD-initialized device (dd_initialized=True) so downstream ramp_bias can ramp either dimension"
  artifacts:
    - path: "petringa/api/results.py"
      provides: "SimResult and MeshData dataclasses per design spec sections 3.2 and 3.3"
      contains: "class SimResult"
    - path: "petringa/api/device.py"
      provides: "Canonical DeviceConfig dataclass + build_device() facade"
      contains: "def build_device"
    - path: "petringa/__init__.py"
      provides: "Public re-exports of DeviceConfig, SimResult, MeshData"
      contains: "from petringa.api.device import DeviceConfig"
  key_links:
    - from: "petringa/api/device.py::build_device"
      to: "petringa/core/device.py::create_sic_device"
      via: "1D branch (half_width_um is None), um->cm conversion on epi/substrate thickness and L_transition"
      pattern: "create_sic_device"
    - from: "petringa/api/device.py::build_device"
      to: "petringa/core/device2d.py::create_sic_2d_device"
      via: "2D branch (half_width_um set), half_width_um passed in um, thicknesses in cm, then setup_poisson + solve_equilibrium + setup_sic_drift_diffusion for DD initialization"
      pattern: "create_sic_2d_device"
    - from: "petringa/api/device.py::build_device"
      to: "petringa/core/drift_diffusion.py::setup_sic_drift_diffusion"
      via: "2D branch DD initialization (mirrors charge_collection_2d.py:140-157), sets dd_initialized=True"
      pattern: "setup_sic_drift_diffusion"
    - from: "petringa/__init__.py"
      to: "petringa/api/device.py"
      via: "re-export"
      pattern: "from petringa.api.device import DeviceConfig"
---

<objective>
Establish the public API contracts for Phase 36: promote the `DeviceConfig` stub to its canonical home `petringa/api/device.py`, add the `SimResult` and `MeshData` dataclasses in `petringa/api/results.py`, and implement the shared `build_device()` facade that both `run_cv()` (Plan 02) and `run_field()` (Plan 03) consume. This is the interface-first wave: it defines the exact types downstream plans build against, so no executor has to explore the codebase to learn the contract.

Purpose: LIB-01 — a developer can import and instantiate `DeviceConfig` from `petringa` with named parameters covering geometry, doping profile (uniform or graded), and operating conditions.
Output: `petringa/api/` package with `device.py`, `results.py`, `__init__.py`; updated top-level `petringa/__init__.py` re-exporting the public contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contracts this plan MUST produce, extracted verbatim from the design spec. -->
<!-- Downstream Plans 02 and 03 depend on these exact field names and signatures. -->

From docs/superpowers/specs/2026-06-26-simulator-library-ui-design.md section 3.1 (DeviceConfig fields + defaults):
epi_thickness_um=10.0, substrate_thickness_um=1.0, half_width_um=None,
N_A=1e19, doping_profile="graded", N_D=None, N_D_junction=2.93e15,
N_D_bulk=8.82e13, L_transition_um=0.987, T=300.0, area_cm2=1e-4

From design spec section 3.2 (SimResult):
config: DeviceConfig; sim_type: str; x: np.ndarray; y: np.ndarray;
metadata: dict; mesh: MeshData | None

From design spec section 3.3 (MeshData):
x_coords: np.ndarray; y_coords: np.ndarray | None;
node_values: dict[str, np.ndarray]; regions: list[dict]; contacts: list[dict]

From petringa/core/device.py::create_sic_device signature (units are CGS: cm):
create_sic_device(device_name, region_name, epi_thickness_cm=10e-4,
substrate_thickness_cm=1e-4, N_A=1e19, N_D=None, T=300,
doping_profile="uniform", N_D_junction=None, N_D_bulk=None, L_transition=None)

From petringa/core/device2d.py::create_sic_2d_device signature (half_width in um, thicknesses in cm):
create_sic_2d_device(device_name, region_name, half_width_um=50.0,
epi_thickness_cm=10e-4, substrate_thickness_cm=1e-4, N_A=1e19, N_D=None, T=300,
doping_profile="graded", N_D_junction=None, N_D_bulk=None, L_transition=None)

From petringa/core/drift_diffusion.py DD-init helpers (import from petringa.core.drift_diffusion / petringa.core.poisson):
setup_sic_drift_diffusion(device_info) # adds DD equations; must run before ramp_bias
setup_poisson(device_info); solve_equilibrium(device_info) # from petringa.core.poisson
create_dd_device(...) -> device_info # 1D: runs create_sic_device+setup_poisson+solve_equilibrium+setup_sic_drift_diffusion, sets dd_initialized=True (drift_diffusion.py:234-242)

From petringa/core/charge_collection_2d.py:140-157 (idiomatic DD-initialized 2D build to mirror):
create_sic_2d_device(...) -> setup_poisson -> solve_equilibrium -> setup_sic_drift_diffusion -> device_info["dd_initialized"] = True
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SimResult and MeshData dataclasses in petringa/api/results.py</name>
  <read_first>
    - docs/superpowers/specs/2026-06-26-simulator-library-ui-design.md (sections 3.2 and 3.3 — the exact field names and types are the contract; do not rename or reorder)
    - petringa/__init__.py (existing module docstring / __future__ import style to mirror)
  </read_first>
  <files>petringa/api/__init__.py, petringa/api/results.py</files>
  <action>
    Create the `petringa/api/` package. Add `petringa/api/__init__.py` (may be an empty package marker with a one-line docstring "petringa public API facades.").

    In `petringa/api/results.py` define two frozen-optional `@dataclass`es exactly per design spec:

    `MeshData` with fields in this order: `x_coords: np.ndarray`, `y_coords: np.ndarray | None`, `node_values: dict[str, np.ndarray]`, `regions: list[dict]`, `contacts: list[dict]`. Use `from __future__ import annotations` so the `| None` and subscripted generics parse on the project's Python (>=3.13 per pyproject; keep annotations as strings to be safe). Import numpy as `np`.

    `SimResult` with fields in this order: `config`, `sim_type: str`, `x: np.ndarray`, `y: np.ndarray`, `metadata: dict`, `mesh: "MeshData | None" = None`. Type `config` as `"DeviceConfig"` using a `TYPE_CHECKING` import from `petringa.api.device` to avoid a runtime import cycle (results.py must not import device.py at module load). `mesh` defaults to `None`; `metadata` should default to an empty dict via `field(default_factory=dict)`.

    Do NOT import from `petringa` (top-level) anywhere in this file — use `petringa.api.*` / `petringa.core.*` absolute paths only, and TYPE_CHECKING for the DeviceConfig annotation, to prevent an import cycle once `petringa/__init__.py` re-exports these names.

  </action>
  <acceptance_criteria>
    - `python -c "from petringa.api.results import SimResult, MeshData"` exits 0
    - `python -c "from petringa.api.results import MeshData; import dataclasses; f=[x.name for x in dataclasses.fields(MeshData)]; assert f==['x_coords','y_coords','node_values','regions','contacts'], f"` exits 0
    - `python -c "from petringa.api.results import SimResult; import dataclasses; f=[x.name for x in dataclasses.fields(SimResult)]; assert f==['config','sim_type','x','y','metadata','mesh'], f"` exits 0
    - `python -c "from petringa.api.results import SimResult; r=SimResult(config=None, sim_type='cv', x=None, y=None, metadata={}); assert r.mesh is None"` exits 0 (mesh defaults to None)
    - `grep -n 'from petringa import' petringa/api/results.py` returns nothing (no top-level import cycle)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from petringa.api.results import SimResult, MeshData; import dataclasses; assert [x.name for x in dataclasses.fields(MeshData)]==['x_coords','y_coords','node_values','regions','contacts']; assert [x.name for x in dataclasses.fields(SimResult)]==['config','sim_type','x','y','metadata','mesh']"</automated>
  </verify>
  <done>SimResult and MeshData importable from petringa.api.results with the exact field names/order from the design spec; no top-level petringa import in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Promote DeviceConfig to petringa/api/device.py and add build_device() facade</name>
  <read_first>
    - petringa/__init__.py (the existing DeviceConfig stub — copy its field names, defaults, and inline unit comments verbatim; this is the source of truth for the field set)
    - docs/superpowers/specs/2026-06-26-simulator-library-ui-design.md (section 3.1 — confirm defaults match the stub: N_D_junction=2.93e15, N_D_bulk=8.82e13, L_transition_um=0.987)
    - petringa/core/device.py (create_sic_device signature — note ALL thickness kwargs are in cm: epi_thickness_cm, substrate_thickness_cm, L_transition; and its graded defaults 2.9e15/8.5e13/1e-4 DIFFER from DeviceConfig, so every field must be passed explicitly)
    - petringa/core/device2d.py (create_sic_2d_device signature lines 153-166 — half_width_um is passed in MICROMETERS, but epi/substrate thickness and L_transition are still in cm; CONFIRM it only builds mesh+doping — it does NOT run setup_poisson/solve_equilibrium/setup_sic_drift_diffusion, so build_device's 2D branch must add them)
    - petringa/core/charge_collection_2d.py lines 140-157 (the idiomatic DD-initialized 2D build pattern to MIRROR: create_sic_2d_device -> setup_poisson -> solve_equilibrium -> setup_sic_drift_diffusion -> device_info["dd_initialized"]=True. Copy this exact ordering for build_device's 2D branch)
    - petringa/core/drift_diffusion.py lines 234-242 (create_dd_device — the 1D reference: it ALSO runs setup_sic_drift_diffusion and sets dd_initialized=True, so a truly "consistent with the 1D branch" 2D branch must do the same) and lines 245-255 (ramp_bias docstring — precondition: DD equations already set up, dd_initialized=True; a device that skips setup_sic_drift_diffusion CANNOT be ramped by run_field)
  </read_first>
  <files>petringa/api/device.py</files>
  <action>
    Move the `DeviceConfig` dataclass out of `petringa/__init__.py` into `petringa/api/device.py` (this becomes its canonical home per design spec section 2). Preserve every field, default value, type, and inline unit comment exactly as they exist in the stub: `epi_thickness_um=10.0`, `substrate_thickness_um=1.0`, `half_width_um: Optional[float]=None`, `N_A=1e19`, `doping_profile="graded"`, `N_D: Optional[float]=None`, `N_D_junction=2.93e15`, `N_D_bulk=8.82e13`, `L_transition_um=0.987`, `T=300.0`, `area_cm2=1e-4`.

    Add `build_device(config, device_name=None)` in the same module. It dispatches on `config.half_width_um`:

    1D branch (`config.half_width_um is None`): call `petringa.core.drift_diffusion.create_dd_device(...)` (which runs create_sic_device + setup_poisson + solve_equilibrium + setup_sic_drift_diffusion and returns device_info with dd_initialized=True). Map EVERY DeviceConfig field explicitly — never rely on core defaults, because core graded defaults differ:
      - epi_thickness_cm = config.epi_thickness_um * 1e-4
      - substrate_thickness_cm = config.substrate_thickness_um * 1e-4
      - N_A = config.N_A
      - T = config.T
      - doping_profile = config.doping_profile
      - N_D = config.N_D  (pass through; may be None for graded)
      - N_D_junction = config.N_D_junction
      - N_D_bulk = config.N_D_bulk
      - L_transition = config.L_transition_um * 1e-4
      - device_name = device_name or a unique name via uuid (follow the `f"..._{uuid.uuid4().hex[:8]}"` pattern already used in cv_analysis.py)

    2D branch (`config.half_width_um is not None`): MUST produce a DD-initialized device, mirroring `charge_collection_2d.py` lines 140-157 exactly. Steps, in order:
      1. Call `petringa.core.device2d.create_sic_2d_device(...)` with half_width_um = config.half_width_um (passed in MICROMETERS, NOT converted), and the same um->cm conversions for epi/substrate thickness and L_transition_um as the 1D branch, plus N_A, T, doping_profile, N_D, N_D_junction, N_D_bulk mapped explicitly. `create_sic_2d_device` builds ONLY the mesh + doping — it does NOT run Poisson/DD.
      2. `setup_poisson(device_info)` (from petringa.core.poisson)
      3. `solve_equilibrium(device_info)` (from petringa.core.poisson)
      4. `setup_sic_drift_diffusion(device_info)` (from petringa.core.drift_diffusion) — THIS IS REQUIRED. Without it the returned device is NOT DD-initialized and run_field's `ramp_bias` (Plan 03) will fail its `dd_initialized=True` precondition.
      5. `device_info["dd_initialized"] = True`
    This makes the 2D branch genuinely consistent with the 1D branch (both end DD-initialized), which is the same dimension-agnostic pattern already used by charge_collection_2d.py's 2D CCE flow.

    Return the raw devsim `device_info` dict. `build_device` performs NO physics changes to the models themselves — it is a pure argument-marshalling + unit-conversion + standard-DD-setup facade over existing core constructors, so both dimensions return a solved-at-equilibrium, DD-initialized device ready to ramp.

    Use absolute imports (`from petringa.core.drift_diffusion import create_dd_device, setup_sic_drift_diffusion`, `from petringa.core.device2d import create_sic_2d_device`, `from petringa.core.poisson import setup_poisson, solve_equilibrium`, etc.). Do NOT `from petringa import ...`.

  </action>
  <acceptance_criteria>
    - `python -c "from petringa.api.device import DeviceConfig, build_device"` exits 0
    - `python -c "from petringa.api.device import DeviceConfig; import dataclasses; f={x.name for x in dataclasses.fields(DeviceConfig)}; assert f=={'epi_thickness_um','substrate_thickness_um','half_width_um','N_A','doping_profile','N_D','N_D_junction','N_D_bulk','L_transition_um','T','area_cm2'}, f"` exits 0
    - `python -c "from petringa.api.device import DeviceConfig; c=DeviceConfig(); assert c.N_D_junction==2.93e15 and c.N_D_bulk==8.82e13 and c.L_transition_um==0.987 and c.doping_profile=='graded'"` exits 0 (defaults match spec, not core)
    - `grep -n '1e-4\|\* 1e4\|/ 1e4' petringa/api/device.py` shows um->cm conversions present on epi_thickness, substrate_thickness, and L_transition
    - `grep -n 'from petringa import' petringa/api/device.py` returns nothing
    - Source assertion (1D branch): build_device's 1D branch references `create_dd_device` and 2D branch references `create_sic_2d_device` (verify via `grep -n 'create_dd_device\|create_sic_2d_device' petringa/api/device.py` showing both)
    - Source assertion (2D branch DD-init, BLOCKER FIX): `grep -n 'setup_sic_drift_diffusion' petringa/api/device.py` returns a match — the 2D branch runs DD setup so the returned device is ramp-able. AND `grep -n 'dd_initialized' petringa/api/device.py` shows the 2D branch sets `dd_initialized=True` (the 1D branch gets it from create_dd_device; the 2D branch must set it explicitly, mirroring charge_collection_2d.py:153)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from petringa.api.device import DeviceConfig, build_device; import dataclasses, inspect; f={x.name for x in dataclasses.fields(DeviceConfig)}; assert f=={'epi_thickness_um','substrate_thickness_um','half_width_um','N_A','doping_profile','N_D','N_D_junction','N_D_bulk','L_transition_um','T','area_cm2'}; c=DeviceConfig(); assert c.N_D_junction==2.93e15 and c.L_transition_um==0.987; s=inspect.getsource(build_device); assert 'setup_sic_drift_diffusion' in s and 'dd_initialized' in s, '2D branch must DD-initialize'"</automated>
  </verify>
  <done>DeviceConfig lives in petringa/api/device.py with all 11 fields and spec-correct defaults; build_device() dispatches 1D vs 2D, converts um->cm for thicknesses, passes half_width_um in um, passes every field explicitly with no reliance on core defaults; BOTH branches return a DD-initialized device (dd_initialized=True) — the 2D branch runs setup_poisson+solve_equilibrium+setup_sic_drift_diffusion mirroring charge_collection_2d.py:140-157, so downstream run_field can ramp either dimension.</done>
</task>

<task type="auto">
  <name>Task 3: Re-export public API from petringa/__init__.py and remove the stub</name>
  <read_first>
    - petringa/__init__.py (current file — remove the inline DeviceConfig dataclass; it now lives in api/device.py)
    - petringa/api/device.py (the promoted DeviceConfig — confirm import path)
    - petringa/api/results.py (SimResult, MeshData — confirm import path)
    - docs/superpowers/specs/2026-06-26-simulator-library-ui-design.md (section 4 Phase 1 — "__init__.py exports: DeviceConfig, SimResult, run_cv, run_field"; run_cv/run_field are added in Plans 02/03, so __init__ must not import them yet)
  </read_first>
  <files>petringa/__init__.py</files>
  <action>
    Rewrite `petringa/__init__.py` to remove the inline `DeviceConfig` dataclass definition (it is now in `petringa.api.device`) and instead re-export the public contract:
      - `from petringa.api.device import DeviceConfig`
      - `from petringa.api.results import SimResult, MeshData`
      - keep `from petringa._version import __version__`
    Set `__all__ = ["DeviceConfig", "SimResult", "MeshData", "__version__"]`.

    Do NOT import `run_cv` / `run_field` here — those functions do not exist until Plans 02 and 03. Plan 02 and Plan 03 will each append their own re-export line to `__init__.py`. Leave a short comment `# run_cv, run_field added by Phase 36 Plans 02/03` at the `__all__` line so the next executor knows where to extend.

    Note: `import petringa` now transitively imports `petringa.api.device` -> `petringa.core.*` -> devsim at package import time. This is expected and acceptable (devsim is a hard runtime dependency).

  </action>
  <acceptance_criteria>
    - `python -c "from petringa import DeviceConfig, SimResult, MeshData, __version__"` exits 0
    - `python -c "import petringa; assert set(petringa.__all__) >= {'DeviceConfig','SimResult','MeshData','__version__'}"` exits 0
    - `grep -c '@dataclass' petringa/__init__.py` returns 0 (the stub dataclass is gone from __init__)
    - `python -c "from petringa import DeviceConfig; from petringa.api.device import DeviceConfig as D2; assert DeviceConfig is D2"` exits 0 (single canonical class, no duplicate)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from petringa import DeviceConfig, SimResult, MeshData, __version__; from petringa.api.device import DeviceConfig as D2; assert DeviceConfig is D2; import petringa; assert set(petringa.__all__) >= {'DeviceConfig','SimResult','MeshData','__version__'}"</automated>
  </verify>
  <done>petringa/__init__.py re-exports DeviceConfig (from api.device), SimResult and MeshData (from api.results), and __version__; the inline stub dataclass is removed; run_cv/run_field intentionally not yet imported.</done>
</task>

</tasks>

<verification>
- `python -c "from petringa import DeviceConfig, SimResult, MeshData"` succeeds (LIB-01 import surface established)
- DeviceConfig has all 11 spec fields with spec-correct defaults (2.93e15 / 8.82e13 / 0.987, not the core 2.9e15 / 8.5e13 / 1e-4)
- No import cycle: `python -c "import petringa"` succeeds; results.py and device.py contain no `from petringa import` lines
- build_device dispatches 1D (create_dd_device) vs 2D (create_sic_2d_device + setup_poisson + solve_equilibrium + setup_sic_drift_diffusion) with correct um->cm handling; both branches return dd_initialized=True
- Existing suite unaffected — spot check per-file isolation: `pytest tests/test_cv.py -q` still passes (imports petringa.core.*, unaffected by api additions)
</verification>

<success_criteria>

- LIB-01 satisfied: DeviceConfig importable from petringa and instantiable with named geometry/doping/operating parameters
- SimResult and MeshData dataclasses exist with exact spec field layout, ready for Plans 02/03 to populate
- build_device() shared facade ready for run_cv (Plan 02) and run_field (Plan 03) to consume; both 1D and 2D branches return a DD-initialized device so run_field can ramp either dimension
- petringa/**init**.py is the single public entry point with no duplicate DeviceConfig definition
  </success_criteria>

<output>
Create `.planning/phases/36-core-api-deviceconfig-c-v-field-vertical-slice/36-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
