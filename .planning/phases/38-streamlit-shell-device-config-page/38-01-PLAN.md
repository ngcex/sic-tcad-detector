---
phase: 38-streamlit-shell-device-config-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/__init__.py
  - app/components/__init__.py
  - app/components/device_sidebar.py
  - tests/test_app_device_sidebar.py
  - tests/test_app_session.py
autonomous: true
requirements: [UI-02, UI-07]

must_haves:
  truths:
    - "A pure assemble_config(values: dict) -> DeviceConfig function turns form values into a DeviceConfig with all 11 fields populated, with zero Streamlit runtime dependency on that code path"
    - "Graded profile yields N_D=None with the graded triplet set; uniform profile yields a set N_D"
    - "1D selection yields half_width_um=None; 2D selection yields a float half_width_um"
    - "The assembled DeviceConfig round-trips through a plain dict stored under a single 'device_config' key without loss (UI-07 persistence contract at logic level)"
    - "pytest can collect and run tests/test_app_*.py because the project root is on the import path"
  artifacts:
    - path: "app/components/device_sidebar.py"
      provides: "assemble_config() pure seam + render_device_sidebar() Streamlit renderer"
      contains: "def assemble_config"
      min_lines: 40
    - path: "tests/test_app_device_sidebar.py"
      provides: "UI-02 unit coverage (config assembly, doping-mode mapping, dimensionality mapping)"
      contains: "test_assemble_config_all_fields"
    - path: "tests/test_app_session.py"
      provides: "UI-07 persistence-key contract test"
      contains: "test_config_persistence_key"
    - path: "pyproject.toml"
      provides: '[tool.pytest.ini_options] pythonpath = ["."] so app.* imports resolve under pytest'
      contains: "pythonpath"
  key_links:
    - from: "app/components/device_sidebar.py"
      to: "petringa.DeviceConfig"
      via: "import + DeviceConfig(**values)"
      pattern: "from petringa import DeviceConfig"
    - from: "tests/test_app_device_sidebar.py"
      to: "app.components.device_sidebar.assemble_config"
      via: "direct import (no Streamlit runtime)"
      pattern: "from app.components.device_sidebar import assemble_config"
---

<objective>
Establish the Phase 38 config core: a pure, Streamlit-free `assemble_config(values: dict) -> DeviceConfig` seam plus the `render_device_sidebar()` Streamlit renderer that calls it and stores the result under the single non-widget key `st.session_state["device_config"]`. Add the pytest path configuration so `app.*` modules import under pytest, and the Wave-0 unit tests that lock UI-02 (config assembly + mode mapping) and UI-07 (persistence-key contract).

Purpose: Phases 39–43 read `st.session_state["device_config"]` as their input contract. This plan fixes that contract and makes the config-assembly logic unit-testable without a browser or a running Streamlit server. It is TDD-first: the failing tests define the exact behavior before the implementation.

Output: `app/components/device_sidebar.py` (with `assemble_config` + `render_device_sidebar`), `pyproject.toml` pytest config, `tests/test_app_device_sidebar.py`, `tests/test_app_session.py`, and the `app/` package `__init__.py` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-streamlit-shell-device-config-page/38-RESEARCH.md
@.planning/phases/38-streamlit-shell-device-config-page/38-UI-SPEC.md
@.planning/phases/38-streamlit-shell-device-config-page/38-VALIDATION.md

<interfaces>
<!-- The DeviceConfig contract this plan populates. Read from petringa/api/device.py — do NOT redefine these fields in the app. -->

From petringa/api/device.py (all 11 fields, with defaults):

- epi_thickness_um: float = 10.0
- substrate_thickness_um: float = 1.0
- half_width_um: Optional[float] = None # None = 1D, float = 2D
- N_A: float = 1e19
- doping_profile: str = "graded" # "graded" | "uniform"
- N_D: Optional[float] = None # set only for "uniform"
- N_D_junction: float = 2.93e15 # graded profile
- N_D_bulk: float = 8.82e13 # graded profile
- L_transition_um: float = 0.987 # graded profile
- T: float = 300.0
- area_cm2: float = 1e-4

Public import path (verified in petringa/**init**.py): `from petringa import DeviceConfig`
</interfaces>

<verified_import_convention>
<!-- Proven empirically this session (see planner notes). Use this convention verbatim. -->

- App code lives under a real `app/` package: `app/__init__.py`, `app/components/__init__.py` (all empty).
- `pyproject.toml` gets `[tool.pytest.ini_options]` with `pythonpath = ["."]` so `from app.components.device_sidebar import ...` resolves when pytest runs from the project root.
- `assemble_config(values: dict) -> DeviceConfig` MUST have ZERO Streamlit calls — it is imported directly by unit tests. `render_device_sidebar()` is the only function that touches `st.*`.
- Direct import `from app.components.device_sidebar import assemble_config` from the project root was verified to work with the pythonpath setting.
</verified_import_convention>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pytest pythonpath config + app package scaffold</name>
  <files>pyproject.toml, app/__init__.py, app/components/__init__.py</files>
  <read_first>
    - pyproject.toml (see current dev-extras and hatch config; there is NO existing [tool.pytest.ini_options] section — confirm before adding)
  </read_first>
  <behavior>
    - After change: `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['tool']['pytest']['ini_options']['pythonpath']==['.']"` succeeds.
    - `app/`, `app/components/` are importable packages (empty `__init__.py` present in each).
  </behavior>
  <action>
    Add a `[tool.pytest.ini_options]` table to `pyproject.toml` with `pythonpath = ["."]` (this is the ONLY pytest option required — do not add testpaths or markers). This makes `app.*` imports resolve when pytest runs from the project root, which the config-assembly unit tests depend on. Do NOT introduce or reference `pytest -q` full-suite runs anywhere — per STATE.md line 98 / PKG-03, bare single-process `pytest -q` is unsatisfiable on this machine due to devsim resource exhaustion; all verification in this phase scopes to `pytest tests/test_app_*.py`. Create the `app/` package with empty `app/__init__.py` and `app/components/__init__.py`.
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['tool']['pytest']['ini_options']['pythonpath']==['.'], d['tool']['pytest']; print('pythonpath OK')" && test -f app/__init__.py && test -f app/components/__init__.py && echo "packages OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains a `[tool.pytest.ini_options]` section with `pythonpath = ["."]`.
    - Files `app/__init__.py` and `app/components/__init__.py` exist and are empty (0 bytes acceptable).
    - No occurrence of a bare `pytest -q` command is introduced into any file in this task.
  </acceptance_criteria>
  <done>pytest pythonpath configured; app package scaffold present; no bare `pytest -q` introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing unit tests for assemble_config + persistence key (RED)</name>
  <files>tests/test_app_device_sidebar.py, tests/test_app_session.py</files>
  <read_first>
    - petringa/api/device.py (DeviceConfig field names, types, defaults — the exact 11 fields the tests assert on)
    - tests/test_api_device.py (existing test style/conventions for DeviceConfig — match import and assertion patterns)
    - .planning/phases/38-streamlit-shell-device-config-page/38-VALIDATION.md (Per-Task Verification Map — the exact test function names required)
  </read_first>
  <behavior>
    - test_assemble_config_all_fields: assemble_config({...all 11 fields...}) returns a DeviceConfig whose every field in DeviceConfig.__dataclass_fields__ equals the supplied value (guards against silent field drift — iterate __dataclass_fields__, assert each is populated).
    - test_doping_mode_mapping: for values with doping_profile="graded", result.N_D is None and result.N_D_junction/N_D_bulk/L_transition_um are set; for doping_profile="uniform", result.N_D is a float.
    - test_dimensionality_mapping: values with half_width_um=None → result.half_width_um is None (1D); values with half_width_um=50.0 → result.half_width_um == 50.0 (2D).
    - test_config_persistence_key (in test_app_session.py): a DeviceConfig assembled by assemble_config, placed in a plain dict under key "device_config" and retrieved, is field-for-field identical (round-trip through a dict simulates st.session_state cross-page persistence, UI-07).
  </behavior>
  <action>
    Create `tests/test_app_device_sidebar.py` with the three test functions named exactly `test_assemble_config_all_fields`, `test_doping_mode_mapping`, `test_dimensionality_mapping` (names are fixed by 38-VALIDATION.md's verification map). Each imports `from app.components.device_sidebar import assemble_config` and `from petringa import DeviceConfig`. Create `tests/test_app_session.py` with `test_config_persistence_key`. Build the input dicts with all 11 DeviceConfig field names as keys (epi_thickness_um, substrate_thickness_um, half_width_um, N_A, doping_profile, N_D, N_D_junction, N_D_bulk, L_transition_um, T, area_cm2). Assert field equality by iterating `DeviceConfig.__dataclass_fields__` so the test fails if the sidebar ever drops a field. These tests MUST fail now (RED) because `assemble_config` does not exist yet — that is the expected state at the end of this task. Do NOT stub `assemble_config` here.
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_device_sidebar.py tests/test_app_session.py -x 2>&1 | tail -5; python -m pytest tests/test_app_device_sidebar.py --collect-only -q 2>&1 | grep -c "test_" | xargs -I{} sh -c 'test {} -ge 3 && echo "3+ tests collected"'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_app_device_sidebar.py` defines exactly the functions `test_assemble_config_all_fields`, `test_doping_mode_mapping`, `test_dimensionality_mapping` (verified: `grep -c "def test_" tests/test_app_device_sidebar.py` returns 3).
    - `tests/test_app_session.py` defines `test_config_persistence_key` (verified: `grep -q "def test_config_persistence_key" tests/test_app_session.py`).
    - Both test files import `assemble_config` from `app.components.device_sidebar` (verified: `grep -q "from app.components.device_sidebar import assemble_config"`).
    - Running the tests now fails with a collection/import error or assertion failure (RED state) because `assemble_config` is not yet implemented — this is the required end state of this task.
  </acceptance_criteria>
  <done>Four named tests exist and fail (RED) because assemble_config is unimplemented; test collection finds ≥3 tests in the sidebar file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement assemble_config + render_device_sidebar (GREEN)</name>
  <files>app/components/device_sidebar.py</files>
  <read_first>
    - petringa/api/device.py (DeviceConfig constructor — the exact kwargs to pass)
    - .planning/phases/38-streamlit-shell-device-config-page/38-UI-SPEC.md (Sidebar Device-Config Contract: field grouping, labels-with-units, widget types, defaults, visibility rules, format="%.3e" for doping/area)
    - .planning/phases/38-streamlit-shell-device-config-page/38-RESEARCH.md (Pattern 2 reactive selectors, Pattern 3 single non-widget key, Code Examples section)
    - tests/test_app_device_sidebar.py (the behavior contract this implements — written in Task 2)
  </read_first>
  <action>
    Implement `assemble_config(values: dict) -> DeviceConfig` as a PURE function with ZERO Streamlit calls: it constructs `DeviceConfig(**values)` (or explicit named kwargs from the dict) covering all 11 fields, and applies the mode-consistency rules so the returned config is always complete: when `values["doping_profile"] == "graded"`, force `N_D=None` and keep the graded triplet; when `"uniform"`, keep `N_D` set. This is the function the unit tests import directly — it must not import or call `st`. Then implement `render_device_sidebar() -> None` which: renders the reactive mode selectors OUTSIDE any `st.form` (dimensionality radio `["1D","2D"]` default "1D" gating half_width_um; doping-profile selectbox `["graded","uniform"]` default "graded" gating the N_D fields) per 38-UI-SPEC Group 1; renders geometry/doping/operating widgets grouped with `st.sidebar.header` + `st.sidebar.divider()` per Groups 2–4, using the exact labels-with-units and defaults in 38-UI-SPEC, with `format="%.3e"` on all doping (N_A, N_D, N_D_junction, N_D_bulk) and area_cm2 fields, and `min_value` guards where the spec lists them; collects the widget values into a dict; calls `assemble_config(values)`; and stores the result under the single non-widget key `st.session_state["device_config"]`. When a gated field is hidden set its value programmatically (1D → half_width_um=None; uniform → graded triplet = the DeviceConfig defaults; graded → N_D=None) so the dict passed to assemble_config always has all 11 keys. Do NOT wrap the sidebar in `st.form`. Do NOT add `st.cache_resource`/`st.cache_data` (research: unwarranted this phase). Tests must now pass (GREEN).
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_device_sidebar.py tests/test_app_session.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_app_device_sidebar.py tests/test_app_session.py -x` reports all 4 tests passing (GREEN).
    - `assemble_config` contains zero references to `st.` or `streamlit` (verified: `grep -v '^#' app/components/device_sidebar.py | sed -n '/def assemble_config/,/^def /p' | grep -c 'st\.'` returns 0).
    - `render_device_sidebar` writes `st.session_state["device_config"]` (verified: `grep -q 'st.session_state\["device_config"\]' app/components/device_sidebar.py`).
    - The sidebar is NOT wrapped in `st.form` (verified: `grep -c "st.form" app/components/device_sidebar.py` returns 0).
    - All doping and area fields use `format="%.3e"` (verified: `grep -c '%.3e' app/components/device_sidebar.py` returns ≥5 for N_A, N_D, N_D_junction, N_D_bulk, area_cm2).
  </acceptance_criteria>
  <done>All 4 app-config unit tests pass; assemble_config is Streamlit-free; sidebar stores under the single device_config key without st.form.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                                       |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| user → sidebar numeric inputs | Untrusted numeric form values cross into DeviceConfig assembly. Local single-user tool; no network, no auth, no file upload, no simulation execution in Phase 38. |

## STRIDE Threat Register

| Threat ID       | Category        | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                        |
| --------------- | --------------- | --------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-01         | Tampering       | assemble_config / DeviceConfig kwargs         | mitigate    | Assemble with explicit named kwargs / `DeviceConfig(**values)` on a fixed 11-key dict — no `setattr`/`eval`, no dynamic attribute injection. `[VERIFIED: petringa/api/sweep.py uses dataclasses.replace, same injection-safe pattern]` |
| T-38-02         | Tampering / DoS | numeric form inputs (thickness, doping, area) | mitigate    | `st.number_input` `min_value` guards per 38-UI-SPEC (epi ≥0.1, substrate ≥0.0, half-width ≥0.1, T ≥1.0). No simulation runs in Phase 38 so blast radius of a bad value is nil.                                                         |
| T-38-03         | Tampering       | numeric form inputs (arbitrary code)          | accept      | ASVS V5 light: numeric inputs only, no eval/exec on input. Local single-user tool, no privilege boundary. Accept residual risk.                                                                                                        |
| T-38-SC         | Tampering       | npm/pip/cargo installs                        | accept      | No package installs this phase — all deps (streamlit, petringa) already declared in pyproject.toml and installed. `[VERIFIED: 38-RESEARCH Package Legitimacy Audit — none added]` No slopcheck gate required.                          |
| </threat_model> |

<verification>
- `python -m pytest tests/test_app_device_sidebar.py tests/test_app_session.py -x` — all 4 tests green.
- `assemble_config` is importable and Streamlit-free (direct `python -c "from app.components.device_sidebar import assemble_config"` succeeds).
- Do NOT run bare `pytest -q` (unsatisfiable per STATE.md line 98). App tests build no devsim devices, so `tests/test_app_*.py` runs safely in isolation.
</verification>

<success_criteria>

- A pure `assemble_config(values: dict) -> DeviceConfig` exists, is unit-tested, and has no Streamlit dependency.
- `render_device_sidebar()` writes a complete DeviceConfig under `st.session_state["device_config"]` with reactive (non-form) mode selectors.
- `pyproject.toml` carries `[tool.pytest.ini_options] pythonpath = ["."]` so app tests resolve.
- UI-02 (all-field assembly + mode mapping) and UI-07 (persistence-key round-trip) are covered by green tests.
  </success_criteria>

<output>
Create `.planning/phases/38-streamlit-shell-device-config-page/38-01-SUMMARY.md` when done.
</output>
