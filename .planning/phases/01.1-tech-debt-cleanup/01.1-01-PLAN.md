---
phase: 01.1-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/01_phase1_validation.ipynb
  - tests/test_plotting.py
  - src/poisson.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Notebook runs top-to-bottom without NameError (no dead imports)"
    - "All 5 plotting functions have pytest coverage that passes"
    - "extract_depletion_width docstring warns that W is analytical, not numerical"
  artifacts:
    - path: "notebooks/01_phase1_validation.ipynb"
      provides: "Clean import cell with no dead imports"
      contains: "from src.sic_material import SiC4H_Parameters"
    - path: "tests/test_plotting.py"
      provides: "Plot function smoke tests"
      min_lines: 80
    - path: "src/poisson.py"
      provides: "Updated extract_depletion_width docstring"
      contains: "analytical"
  key_links:
    - from: "tests/test_plotting.py"
      to: "src/plotting.py"
      via: "import and call each public function"
      pattern: "from src\\.plotting import"
---

<objective>
Clean up Phase 1 tech debt: remove dead imports from the validation notebook, add pytest coverage for the plotting module, and document the analytical-W contract on extract_depletion_width for Phase 2 handoff.

Purpose: Eliminate tech debt before Phase 2 builds on this foundation. The docstring fix is especially critical -- Phase 2 drift-diffusion code must not assume extract_depletion_width returns numerically-solved W.
Output: Clean notebook, new test_plotting.py, updated poisson.py docstring
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-tech-debt-cleanup/01.1-RESEARCH.md

@src/plotting.py
@src/poisson.py
@notebooks/01_phase1_validation.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead imports from notebook import cell</name>
  <files>notebooks/01_phase1_validation.ipynb</files>
  <action>
Edit the notebook JSON to remove exactly these 6 dead imports from cell 1 (the import cell, which is the second cell -- index 1 after the markdown header cell):

Dead imports to remove:

- `compute_ni` from `src.sic_material`
- `mobility_caughey_thomas` from `src.sic_material`
- `voltage_sweep` from `src.poisson`
- `plot_electric_field_multi` from `src.plotting`
- `plot_doping_profile` from `src.plotting`
- `save_figure` from `src.plotting`

Use Python `json` stdlib to load the .ipynb, modify only the `cell['source']` lines, and write back with `json.dump(indent=1)`. Do NOT modify cell metadata, outputs, or notebook-level metadata.

The import lines are grouped by module. After removal, the remaining imports from each module should still be valid Python (no trailing commas on last import, no empty import statements). If a module's import line becomes empty after removal, remove the entire import line.

Specifically:

- The `from src.sic_material import ...` line should keep only `SiC4H_Parameters`
- The `from src.poisson import ...` line should keep: `create_sic_device, DEFAULT_N_D, setup_poisson, solve_equilibrium, extract_electric_field, extract_depletion_width, extract_depletion_width_numerical`
- The `from src.plotting import ...` line should keep: `plot_electric_field, plot_depletion_width_vs_bias`

After editing, verify the notebook JSON is valid by loading it back with json.load.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -c "import json; nb=json.load(open('notebooks/01_phase1_validation.ipynb')); cell_src=''.join(nb['cells'][1]['source']); assert 'compute_ni' not in cell_src; assert 'mobility_caughey_thomas' not in cell_src; assert 'voltage_sweep' not in cell_src; assert 'plot_electric_field_multi' not in cell_src; assert 'plot_doping_profile' not in cell_src; assert 'save_figure' not in cell_src; assert 'SiC4H_Parameters' in cell_src; assert 'plot_electric_field' in cell_src; print('PASS: dead imports removed, live imports preserved')"</automated>
</verify>
<done>Notebook import cell contains only imports that are actually used in subsequent cells. The 6 dead imports are gone; all live imports remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add pytest coverage for plotting module</name>
  <files>tests/test_plotting.py</files>
  <action>
Create `tests/test_plotting.py` with smoke tests for all 5 public functions in `src/plotting.py`. Follow the existing test patterns (class-based grouping, pytest.approx for numerics, self-contained tests).

Structure:

1. Set matplotlib backend to 'Agg' at top of file (before importing pyplot) to avoid display errors
2. Import all 5 functions from src.plotting
3. Each test class has `teardown_method` calling `plt.close('all')` to prevent memory leaks

Test classes and methods:

**class TestPlotElectricField:**

- `test_returns_axes`: Call with synthetic x (np.linspace(0, 10e-4, 100)) and E (np.linspace(5e4, 0, 100)), assert returns matplotlib.axes.Axes
- `test_plots_data`: Assert ax.get_lines() is non-empty after call
- `test_uses_provided_ax`: Create fig,ax with plt.subplots(), pass ax=ax, assert returned ax is same object
- `test_sets_labels`: Assert ax.get_xlabel() and ax.get_ylabel() are non-empty strings

**class TestPlotElectricFieldMulti:**

- `test_returns_axes`: Create results_dict with 'voltages' list (e.g., [0, -5, -10, -20, -30]) and 'E_fields' list of (x, E) tuples (synthetic data), assert returns Axes
- `test_plots_multiple_curves`: Assert len(ax.get_lines()) > 1

**class TestPlotDepletionWidthVsBias:**

- `test_returns_axes`: Call with voltages=np.linspace(0, -30, 10) and W_numerical=np.linspace(1.7e-4, 9e-4, 10), assert returns Axes
- `test_with_analytical`: Pass W_analytical array too, assert more lines plotted
- `test_default_experimental_data`: Call without W_experimental, assert experimental points are plotted (check for line with 'ro' marker or just check total lines >= 2)

**class TestPlotDopingProfile:**

- `test_returns_axes`: Call with x and mixed net_doping (positive n-type and negative p-type regions), assert returns Axes
- `test_all_positive_doping`: Call with all-positive array, assert no error
- `test_all_negative_doping`: Call with all-negative array, assert no error

**class TestSaveFigure:**

- `test_creates_png_and_pdf(tmp_path)`: Create a simple figure, monkeypatch `os.path.dirname` in `src.plotting` module OR directly set the figures directory. Simplest approach: use monkeypatch to temporarily replace `plotting.os.path.dirname` to return `str(tmp_path)` so that `fig_dir` resolves to `tmp_path/figures`. Then call save_figure(fig, "test_output") and assert both `tmp_path/figures/test_output.png` and `tmp_path/figures/test_output.pdf` exist.

All test data should be synthetic numpy arrays -- no devsim dependency needed.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -m pytest tests/test_plotting.py -v</automated>
</verify>
<done>All 5 plotting functions have at least one passing test. `pytest tests/test_plotting.py -v` shows all green. No regressions in existing tests (`pytest tests/ -v` all green).</done>
</task>

<task type="auto">
  <name>Task 3: Document analytical-W contract on extract_depletion_width</name>
  <files>src/poisson.py</files>
  <action>
Update the docstring of `extract_depletion_width` (at line 268 of src/poisson.py) to add a Note section warning that this returns analytically-derived W, not numerically-extracted W. This is critical for Phase 2 handoff.

Add the following after the existing Returns section, before the closing triple-quotes:

```
    Note
    ----
    This returns W computed from the **analytical** one-sided depletion
    approximation (W = sqrt(2*eps*(V_bi - V)/(q*N_D))), NOT a W extracted
    from the numerical Poisson electric field solution. The analytical
    formula was validated against the numerical equilibrium solution at
    0V (agreement within ~20%), but under reverse bias it does not account
    for carrier redistribution or non-abrupt junction profiles.

    Phase 2 drift-diffusion code should NOT assume this function returns
    a numerically-derived depletion width.

    See Also
    --------
    extract_depletion_width_numerical : Extracts W from the numerical
        Poisson E-field solution (equilibrium only, uses 1% threshold).
```

Do NOT change any code logic -- only the docstring.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -c "from src.poisson import extract_depletion_width; d=extract_depletion_width.**doc**; assert 'analytical' in d.lower(); assert 'NOT' in d or 'not' in d.lower(); assert 'Phase 2' in d; assert 'extract_depletion_width_numerical' in d; print('PASS: docstring contains analytical-W warning and See Also')"</automated>
</verify>
<done>extract_depletion_width docstring explicitly warns that it returns analytical W (not numerical), references extract_depletion_width_numerical, and warns Phase 2 code about the distinction.</done>
</task>

</tasks>

<verification>
1. `cd /Users/ngcex/projects/physics/petringa && python -m pytest tests/ -v` -- all tests pass (including new test_plotting.py), no regressions
2. Notebook JSON is valid and dead imports removed (Task 1 verify command)
3. Docstring contains analytical-W warning (Task 3 verify command)
</verification>

<success_criteria>

- Notebook import cell has exactly 0 dead imports; all 6 confirmed-dead imports removed
- tests/test_plotting.py exists with passing tests for all 5 plotting functions
- extract_depletion_width docstring contains explicit analytical-W warning and See Also section
- All existing tests still pass (no regressions)
  </success_criteria>

<output>
After completion, create `.planning/phases/01.1-tech-debt-cleanup/01.1-01-SUMMARY.md`
</output>
