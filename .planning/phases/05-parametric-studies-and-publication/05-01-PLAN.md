---
phase: 05-parametric-studies-and-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/flash_recombination.py
  - tests/test_flash_recombination.py
autonomous: true
requirements: [FLASH-04]

must_haves:
  truths:
    - "parametric_cce_sweep runs across epi/doping/bias combinations and returns structured results"
    - "cce_vs_dose_rate accepts N_D_junction, N_D_bulk, L_transition as parameters instead of hardcoding them"
    - "Results can be saved to JSON and reloaded without re-running the sweep"
  artifacts:
    - path: "src/flash_recombination.py"
      provides: "parametric_cce_sweep function and doping-parametrized cce_vs_dose_rate"
      exports:
        [
          "parametric_cce_sweep",
          "cce_vs_dose_rate",
          "save_parametric_results",
          "load_parametric_results",
        ]
    - path: "tests/test_flash_recombination.py"
      provides: "Tests for parametric sweep and doping-parametrized cce_vs_dose_rate"
  key_links:
    - from: "src/flash_recombination.py::parametric_cce_sweep"
      to: "src/flash_recombination.py::cce_vs_dose_rate"
      via: "itertools.product loop calling cce_vs_dose_rate for each combination"
      pattern: "cce_vs_dose_rate.*N_D_junction.*N_D_bulk"
    - from: "src/flash_recombination.py::parametric_cce_sweep"
      to: "src/drift_diffusion.py::create_dd_device"
      via: "cce_vs_dose_rate passes doping params to create_dd_device"
      pattern: "create_dd_device.*N_D_junction"
---

<objective>
Parametric CCE sweep infrastructure: extend cce_vs_dose_rate to accept doping parameters and create a multi-dimensional parametric_cce_sweep wrapper with JSON caching.

Purpose: FLASH-04 requires sweeping CCE vs dose-rate across 4 epi thicknesses, 4 doping levels, and 3 bias voltages. The existing cce_vs_dose_rate hardcodes doping. This plan makes it parametric and wraps it in a sweep function.

Output: Extended cce_vs_dose_rate, parametric_cce_sweep function, save/load helpers, tests.
</objective>

<execution_context>
@/Users/ngcex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ngcex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-parametric-studies-and-publication/05-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs -->

From src/flash_recombination.py:

```python
def cce_vs_dose_rate(
    dose_rates_Gy_s,
    V_bias=-30.0,
    epi_thickness_cm=10e-4,
    E_MeV=62,
    n_continuation_steps=5,
):
    """Returns dict with: dose_rates, cce_values, cce_no_auger_ref, V_bias, epi_thickness_cm, E_MeV"""
```

Note: Lines 289-291 hardcode N_D_junction=2.90e15, N_D_bulk=8.50e13, L_transition=1.0e-4 in the create_dd_device call.

From src/drift_diffusion.py:

```python
def create_dd_device(doping_profile="graded", **kwargs):
    """Accepts N_D_junction, N_D_bulk, L_transition via **kwargs."""
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parametrize cce_vs_dose_rate doping and create parametric_cce_sweep</name>
  <files>src/flash_recombination.py</files>
  <action>
1. Modify `cce_vs_dose_rate` signature to accept optional doping parameters:
   - Add `N_D_junction=2.90e15`, `N_D_bulk=8.50e13`, `L_transition=1.0e-4` as keyword arguments (default values match current hardcoded values for backward compatibility).
   - Update the `create_dd_device` call on lines ~285-292 to pass these parameters instead of hardcoded literals.
   - Update the return dict to include `N_D_junction`, `N_D_bulk`, `L_transition`.
   - Do the same for the no-Auger reference device creation (find the second `create_dd_device` call in the function).

2. Add `parametric_cce_sweep` function after `cce_vs_dose_rate`:

   ```python
   def parametric_cce_sweep(
       dose_rates_Gy_s,
       epi_thicknesses_cm=(5e-4, 10e-4, 15e-4, 20e-4),
       N_D_bulk_values=(5e13, 1e14, 2e14, 5e14),
       bias_voltages=(-10.0, -30.0, -50.0),
       E_MeV=62,
       N_D_junction_base=2.90e15,
       N_D_bulk_base=8.50e13,
       L_transition=1.0e-4,
       n_continuation_steps=5,
   ):
   ```

   - Use `itertools.product(epi_thicknesses_cm, N_D_bulk_values, bias_voltages)` for combinations.
   - For each N_D_bulk value, scale N_D_junction proportionally: `N_D_junction = N_D_junction_base * (N_D_bulk / N_D_bulk_base)` to preserve graded profile shape.
   - Call `cce_vs_dose_rate(dose_rates_Gy_s, V_bias=V, epi_thickness_cm=epi, E_MeV=E_MeV, N_D_junction=N_D_j, N_D_bulk=N_D_b, L_transition=L_transition, n_continuation_steps=n_continuation_steps)` for each combination.
   - Wrap each call in try/except: on failure, log warning and store None for that combination (graceful degradation for solver divergence).
   - Log progress: `logger.info(f"Parametric sweep {i}/{total}: epi={epi*1e4:.0f}um, N_D_bulk={N_D_b:.1e}, V={V}V")`.
   - Return dict keyed by `(epi, N_D_bulk, V_bias)` tuples, values are the result dicts from cce_vs_dose_rate (or None on failure).

3. Add `save_parametric_results` and `load_parametric_results` functions:
   - `save_parametric_results(results, path)`: Convert tuple keys to strings via `str(key)`, convert numpy arrays to lists via `.tolist()`, write JSON with indent=2. Skip None entries.
   - `load_parametric_results(path)`: Read JSON, reconstruct tuple keys via `ast.literal_eval`, convert lists back to numpy arrays for "dose_rates" and "cce_values" fields.

4. Add necessary imports at top of file: `import itertools`, `import json`, `import ast` (if not already present).
   </action>
   <verify>
   <automated>cd /Users/ngcex/projects/physics/petringa && python -c "from src.flash_recombination import cce_vs_dose_rate, parametric_cce_sweep, save_parametric_results, load_parametric_results; print('All imports OK')"</automated>
   </verify>
   <done>cce_vs_dose_rate accepts N_D_junction/N_D_bulk/L_transition kwargs with backward-compatible defaults. parametric_cce_sweep loops over parameter combinations. save/load helpers handle JSON serialization with tuple keys.</done>
   </task>

<task type="auto">
  <name>Task 2: Add tests for parametric sweep and doping parametrization</name>
  <files>tests/test_flash_recombination.py</files>
  <action>
Add tests to the existing test file (do not remove existing tests):

1. `test_cce_vs_dose_rate_accepts_doping_params`: Call `cce_vs_dose_rate` with explicit `N_D_junction=2.90e15, N_D_bulk=8.50e13` (the defaults) and a single dose rate `[100.0]`. Assert it returns a dict with keys "dose_rates", "cce_values", "N_D_junction", "N_D_bulk", "L_transition". Assert result["N_D_junction"] == 2.90e15.

2. `test_save_load_parametric_results_roundtrip`: Create a mock results dict with two entries keyed by tuples, e.g. `{(10e-4, 8.5e13, -30.0): {"dose_rates": np.array([20, 100]), "cce_values": np.array([0.98, 0.97]), "V_bias": -30.0}}`. Save to a temp file via `save_parametric_results`, load back via `load_parametric_results`. Assert keys match, arrays are numpy arrays, values are close.

3. `test_parametric_cce_sweep_structure`: Call `parametric_cce_sweep` with minimal parameter space: `dose_rates_Gy_s=[100.0], epi_thicknesses_cm=(10e-4,), N_D_bulk_values=(8.50e13,), bias_voltages=(-30.0,)`. Assert result is a dict with exactly one key `(10e-4, 8.50e13, -30.0)`. Assert the value is a dict with "dose_rates" and "cce_values" keys.

Use `import tempfile` for the roundtrip test. Use `pytest.approx` for float comparisons.
</action>
<verify>
<automated>cd /Users/ngcex/projects/physics/petringa && python -m pytest tests/test_flash_recombination.py -x -v 2>&1 | tail -20</automated>
</verify>
<done>All existing and new tests pass. Doping parametrization, save/load roundtrip, and sweep structure verified.</done>
</task>

</tasks>

<verification>
1. `python -c "from src.flash_recombination import parametric_cce_sweep, save_parametric_results, load_parametric_results"` succeeds
2. `python -m pytest tests/test_flash_recombination.py -x -v` all pass
3. Existing tests still pass (backward compatibility of cce_vs_dose_rate default args)
</verification>

<success_criteria>

- cce_vs_dose_rate accepts N_D_junction, N_D_bulk, L_transition as kwargs with backward-compatible defaults
- parametric_cce_sweep iterates over epi/doping/bias combinations via itertools.product
- JSON save/load helpers handle tuple-keyed results
- All tests pass including existing ones (no regressions)
  </success_criteria>

<output>
After completion, create `.planning/phases/05-parametric-studies-and-publication/05-01-SUMMARY.md`
</output>
